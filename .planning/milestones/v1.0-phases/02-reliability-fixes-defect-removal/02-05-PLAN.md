---
phase: 02-reliability-fixes-defect-removal
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - scripts/run_all_tasks.py
autonomous: true
requirements: [FIX-03]
must_haves:
  truths:
    - "A single scripted run invokes all 11 runner tasks sequentially via the runner CLI from scripts/ with python3 (D-08)"
    - "Each task is asserted to exit 0 with a JSON_RESULT= line and no uncaught exception under normal conditions"
    - "The harness reports a per-task pass/fail summary and a non-zero overall exit if any task fails"
  artifacts:
    - path: "scripts/run_all_tasks.py"
      provides: "the FIX-03 clean-pass run-all harness over all 11 tasks"
      contains: "ALL_TASKS"
  key_links:
    - from: "run_all_tasks.py"
      to: "sports_system_runner.py CLI"
      via: "subprocess.Popen per --task, cwd=scripts/, exit-code + JSON_RESULT check"
      pattern: "JSON_RESULT="
---

<objective>
Build the FIX-03 clean-pass harness: a repeatable script that invokes all 11 runner tasks
sequentially through the runner CLI (from scripts/, with python3) and asserts each exits 0
with a JSON_RESULT= line and no uncaught exception under normal conditions. This is the
deterministic, cron-independent definition of done for "all 11 tasks run end-to-end" and
seeds Phase-5 CI (D-08). It is a new ops/test script — NOT a runner change.

Purpose: Prove FIX-03 (success criterion 3) without waiting on a real cron cycle, on the
runner already cleaned by Plans 01 and 02.
Output: scripts/run_all_tasks.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-fixes-defect-removal/02-CONTEXT.md
@.planning/phases/02-reliability-fixes-defect-removal/02-PATTERNS.md

<interfaces>
The 11 runner tasks (exact --task values, from run_task's mapping at sports_system_runner.py:5593):
  nba_daily_picks, mlb_daily_picks, nba_prop_monitor, mlb_prop_monitor,
  nba_injury_monitor, mlb_injury_monitor, nba_clv_tracker, mlb_clv_tracker,
  game_completion_monitor, check_results, verify.

Runner contract: prints JSON_RESULT={...} on stdout; exits non-zero only on uncaught failure.
Tasks are defensive: missing games/workbooks become explicit SKIP states (status fields in the
JSON_RESULT), NOT exceptions — so a SKIP is still a clean exit 0 with a JSON_RESULT line.

Subprocess invocation analog (scripts/repro_broken_pipe.py lines ~205-213):
  subprocess.Popen([sys.executable, "-u", str(RUNNER), "--task", task],
                   stdout=PIPE, stderr=PIPE, cwd=str(SCRIPTS_DIR))
SCRIPTS_DIR = Path(__file__).resolve().parent ; RUNNER = SCRIPTS_DIR / "sports_system_runner.py".

OPERATIONAL CAUTION: this harness invokes the REAL production tasks — nba/mlb daily_picks trigger
live DFS/Odds-API fetches, real Telegram alerts, and real workbook writes. It is not a dry run.
It must be executed during a non-trading / low-risk window (see <verification>).

Full reference structure is in PATTERNS.md "NEW scripts/run_all_tasks.py" section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the run-all harness over all 11 tasks (FIX-03, D-08)</name>
  <files>scripts/run_all_tasks.py</files>
  <read_first>
    - scripts/sports_system_runner.py lines 5593-5610 (run_task mapping — the authoritative list of the 11 task names)
    - scripts/repro_broken_pipe.py lines 110-118 and 205-213 (portable path constants + the Popen-of-runner pattern to reuse)
    - .planning/phases/02-reliability-fixes-defect-removal/02-PATTERNS.md "NEW scripts/run_all_tasks.py" section (the complete reference implementation)
    - .planning/phases/02-reliability-fixes-defect-removal/02-CONTEXT.md D-08 (scripted run-all; assert each exits 0 with no uncaught exception)
  </read_first>
  <action>
    Create scripts/run_all_tasks.py following the PATTERNS.md reference structure. Define SCRIPTS_DIR and
    RUNNER via Path(__file__).resolve().parent (portable, no hardcoded path). Define ALL_TASKS as the
    exact 11 task names from run_task's mapping. For each task, spawn the runner via subprocess.Popen
    with [sys.executable, "-u", str(RUNNER), "--task", task], stdout=PIPE, stderr=PIPE, cwd=str(SCRIPTS_DIR),
    and a per-task timeout (use 600s, matching the runner's max subprocess budget; on TimeoutExpired,
    kill + record a FAIL). A task PASSES when returncode == 0 AND "JSON_RESULT=" appears in stdout. Print
    a per-task line (running / OK / FAIL with exit code) and, on failure, an stderr excerpt (truncated to
    ~300 chars, no env/secret echo). Collect failures; return 1 if any task failed, 0 if all passed. Add a
    main() -> int and the if __name__ == "__main__": raise SystemExit(main()) guard. Treat a defensive SKIP
    (status SKIP in the JSON_RESULT, exit 0) as a PASS — do NOT flag SKIP states as failures (that is the
    documented runner contract). Do NOT modify the runner. Note in the docstring this is the FIX-03
    clean-pass harness (D-08), is intended to seed Phase-5 CI (CI-01/CI-02), and MUST be run during a
    non-trading / low-risk window because it invokes the real tasks (live fetches, real Telegram, real
    workbook writes).
  </action>
  <verify>
    <automated>cd scripts && python3 -c "import run_all_tasks as m; assert len(m.ALL_TASKS)==11; assert set(m.ALL_TASKS)=={'nba_daily_picks','mlb_daily_picks','nba_prop_monitor','mlb_prop_monitor','nba_injury_monitor','mlb_injury_monitor','nba_clv_tracker','mlb_clv_tracker','game_completion_monitor','check_results','verify'}; print('OK 11 tasks')" && timeout 3600 python3 run_all_tasks.py; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run_all_tasks.py defines ALL_TASKS with exactly the 11 names from run_task: the import check `python3 -c "import run_all_tasks as m; assert len(m.ALL_TASKS)==11; assert set(m.ALL_TASKS)=={...the 11...}"` exits 0
    - The full run is invoked under an OUTER timeout: `timeout 3600 python3 run_all_tasks.py` (caps the worst-case 11x600s ≈ 110 min so a stalled task cannot block verification indefinitely)
    - The live run must be performed during a non-trading / low-risk window (see <verification>) so the real Telegram alerts and workbook writes do not produce spurious operator alerts or unintended state
    - python3 run_all_tasks.py (from scripts/, on the Plan-01+02 runner, in a safe window) exits 0 with a final "All 11 tasks passed." line under normal conditions (network available; defensive SKIPs count as pass)
    - The harness uses no hardcoded absolute path: grep -c 'Path("/Users' scripts/run_all_tasks.py returns 0
    - On any task exiting non-zero or raising an uncaught exception, the harness prints that task as FAIL and returns 1
  </acceptance_criteria>
  <done>run_all_tasks.py invokes all 11 tasks via the runner CLI from scripts/, treats exit-0-with-JSON_RESULT (including defensive SKIP) as pass, reports a per-task summary, and exits 0 when all pass / 1 on any failure. Its verification wraps the full run in an outer timeout and documents the non-trading-window requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> runner subprocess | The harness spawns the production runner with the same CLI cron uses. It introduces no new input path; it only invokes existing tasks and reads their stdout exit status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | run-all mutating real workbooks / sending real Telegram during a clean pass | accept | The harness invokes the real tasks (that IS the FIX-03 proof per D-08); this is the same surface cron exercises daily. Run it during a non-trading / low-risk window (documented in <verification> + the script docstring) to avoid spurious operator alerts and unintended workbook state. Tasks are defensive and idempotent on rerun (they clear their own GENERATED_MARKER rows first). No new exposure beyond normal operation. |
| T-02-14 | Information Disclosure | stderr excerpts printed on failure | mitigate | Truncate stderr excerpts (e.g. first 300 chars) and do not echo env/secret values; stderr from the runner does not print secret values (env_value never logs raw secrets). |
| T-02-15 | Denial of Service | a stalled task hanging the run-all verification | mitigate | Per-task 600s timeout inside the harness PLUS an outer `timeout 3600 python3 run_all_tasks.py` on the verify invocation bound the worst case. |
| T-02-16 | Tampering | npm/pip/cargo installs | accept | No package installs; stdlib (subprocess, sys, pathlib) only. No legitimacy gate needed. |

No new untrusted-input paths, secrets handling, or network endpoints are introduced. No HIGH-severity
threat applies at ASVS L1 — the harness only re-runs existing tasks an operator already runs via cron.
</threat_model>

<verification>
- python3 -c "import run_all_tasks; assert len(run_all_tasks.ALL_TASKS)==11" succeeds.
- The full run-all is verified under an OUTER timeout: `timeout 3600 python3 run_all_tasks.py` (from scripts/) so a stalled task cannot block verification; it exits 0 with "All 11 tasks passed." on the cleaned runner.
- OPERATIONAL GUIDANCE: run_all_tasks.py invokes the REAL production tasks — nba/mlb daily_picks perform live fetches, send real Telegram alerts, and write real workbooks. It MUST be run during a non-trading / low-risk window so it does not produce spurious operator alerts or leave unintended workbook state. Tasks are defensive and idempotent on rerun (they clear their own GENERATED_MARKER rows first), but the alert/IO side effects are real.
- No hardcoded absolute path in the harness.
</verification>

<success_criteria>
- All 11 tasks complete a clean end-to-end invocation without uncaught exceptions (FIX-03 success criterion 3).
- The harness is deterministic, cron-independent, and reusable as a Phase-5 CI seed (D-08); its run is bounded by an outer timeout and documented to run in a safe window.
</success_criteria>

<output>
Create `.planning/phases/02-reliability-fixes-defect-removal/02-05-SUMMARY.md` when done.
</output>
