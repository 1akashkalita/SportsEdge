---
phase: 02-reliability-fixes-defect-removal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_projections.py
  - scripts/test_def02_path_resolution.py
autonomous: true
requirements: [DEF-02]
must_haves:
  truths:
    - "generate_projections.py resolves its BASE path via Path.home() / 'sports_picks', not a hardcoded user path"
    - "All BASE-derived paths (DATA, HIT_RATE_DIR, PROJ_DIR, NBA_DIR, MLB_DIR, RUN_LOG) still point at the same locations under ~/sports_picks"
    - "generate_projections runs successfully regardless of the OS user (the username is never hardcoded)"
  artifacts:
    - path: "scripts/generate_projections.py"
      provides: "Path.home()-based BASE path resolution per REQUIREMENTS.md DEF-02 / ROADMAP SC-5"
      contains: "Path.home() / \"sports_picks\""
    - path: "scripts/test_def02_path_resolution.py"
      provides: "regression test asserting BASE == Path.home()/'sports_picks' and no hardcoded absolute user path"
      contains: "Path.home"
  key_links:
    - from: "generate_projections.BASE"
      to: "DATA / HIT_RATE_DIR / PROJ_DIR"
      via: "derived from the Path.home()-based BASE anchor"
      pattern: "BASE /"
---

<objective>
Replace the hardcoded `BASE = Path("/Users/akashkalita/sports_picks")` in
`scripts/generate_projections.py` with `BASE = Path.home() / "sports_picks"` — the
literal idiom specified by REQUIREMENTS.md DEF-02 and ROADMAP success criterion 5
(the system's canonical install location IS `~/sports_picks`) — and ship the DEF-02
regression test that asserts the base path resolves to `Path.home() / "sports_picks"`
with no embedded username.

Purpose: The hardcoded absolute path breaks the projection subprocess stage for any OS
user other than the original author's — a stability defect for portability and CI (DEF-02).
Output: A portable generate_projections.py anchored on `Path.home()` and a passing
path-resolution test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reliability-fixes-defect-removal/02-CONTEXT.md
@.planning/phases/02-reliability-fixes-defect-removal/02-PATTERNS.md

<interfaces>
Current state extracted from scripts/generate_projections.py:
  line 26 BASE = Path("/Users/akashkalita/sports_picks")   the hardcoded anchor (DEF-02 target)
  line 27 DATA = BASE / "data"
  line 28 HIT_RATE_DIR = DATA / "research" / "hit_rates"
  line 29 PROJ_DIR = DATA / "research" / "projections"
  line 30 NBA_DIR = DATA / "nba"
  line 31 MLB_DIR = DATA / "mlb"
  line 32 RUN_LOG = DATA / "pnl" / "logs" / "run_log.txt"
All downstream constants derive from BASE — only the BASE anchor changes; their derivations stay.

Authoritative idiom (REQUIREMENTS.md DEF-02 + ROADMAP success criterion 5):
  BASE = Path.home() / "sports_picks"
DEF-02 literally specifies "resolves its base path via Path.home()". The canonical
install location IS ~/sports_picks, so Path.home() / "sports_picks" == the old hardcoded
path on the author's machine while remaining user-portable.

NOTE — PATTERNS.md (02-PATTERNS.md "DEF-02: Replace hardcoded BASE path") suggests the
`Path(__file__).resolve().parents[1]` runner-root idiom. CONTEXT.md D-02 permits either,
but REQUIREMENTS.md DEF-02 and ROADMAP SC-5 are AUTHORITATIVE and literally name
Path.home(). Use Path.home() / "sports_picks" here — do NOT follow the parents[1] idiom
in PATTERNS.md for this file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hardcoded BASE with Path.home() / "sports_picks" (DEF-02)</name>
  <files>scripts/generate_projections.py</files>
  <read_first>
    - scripts/generate_projections.py lines 1-35 (imports + the BASE block and all derived path constants)
    - .planning/REQUIREMENTS.md DEF-02 (authoritative: "resolves its base path via Path.home()")
    - .planning/ROADMAP.md Phase 2 success criterion 5 (authoritative: Path.home() base path)
    - .planning/phases/02-reliability-fixes-defect-removal/02-CONTEXT.md D-02 (discretion granted, Path.home() chosen to match the authoritative requirement)
  </read_first>
  <action>
    On line 26, replace the hardcoded BASE assignment with BASE = Path.home() / "sports_picks"
    and add a short trailing comment noting it anchors on the canonical ~/sports_picks install
    location per REQUIREMENTS.md DEF-02 / ROADMAP SC-5. Confirm Path is already imported (it is —
    the file uses Path). Do NOT use the Path(__file__).resolve().parents[1] idiom from PATTERNS.md
    for this file — REQUIREMENTS.md DEF-02 and ROADMAP SC-5 literally specify Path.home(), so this
    file uses Path.home() / "sports_picks". Do NOT change DATA, HIT_RATE_DIR, PROJ_DIR, NBA_DIR,
    MLB_DIR, RUN_LOG or any other BASE-derived line — they must keep deriving from BASE so the
    resolved targets are identical to before. Do NOT change projection math, headers, or any output
    behavior — this is a pure path-anchor fix.
  </action>
  <verify>
    <automated>cd scripts && grep -q 'Path.home() / "sports_picks"' generate_projections.py && ! grep -q 'Path("/Users' generate_projections.py && ! grep -q "akashkalita" generate_projections.py && echo PASS_DEF02_SOURCE</automated>
  </verify>
  <acceptance_criteria>
    - grep -n 'Path("/Users' scripts/generate_projections.py returns nothing (no hardcoded absolute user path remains)
    - grep -n 'akashkalita' scripts/generate_projections.py returns nothing
    - grep -c 'Path.home() / "sports_picks"' scripts/generate_projections.py returns 1
    - After import, generate_projections.BASE equals Path.home() / "sports_picks" (absolute, ends with sports_picks)
    - generate_projections.DATA still starts with str(BASE) (derivation preserved)
    - cd scripts && python3 -c "import generate_projections" exits 0
  </acceptance_criteria>
  <done>BASE is resolved via Path.home() / "sports_picks"; no hardcoded absolute user path or username remains; all derived path constants are unchanged and still anchored to BASE; the module imports cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the DEF-02 path-resolution regression test (D-10)</name>
  <files>scripts/test_def02_path_resolution.py</files>
  <read_first>
    - scripts/test_generate_projections.py lines 1-16 (direct-import + sys.path.insert + unittest.TestCase header pattern — the exact analog)
    - scripts/generate_projections.py lines 1-35 (the BASE + derived constants the test asserts against)
    - .planning/phases/02-reliability-fixes-defect-removal/02-PATTERNS.md "NEW scripts/test_def02_path_resolution.py" section (the reference test structure — adapt its assertions to Path.home())
    - .planning/phases/02-reliability-fixes-defect-removal/02-CONTEXT.md D-10 (DEF-02 ships with a path-resolves-regardless-of-user/prefix test)
  </read_first>
  <action>
    Create scripts/test_def02_path_resolution.py as a plain unittest.TestCase following the
    test_generate_projections.py header pattern (sys.path.insert of the script dir, then
    import generate_projections as gp). Add these assertions: (1) gp.BASE == Path.home() / "sports_picks"
    (the authoritative DEF-02 / SC-5 contract); (2) str(gp.BASE) does NOT contain the substring
    akashkalita (the regression that fails on the pre-fix hardcoded path); (3) the SOURCE file
    contains no hardcoded absolute user path — read scripts/generate_projections.py and assert
    'Path("/Users' is absent from its text (catches a re-introduced hardcoded path even if BASE
    happens to resolve equal); (4) gp.BASE is absolute and exists; (5) str(gp.DATA) starts with
    str(gp.BASE) (derivation preserved). Add the __main__ guard calling unittest.main(). Note in the
    docstring this is the DEF-02 regression test (D-10) asserting Path.home()-based resolution.
  </action>
  <verify>
    <automated>cd scripts && python3 test_def02_path_resolution.py</automated>
  </verify>
  <acceptance_criteria>
    - python3 test_def02_path_resolution.py (from scripts/) exits 0 with an OK summary line
    - The test asserts gp.BASE == Path.home() / "sports_picks" (the authoritative DEF-02 / SC-5 contract)
    - The test asserts the substring akashkalita is absent from str(gp.BASE) — this assertion would FAIL against the pre-fix hardcoded BASE (confirms a real failing-before/passing-after regression test per D-10)
    - The test reads generate_projections.py source and asserts it contains no 'Path("/Users' hardcoded absolute user path
    - The test asserts gp.BASE is absolute and exists and that gp.DATA starts with gp.BASE
    - python3 -m pytest test_def02_path_resolution.py (from scripts/) reports the tests passing
  </acceptance_criteria>
  <done>A DEF-02 regression test exists that asserts BASE == Path.home()/'sports_picks', fails on the hardcoded path, and passes on the portable one; it runs green from scripts/ with python3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes a path-anchor constant and adds a static test. No new input, network, or secret path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | BASE resolving to the wrong root on a different layout | mitigate | The test asserts BASE == Path.home()/"sports_picks", is absolute, exists, and that DATA derives from it — catching a misresolved root. |
| T-02-08 | Tampering | npm/pip/cargo installs | accept | No package installs; stdlib (pathlib, unittest) only. No legitimacy gate needed. |

No new untrusted-input paths, secrets handling, or network endpoints. No HIGH-severity threat applies
at ASVS L1 — this is a local path-portability fix on a single-operator cron backend.
</threat_model>

<verification>
- grep confirms no hardcoded absolute user path or username remains in generate_projections.py.
- generate_projections imports cleanly and BASE resolves to Path.home() / "sports_picks".
- The DEF-02 test passes from scripts/ and would fail on the pre-fix hardcoded path.
</verification>

<success_criteria>
- generate_projections.py resolves its base path via Path.home() / "sports_picks" and runs regardless of the OS user (DEF-02 / ROADMAP success criterion 5).
- The DEF-02 regression test passes (failing-before / passing-after per D-10).
</success_criteria>

<output>
Create `.planning/phases/02-reliability-fixes-defect-removal/02-03-SUMMARY.md` when done.
</output>
