---
phase: 01-diagnosis
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - .planning/phases/01-diagnosis/DIAGNOSIS.md
autonomous: true
requirements: [DIAG-01, DIAG-02]
must_haves:
  truths:
    - "DIAGNOSIS.md names the exact file/function/line, mechanism, evidence artifact, and confidence for the broken-pipe failure (DIAG-01, D-08)"
    - "DIAGNOSIS.md names the exact task/stage/subprocess, mechanism, evidence artifact, and confidence for the cron-timeout failure (DIAG-02)"
    - "The timeout section presents a ranked-contributors table against the cron time budget (D-10)"
    - "The diagnosis states a recommended fix DIRECTION for each failure without locking the Phase-2 implementation (D-09)"
    - "The python3 3.14-alpha timing caveat is stated, and every one of the 3 ROADMAP Phase-1 success criteria maps to a concrete evidence pointer in DIAGNOSIS.md"
  artifacts:
    - path: ".planning/phases/01-diagnosis/DIAGNOSIS.md"
      provides: "The phase deliverable: evidence-backed root-cause document for both failures (DIAG-01 + DIAG-02), consumed by the operator and the Phase-2 planner"
      min_lines: 60
      contains: "Ranked"
  key_links:
    - from: ".planning/phases/01-diagnosis/DIAGNOSIS.md"
      to: ".planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md"
      via: "the timeout section cites the ranked-contributors table as its evidence artifact"
      pattern: "01-TIMING-EVIDENCE"
    - from: ".planning/phases/01-diagnosis/DIAGNOSIS.md"
      to: "scripts/repro_broken_pipe.py"
      via: "the broken-pipe section cites the repro script as its evidence artifact"
      pattern: "repro_broken_pipe"
    - from: ".planning/phases/01-diagnosis/DIAGNOSIS.md"
      to: "data/pnl/logs/run_log.txt"
      via: "the broken-pipe section cites the captured TRACEBACK/ERROR run-log lines (via the D-03/D-04 hook) as supporting evidence"
      pattern: "run_log"
---

<objective>
Synthesize the evidence from Plans 01 and 02 into the single phase deliverable:
`.planning/phases/01-diagnosis/DIAGNOSIS.md` (D-08). For EACH failure — the
`[Errno 32] Broken pipe` on `mlb_prop_monitor` (DIAG-01) and the cron-job timeouts
(DIAG-02) — state the exact file/function/line, the mechanism narrative, the
supporting evidence artifact pointer, and a stated confidence level. Present the
timeout findings as a ranked-contributors table against the cron budget (D-10),
and name a recommended fix DIRECTION for Phase 2 without locking the implementation
(D-09). Then verify traceability: every ROADMAP § Phase 1 success criterion maps
to a concrete evidence pointer in the document.

Purpose: This is the artifact the operator points to as the "documented,
evidence-backed root cause" for both failure modes, and the input the Phase-2
planner uses to scope the fixes. It folds Plan 01's broken-pipe evidence (repro
behavior + the failing frame at sports_system_runner.py:5634/5640 + the captured
trace via the D-03/D-04 hook) and Plan 02's timeout evidence (the
01-TIMING-EVIDENCE.md ranked-contributors table) into one diagnosis.

Output: `.planning/phases/01-diagnosis/DIAGNOSIS.md` — the diagnosis deliverable
(no source changes; this plan authors and verifies a markdown document only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-diagnosis/01-CONTEXT.md
@.planning/phases/01-diagnosis/01-RESEARCH.md
@.planning/phases/01-diagnosis/01-PATTERNS.md

<interfaces>
<!-- Upstream evidence this plan synthesizes. These exist at execute time (wave 2). -->

Plan 01 evidence (DIAG-01 — broken pipe):
  scripts/repro_broken_pipe.py            — deterministic repro; PASS/exit-0 behavior + new run-log error count
  scripts/sports_system_runner.py:5634    — success-path bare print("JSON_RESULT=...") (the failing frame, unprotected)
  scripts/sports_system_runner.py:5640    — except-path bare print("JSON_RESULT=...") (second failing frame)
  scripts/sports_system_runner.py except  — D-03/D-04 traceback hook now writes `TRACEBACK task=` to RUN_LOG
  data/pnl/logs/run_log.txt               — captured `TRACEBACK task=` / `ERROR task=` lines from the hook
  .planning/phases/01-diagnosis/01-01-SUMMARY.md — Plan 01 outcome: failing frame confirmed + hook insertion line

Plan 02 evidence (DIAG-02 — timeouts):
  .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md — duration profile + representative timed runs + D-10 ranked-contributors table + Lead #2 disposition
  .planning/phases/01-diagnosis/01-02-SUMMARY.md       — Plan 02 outcome: dominant contributor named + Lead #2 disposition

DIAGNOSIS.md shape (authoritative content source):
  01-RESEARCH.md § "DIAGNOSIS.md Shape" (lines 435–463) — Section 1 (DIAG-01), Section 2 (DIAG-02), and the exact ranked-contributors table schema
  01-CONTEXT.md D-08 (per-failure: file/function/line + mechanism + evidence artifact + confidence), D-09 (cause + fix direction, no implementation lock), D-10 (ranked-contributors table)

The exact broken-pipe alert surface to trace back to (01-CONTEXT.md § Specific Ideas):
  ❌ SPORTS TASK FAILED: mlb_prop_monitor / Error: [Errno 32] Broken pipe

ROADMAP § Phase 1 success criteria (each must map to an evidence pointer in DIAGNOSIS.md):
  1. Names the exact code path producing [Errno 32] Broken pipe on mlb_prop_monitor, backed by repro or captured trace
  2. Names which task/stage/subprocess exceeds the cron budget, backed by timing evidence
  3. The log()/obsidian_sync() per-line lead AND the stacked subprocess timeout totals are confirmed or ruled out with evidence
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author DIAGNOSIS.md from the Plan 01 + Plan 02 evidence (D-08/D-09/D-10)</name>
  <files>.planning/phases/01-diagnosis/DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/01-diagnosis/01-01-SUMMARY.md (Plan 01 outcome — the repro behavior, the confirmed failing frame at sports_system_runner.py:5634/5640, and the D-03/D-04 hook insertion line)
    - .planning/phases/01-diagnosis/01-02-SUMMARY.md (Plan 02 outcome — the dominant timeout contributor named + the Lead #2 disposition)
    - .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md (the ranked-contributors table + measured durations to fold into Section 2)
    - scripts/repro_broken_pipe.py (the repro evidence artifact to cite in Section 1)
    - data/pnl/logs/run_log.txt (the captured `TRACEBACK task=` / `ERROR task=mlb_prop_monitor: [Errno 32] Broken pipe` lines + the `completed in 129.4s` corroboration to quote as evidence)
    - .planning/phases/01-diagnosis/01-RESEARCH.md § "DIAGNOSIS.md Shape" (lines 435–463 — Section 1, Section 2, the ranked-contributors table schema) and § "Confirmed vs Ruled Out: Lead Summary" (lines 200–207)
    - .planning/phases/01-diagnosis/01-CONTEXT.md D-08 (per-failure content requirements), D-09 (fix direction without locking implementation), D-10 (ranked-contributors table)
    - ./CLAUDE.md (minimal-invasive; never quote secrets)
  </read_first>
  <action>
    Author `.planning/phases/01-diagnosis/DIAGNOSIS.md` as the phase deliverable,
    following 01-RESEARCH.md § "DIAGNOSIS.md Shape" exactly. Structure it with these
    sections (D-08 per-failure fields are mandatory in each):

    "## Section 1 — Broken-Pipe Root Cause (DIAG-01)" containing:
      - "Statement": the spurious `❌ SPORTS TASK FAILED: mlb_prop_monitor / Error:
        [Errno 32] Broken pipe` alert; the task's work completes successfully; the
        error occurs at the bare `print("JSON_RESULT=...")` at
        `scripts/sports_system_runner.py:5634` (success path) / `:5640` (except path)
        after the Hermes `no_agent` pipe closes. Name the exact file + function (`main()`)
        + line numbers.
      - "Mechanism": dispatch_alerts → N×send_telegram → N×log → N×obsidian_sync →
        pipe closes → bare print raises BrokenPipeError → `except Exception` catches it
        → spurious TASK FAILED Telegram. Note safe_print() (line 192) protects per-line
        prints but the JSON_RESULT= prints at 5634/5640 are bare.
      - "Evidence artifact": pointer to `scripts/repro_broken_pipe.py` (the deterministic
        repro from Plan 01, its PASS/exit-0 behavior) AND the captured `TRACEBACK task=` /
        `ERROR task=mlb_prop_monitor: [Errno 32] Broken pipe` lines in
        `data/pnl/logs/run_log.txt` written by the D-03/D-04 hook; quote the run-log
        excerpt (task-complete then ERROR then `completed in 129.4s`) verbatim with
        timestamps, redacting any secret-shaped value.
      - "Confidence": HIGH (per RESEARCH — confirmed across 34+ run-log occurrences with
        identical pattern + the repro).
      - "Fix direction" (D-09 — direction only, do NOT lock implementation): wrap the
        5634/5640 prints in safe_print() and distinguish BrokenPipeError in the except
        so a completed task does not fire the TASK FAILED Telegram. State this is the
        Phase-2 owner's call on the how.

    "## Section 2 — Timeout Root Cause (DIAG-02)" containing:
      - "Statement": the observed cron 'timeouts' are NOT `subprocess.TimeoutExpired`;
        they are the runner spending minutes-to-hours in `send_telegram()` retry loops
        during network outages plus compounding per-log-line `obsidian_sync` subprocess
        calls. Name the dominant task/stage/subprocess (per 01-02-SUMMARY.md) with its
        measured duration.
      - "Ranked-contributors table" (D-10): reproduce the D-10 5-column table (Rank |
        Contributor | Max observed overhead | Occurs when | Fix direction) FROM
        01-TIMING-EVIDENCE.md (this machine's measured numbers), with `send_telegram`
        retry loop ranked #1.
      - "Evidence artifact": pointer to `.planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md`
        and a quoted run-log excerpt showing `NameResolutionError` for `api.telegram.org`
        co-occurring with an extreme duration, redacting any secret-shaped value.
      - "Confidence": HIGH for the Telegram retry loop as dominant cause; MEDIUM for
        obsidian_sync compounding as secondary (per RESEARCH).
      - "Lead disposition": explicitly carry over from 01-TIMING-EVIDENCE.md whether
        Lead #2 (stacked subprocess timeout totals) is CONFIRMED or RULED OUT vs the
        1500s ceiling, and that the log()/obsidian_sync() per-line lead is confirmed as
        a contributor.
      - "Fix direction" (D-09 — direction only): hard per-task wall-clock timeout;
        batch/decouple obsidian_sync from the hot log() path; short-circuit Telegram on
        consecutive failures. State Phase 2 owns the implementation.

    "## Timing Caveat": state that `python3` is 3.14 ALPHA and the interpreter is a
    variable when interpreting absolute durations (carried from D-11 / CONTEXT timing
    caveat).

    Do NOT propose or write any source fix here (D-09 — diagnosis names direction, not
    implementation; Phase 2 owns the how). Quote no secrets: redact any
    `TELEGRAM_BOT_TOKEN`/`PRIZEPICKS_COOKIE`/`ODDS_API_IO_KEY`-shaped value if it
    appears in any cited evidence line. This task authors the markdown deliverable only —
    no edits to `scripts/`.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-diagnosis/DIAGNOSIS.md && grep -q 'Broken-Pipe Root Cause' .planning/phases/01-diagnosis/DIAGNOSIS.md && grep -q 'Timeout Root Cause' .planning/phases/01-diagnosis/DIAGNOSIS.md && grep -qi 'Ranked' .planning/phases/01-diagnosis/DIAGNOSIS.md && grep -qi 'Confidence' .planning/phases/01-diagnosis/DIAGNOSIS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-diagnosis/DIAGNOSIS.md` exists, is >= 60 lines, and contains a "## Section 1 — Broken-Pipe Root Cause (DIAG-01)" and a "## Section 2 — Timeout Root Cause (DIAG-02)" (assertable by grep for both headers)
    - Section 1 names the exact file/function/line `scripts/sports_system_runner.py:5634` (and `:5640`) inside `main()` (assertable by grep for "5634" and "main()")
    - Section 1 cites BOTH `scripts/repro_broken_pipe.py` and a verbatim `data/pnl/logs/run_log.txt` excerpt containing the `[Errno 32] Broken pipe` ERROR line with a timestamp (assertable by grep for "repro_broken_pipe" and "Broken pipe")
    - Section 2 contains a ranked-contributors table with the D-10 columns (Rank, Contributor, Max observed overhead, Occurs when, Fix direction) and `send_telegram` ranked #1, and cites `01-TIMING-EVIDENCE.md` (assertable by grep for "Ranked", "send_telegram", "01-TIMING-EVIDENCE")
    - BOTH sections include an explicit "Confidence" line and a "Fix direction" line; the fix direction names what to change and why WITHOUT prescribing the exact implementation (D-09 — assertable by reading both sections; grep for "Confidence" and "Fix direction")
    - A "## Timing Caveat" line names python3 3.14 ALPHA (assertable by grep for "alpha")
    - No secret/token value appears anywhere — `grep -E 'TELEGRAM_BOT_TOKEN|PRIZEPICKS_COOKIE|ODDS_API_IO_KEY' .planning/phases/01-diagnosis/DIAGNOSIS.md` returns nothing
  </acceptance_criteria>
  <done>DIAGNOSIS.md exists with both failure sections complete per D-08 (file/function/line + mechanism + evidence artifact pointer + confidence for each), a D-10 ranked-contributors table with send_telegram #1, D-09 fix-direction-not-implementation for both failures, the python3-alpha caveat, and no leaked secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Verify DIAGNOSIS.md traceability to all 3 ROADMAP success criteria + DIAG-01/DIAG-02</name>
  <files>.planning/phases/01-diagnosis/DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/01-diagnosis/DIAGNOSIS.md (the document authored in Task 1 — you are appending a traceability section and verifying coverage)
    - .planning/ROADMAP.md § "Phase 1: Diagnosis" (lines 23–31 — the goal + the 3 success criteria the document must satisfy)
    - .planning/REQUIREMENTS.md (DIAG-01 lines 12, DIAG-02 line 13 — both must be explicitly addressed)
    - .planning/phases/01-diagnosis/01-01-SUMMARY.md and 01-02-SUMMARY.md (the upstream evidence pointers to reference in the traceability map)
    - ./CLAUDE.md (minimal-invasive; never quote secrets)
  </read_first>
  <action>
    Append a "## Traceability" section to `.planning/phases/01-diagnosis/DIAGNOSIS.md`
    that maps EACH of the 3 ROADMAP § Phase 1 success criteria to a concrete evidence
    pointer already present in the document, and explicitly marks DIAG-01 and DIAG-02
    as addressed. Use a table with columns: Success Criterion | Where Addressed in
    DIAGNOSIS.md | Evidence Artifact. Required rows:
      - Criterion 1 (names the exact code path producing the broken pipe, backed by
        repro or captured trace) → Section 1 → `scripts/repro_broken_pipe.py` +
        the `data/pnl/logs/run_log.txt` captured trace
      - Criterion 2 (names which task/stage/subprocess exceeds the cron budget, backed
        by timing evidence) → Section 2 ranked-contributors table →
        `.planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md`
      - Criterion 3 (the log()/obsidian_sync() per-line lead AND the stacked subprocess
        timeout totals are confirmed or ruled out with evidence) → Section 2 ranked
        table + Lead disposition → `01-TIMING-EVIDENCE.md` Lead #2 disposition
    Add a "Requirements addressed: DIAG-01, DIAG-02" line beneath the table.

    Then perform the verification pass: confirm by grep/read that each required
    section, the ranked table, the confidence lines, the fix-direction lines, and the
    python3-alpha caveat are present, and that no `TELEGRAM_BOT_TOKEN` /
    `PRIZEPICKS_COOKIE` / `ODDS_API_IO_KEY` value appears anywhere in the document. If
    any required element is missing, fix it in Task 1's content before declaring done.

    This task edits only `.planning/phases/01-diagnosis/DIAGNOSIS.md` — no source
    changes.
  </action>
  <verify>
    <automated>grep -q 'Traceability' .planning/phases/01-diagnosis/DIAGNOSIS.md && grep -q 'DIAG-01' .planning/phases/01-diagnosis/DIAGNOSIS.md && grep -q 'DIAG-02' .planning/phases/01-diagnosis/DIAGNOSIS.md && grep -q '01-TIMING-EVIDENCE' .planning/phases/01-diagnosis/DIAGNOSIS.md && grep -q 'repro_broken_pipe' .planning/phases/01-diagnosis/DIAGNOSIS.md && ! grep -Eq 'TELEGRAM_BOT_TOKEN|PRIZEPICKS_COOKIE|ODDS_API_IO_KEY' .planning/phases/01-diagnosis/DIAGNOSIS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - DIAGNOSIS.md contains a "## Traceability" table with one row per ROADMAP § Phase 1 success criterion (3 rows), each row naming where in DIAGNOSIS.md it is addressed AND its evidence artifact (assertable by grep for "Traceability" + 3 criterion rows)
    - The traceability table's evidence column references `scripts/repro_broken_pipe.py` (criterion 1), `01-TIMING-EVIDENCE.md` (criterion 2), and the Lead #2 disposition (criterion 3) (assertable by grep for those pointers)
    - A "Requirements addressed: DIAG-01, DIAG-02" line is present (assertable by grep for "DIAG-01" and "DIAG-02")
    - The verification grep gate passes: all required sections/markers present and `grep -E 'TELEGRAM_BOT_TOKEN|PRIZEPICKS_COOKIE|ODDS_API_IO_KEY' .planning/phases/01-diagnosis/DIAGNOSIS.md` returns nothing
  </acceptance_criteria>
  <done>DIAGNOSIS.md has a Traceability section mapping all 3 ROADMAP Phase-1 success criteria to concrete in-document evidence pointers, explicitly addresses DIAG-01 + DIAG-02, and the verification gate (sections present, no secrets) passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cited evidence (run-log excerpts, traces) → DIAGNOSIS.md | The deliverable quotes lines from `data/pnl/logs/run_log.txt` and the captured traceback; any token/cookie/key-shaped value in a cited line must be redacted before it lands in the operator-facing document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | run-log / traceback excerpts quoted in DIAGNOSIS.md | mitigate | Redact any `TELEGRAM_BOT_TOKEN`/`PRIZEPICKS_COOKIE`/`ODDS_API_IO_KEY`-shaped value when quoting evidence lines. Both tasks' verify gates and Task 2's acceptance criteria grep DIAGNOSIS.md for those tokens and require zero matches. Run-log lines and Python stack traces are operational text and do not normally carry secrets, but redaction is enforced as a gate. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — authoring a markdown document with grep verification only. No legitimacy gate required. |
</threat_model>

<verification>
- `test -f .planning/phases/01-diagnosis/DIAGNOSIS.md` — the deliverable exists
- `grep -q 'Broken-Pipe Root Cause' ...DIAGNOSIS.md` and `grep -q 'Timeout Root Cause' ...` — both failure sections present
- `grep -qi 'Ranked' ...DIAGNOSIS.md` and `grep -q 'send_telegram' ...` — the D-10 ranked-contributors table is present with send_telegram #1
- `grep -q 'Traceability' ...DIAGNOSIS.md` and `grep -q 'DIAG-01' ...` and `grep -q 'DIAG-02' ...` — traceability section maps both requirements
- `grep -q 'repro_broken_pipe' ...DIAGNOSIS.md` and `grep -q '01-TIMING-EVIDENCE' ...` — both upstream evidence artifacts are cited
- `grep -qi 'alpha' ...DIAGNOSIS.md` — python3 3.14-alpha timing caveat present
- `grep -E 'TELEGRAM_BOT_TOKEN|PRIZEPICKS_COOKIE|ODDS_API_IO_KEY' ...DIAGNOSIS.md` returns nothing — no secrets leaked
- Minimal-invasive confirmed: this plan touches only `.planning/phases/01-diagnosis/DIAGNOSIS.md`; the only kept source change in Phase 1 remains Plan 01's traceback hook
</verification>

<success_criteria>
- ROADMAP § Phase 1 success criterion 1 satisfied: DIAGNOSIS.md names the exact code path producing `[Errno 32] Broken pipe` on `mlb_prop_monitor`, backed by the Plan-01 repro script and the captured run-log trace (DIAG-01)
- ROADMAP § Phase 1 success criterion 2 satisfied: DIAGNOSIS.md names which task/stage/subprocess exceeds the cron budget, backed by the Plan-02 timing evidence and ranked-contributors table (DIAG-02)
- ROADMAP § Phase 1 success criterion 3 satisfied: DIAGNOSIS.md confirms-or-rules-out both the log()/obsidian_sync() per-line lead and the stacked subprocess timeout totals with evidence (the Lead #2 disposition + the ranked table)
- The diagnosis gives cause + recommended fix direction for both failures WITHOUT locking the Phase-2 implementation (D-09), with the python3-alpha caveat stated (D-11)
- The Traceability section makes the DIAG-01/DIAG-02 coverage and the 3-criterion mapping auditable in one place
</success_criteria>

<output>
Create `.planning/phases/01-diagnosis/01-03-SUMMARY.md` when done, recording: that
DIAGNOSIS.md was authored, the root cause named for each failure (file/function/line +
confidence), confirmation that all 3 ROADMAP § Phase 1 success criteria map to
in-document evidence pointers, and that DIAG-01 + DIAG-02 are both addressed —
closing the diagnosis phase.
</output>
