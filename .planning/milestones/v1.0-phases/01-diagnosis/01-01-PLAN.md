---
phase: 01-diagnosis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/repro_broken_pipe.py
  - scripts/sports_system_runner.py
autonomous: true
requirements: [DIAG-01]
must_haves:
  truths:
    - "A deterministic repro script reproduces the BrokenPipeError that mlb_prop_monitor hits in production and exits 0 — the repro evidence path (D-01) that is also the primary mechanism-pinning artifact and Phase-3 regression-test seed (D-02)"
    - "The repro independently confirms the failing frame is the in-try JSON_RESULT= print (line 5634/5640), not an earlier print — exercising the three named broken-pipe leads in asymmetric scope (D-05)"
    - "main()'s top-level except writes the captured err['traceback'] to the run-log file (robust sink), never solely stdout"
    - "The traceback hook is the ONLY committed source change in Phase 1 — additive logging, no gate/pick/schema/behavior change"
  artifacts:
    - path: "scripts/repro_broken_pipe.py"
      provides: "Deterministic BrokenPipeError reproduction; doubles as Phase-3 regression seed (RES-04)"
      min_lines: 40
      contains: "subprocess.Popen"
    - path: "scripts/sports_system_runner.py"
      provides: "Additive traceback dump to RUN_LOG in main()'s except block (D-03/D-04)"
      contains: "TRACEBACK task="
  key_links:
    - from: "scripts/repro_broken_pipe.py"
      to: "scripts/sports_system_runner.py"
      via: "subprocess.Popen spawning the runner with stdout=PIPE then closing the read end"
      pattern: "subprocess\\.Popen"
    - from: "scripts/sports_system_runner.py main() except"
      to: "data/pnl/logs/run_log.txt"
      via: "RUN_LOG.open('a') append of traceback.format_exc()"
      pattern: "RUN_LOG\\.open"
---

<objective>
Commission the broken-pipe evidence for DIAG-01: (a) a deterministic local repro
script that forces the `[Errno 32] Broken pipe` on a runner task and captures the
exact failing frame, and (b) one small additive traceback dump in `main()`'s
top-level `except` that writes the already-computed `err["traceback"]` to the
run-log file so a real scheduled run leaves a stack trace behind.

Purpose: Independently confirm (not just restate) the HIGH-confidence research
finding that the spurious `❌ SPORTS TASK FAILED: mlb_prop_monitor / Error:
[Errno 32] Broken pipe` originates at the unprotected `print("JSON_RESULT=...")`
in `main()`, after the Hermes `no_agent` cron wrapper closes stdout. The repro is
the primary mechanism-pinning artifact (D-01a) and is designed to double as the
Phase-3 regression-test seed (D-02 / RES-04). The traceback hook is the one kept
code change (D-03/D-04), a down payment on Phase 4 observability.

Output: `scripts/repro_broken_pipe.py` (new) and an additive 3-block traceback
dump inside `main()`'s `except` in `scripts/sports_system_runner.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-diagnosis/01-CONTEXT.md
@.planning/phases/01-diagnosis/01-RESEARCH.md
@.planning/phases/01-diagnosis/01-PATTERNS.md

<interfaces>
<!-- Verified line anchors in scripts/sports_system_runner.py (confirmed by reading the file). -->
<!-- Executor: use these directly. Do NOT re-derive by scanning the 5,650-line runner. -->

Line 25:   import traceback                         # already imported — no new import needed
Line 57:   RUN_LOG = LOG_DIR / "run_log.txt"        # the robust file sink (Path constant)
Line 169:  def now_iso() -> str:                     # ISO-UTC timestamp helper, already in scope
Line 192:  def safe_print(line, *, file=None)        # catches BrokenPipeError on its OWN print (per-line prints are protected)
Line 203:  def log(msg)                              # writes RUN_LOG.open("a"), then obsidian_sync (except/pass), then safe_print

main()'s try/except/finally (VERIFIED current code, lines 5612–5646):
  5621:  print("JSON_RESULT=" + json.dumps(result, ...))   # --test-telegram path — OUTSIDE the try/except (not the target)
  5625:  task_start_time = time.time()
  5626:  try:
  5628:      fcntl.flock(lock, fcntl.LOCK_EX)
  5631:      with task_workbook_locks(args.task): result = run_task(args.task)
  5633:  dispatch_alerts(args.task, result)
  5634:  print("JSON_RESULT=" + json.dumps(result, ...))   # SUCCESS-path bare print — UNPROTECTED (target #1)
  5635:  return 0
  5636:  except Exception as e:
  5637:      err = {"status":"error","task":args.task,"error":str(e),"traceback":traceback.format_exc()}
  5638:      log(f"ERROR task={args.task}: {e}")
  5639:      send_telegram(f"❌ SPORTS TASK FAILED: {args.task}\nError: {e}")
  5640:      print("JSON_RESULT=" + json.dumps(err, ...))   # EXCEPTION-path bare print — UNPROTECTED (target #2)
  5641:      return 1
  5642:  finally:
  5643:      elapsed = time.time() - task_start_time
  5644:      log(f"[{args.task}] completed in {elapsed:.1f}s")
  5645:      if elapsed > 90: log(f"WARNING: ...")

CRITICAL repro nuance: the `--test-telegram` print at 5621 is OUTSIDE main()'s
try/except, so a BrokenPipeError there is NOT caught and writes NO traceback.
The repro MUST target a task that routes through the try/except (so the failing
frame is 5634 or 5640 and the new traceback hook fires). See Task 1 action.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the deterministic broken-pipe repro script (Wave 0 test scaffold for DIAG-01)</name>
  <files>scripts/repro_broken_pipe.py</files>
  <read_first>
    - scripts/repro_broken_pipe.py (does not exist yet — you are creating it)
    - .planning/phases/01-diagnosis/01-RESEARCH.md § "Broken-Pipe Repro Script Design" (lines 318–407) and § "Common Pitfalls" (Pitfall 1 + Pitfall 4) — the mechanism and the early-close trap
    - .planning/phases/01-diagnosis/01-PATTERNS.md § "scripts/repro_broken_pipe.py" (the header, SCRIPTS_DIR/RUN_LOG path idiom, subprocess pipe-closure pattern, exit-code contract, `raise SystemExit(main())`, `from __future__ import annotations`, `cwd=str(SCRIPTS_DIR)`)
    - scripts/test_game_completion_monitor_smoke.py (analog: runner-as-subprocess wiring, exit-code contract)
    - scripts/test_prop_monitor_alert_aggregation.py (analog: BrokenPipe concern, in-process safe_print assertion)
    - ./CLAUDE.md (run from scripts/ with python3; snake_case; type annotations; JSON_RESULT contract)
  </read_first>
  <action>
    Create `scripts/repro_broken_pipe.py` following the PATTERNS.md repro analog
    exactly. Header: `#!/usr/bin/env python3`, a module docstring stating it is the
    Phase-1 deterministic BrokenPipeError repro that doubles as the Phase-3
    regression seed (RES-04), `from __future__ import annotations`, stdlib imports
    (`subprocess`, `sys`, `os`, `time`, `pathlib.Path`). Derive paths with the
    portable idiom (NOT a hardcoded absolute path): `SCRIPTS_DIR =
    Path(__file__).resolve().parent`, `RUNNER = SCRIPTS_DIR / "sports_system_runner.py"`,
    `RUN_LOG = SCRIPTS_DIR.parents[0].parent / "data" / "pnl" / "logs" / "run_log.txt"`
    (the real run-log, NOT a temp — the traceback hook from Task 2 must be observable).

    Implement `count_broken_pipe_in_log(before_size: int) -> int` that seeks to
    `before_size` and counts new occurrences of "Broken pipe", "ERROR task=", and
    "TRACEBACK task=" since the snapshot (wrapped in try/except returning 0).

    Implement `main() -> int`: snapshot `os.path.getsize(RUN_LOG)`; spawn the runner
    via `subprocess.Popen([sys.executable, str(RUNNER), <task-args>], stdout=PIPE,
    stderr=PIPE, cwd=str(SCRIPTS_DIR))`; close the read end (`proc.stdout.close()`)
    to simulate Hermes closing the pipe; `proc.wait(timeout=60)`; then count new log
    errors.

    TARGET TASK SELECTION (this is the load-bearing correctness detail): the
    `--test-telegram` print at runner line 5621 is OUTSIDE main()'s try/except, so a
    broken pipe there leaves NO traceback and would defeat the repro's assertion.
    Choose a `--task` value that routes through the try/except (lines 5626–5641) so
    the failing frame is the in-try JSON_RESULT= print at 5634/5640 and the Task-2
    traceback hook fires. Prefer a lightweight, side-effect-safe task that still
    reaches the success-path or except-path print: `--task verify` or `--task
    check_results` (these do not generate picks, do not send line-move alert fanout,
    and do not mutate production picks). Make the task name a module-level constant
    (e.g. `REPRO_TASK = "verify"`) with a comment explaining why a try/except-routed
    task is required and why a side-effect-safe one is chosen (see threat_model).
    Per RESEARCH Pitfall 1, if closing the pipe immediately surfaces an EARLIER
    print frame, insert a small `time.sleep(...)` before `proc.stdout.close()` so the
    pipe closes after the task body runs but before the final JSON_RESULT= print —
    matching production timing.

    Assertion / exit contract (PATTERNS.md): print a single PASS/FAIL summary line.
    Return 0 when the broken pipe is reproduced AND a traceback/ERROR line was
    written to the run log (i.e. `new_errors > 0`), confirming both the failing
    frame and that the Task-2 hook captured it. Return 1 on test-infra failure
    (subprocess could not start / timed out). Return 2 when the broken pipe was NOT
    reproduced (unexpected — fix may already be applied; this is the inverted state
    the Phase-3 regression will assert). End with `if __name__ == "__main__": raise
    SystemExit(main())`.

    Do NOT use `head -c 1` / `head -n 1` (Pitfall 1 — closes too early). Do NOT
    import the runner via importlib — this script spawns it as a subprocess.
  </action>
  <verify>
    <automated>cd scripts && python3 repro_broken_pipe.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/repro_broken_pipe.py` exists, is >= 40 lines, and contains `subprocess.Popen`, `proc.stdout.close()`, `from __future__ import annotations`, and `raise SystemExit(main())` (assertable by grep)
    - Running `cd scripts && python3 repro_broken_pipe.py` exits 0 and prints a PASS line naming the reproduced BrokenPipeError and the count of new run-log error lines
    - The script targets a `--task` that routes through main()'s try/except (NOT `--test-telegram`), recorded in a module-level constant with an explanatory comment (assertable by grep for the constant + comment)
    - Paths are derived via `Path(__file__).resolve().parent` (no hardcoded `/Users/...` absolute path in the file) (assertable by grep `-c '/Users/'` returning 0 after filtering comments)
    - The script runs with `cwd=str(SCRIPTS_DIR)` in its Popen call (assertable by grep `cwd=`)
  </acceptance_criteria>
  <done>The repro script deterministically reproduces the broken pipe, exits 0, and prints a PASS line; the failing frame is the in-try JSON_RESULT= print path, not an earlier print.</done>
</task>

<task type="auto">
  <name>Task 2: Add the additive traceback dump to main()'s except block (D-03/D-04 — the one kept code change)</name>
  <files>scripts/sports_system_runner.py</files>
  <read_first>
    - scripts/sports_system_runner.py lines 5612–5646 (main()'s try/except/finally — the exact insertion site) and lines 192–213 (safe_print + log, the file-sink write idiom to mirror)
    - .planning/phases/01-diagnosis/01-PATTERNS.md § "scripts/sports_system_runner.py — main() except block (MODIFY)" — the exact 3-block insertion (comment + try/except + pass), variable name `_tb_file`, the `RUN_LOG.open("a")` idiom, the rule that this is the ONLY Phase-1 change
    - .planning/phases/01-diagnosis/01-RESEARCH.md § "Where to add the D-03/D-04 traceback hook" (lines 241–272) and § "Phase 1 additive change" (lines 483–502) — NOTE the BrokenPipeError branch + safe_print replacement shown there are PHASE 2, NOT Phase 1
    - .planning/phases/01-diagnosis/01-CONTEXT.md D-03/D-04 (locked: instrumentation stays, writes to run-log file, never solely stdout)
    - ./CLAUDE.md ("No gate logic, pick output, or workbook schema changes"; minimal-invasive)
  </read_first>
  <action>
    Insert exactly one additive block in `scripts/sports_system_runner.py` inside
    `main()`'s `except Exception as e:` (line 5636), immediately AFTER the `err = {...}`
    assignment on line 5637 and BEFORE the `log(f"ERROR task={args.task}: {e}")` on
    line 5638. The block writes the already-computed traceback to the RUN_LOG file
    sink, mirroring the `log()` write idiom (lines 203–213):

    A comment line tagging it `# D-03/D-04: Additive traceback dump to robust file
    sink — never stdout.` followed by `try: with RUN_LOG.open("a") as _tb_file:
    _tb_file.write(f"[{now_iso()}] TRACEBACK task={args.task}:\n{traceback.format_exc()}\n")`
    and `except Exception: pass`. Use the `RUN_LOG` Path constant (line 57), `now_iso()`
    (line 169), and `traceback` (already imported line 25) — no new imports, no new
    module-level state.

    SCOPE GUARDRAILS (per CONTEXT D-03 and PATTERNS line 205): this is the ONLY
    change. Do NOT add a `BrokenPipeError` branch. Do NOT replace the bare `print(...)`
    at line 5634 or 5640 with `safe_print(...)`. Do NOT modify the `log(...)`,
    `send_telegram(...)`, the `finally` block, or any gate / pick / workbook code —
    all of that is Phase 2. The net diff for this file must be the additive
    comment + try/except/pass block only.

    SECRET-SAFETY (threat_model T-01-01): `err["traceback"]` is the Python stack
    trace of the exception; it is already what production computes. Confirm the
    inserted write logs ONLY `traceback.format_exc()` (the stack), not env values,
    not `os.environ`, not any of `TELEGRAM_BOT_TOKEN` / `PRIZEPICKS_COOKIE` /
    `ODDS_API_IO_KEY`. Do not add any new echo of config/secret values.
  </action>
  <verify>
    <automated>cd scripts && python3 -c "import ast,sys; ast.parse(open('sports_system_runner.py').read()); print('syntax-ok')" && grep -n 'TRACEBACK task=' sports_system_runner.py && grep -c 'RUN_LOG.open' sports_system_runner.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/sports_system_runner.py` parses (ast.parse succeeds) after the edit
    - `grep -n 'TRACEBACK task='` returns exactly one new line located inside main()'s except block (between the `err = {...}` line and the `log(f"ERROR task=...")` line)
    - The new write uses `RUN_LOG.open("a")` and `traceback.format_exc()` — verifiable by reading the surrounding diff
    - `git diff scripts/sports_system_runner.py` shows ONLY the additive comment + try/except/pass block — no change to lines 5634/5640's bare `print`, no `BrokenPipeError` branch, no `safe_print` substitution, no gate/pick/schema edits (assertable by reviewing the diff)
    - The inserted code logs only `traceback.format_exc()` and contains no reference to `os.environ`, `env_value`, `TELEGRAM_BOT_TOKEN`, `PRIZEPICKS_COOKIE`, or `ODDS_API_IO_KEY` (assertable by reading the inserted block)
  </acceptance_criteria>
  <done>main()'s except writes the captured traceback to data/pnl/logs/run_log.txt via RUN_LOG.open("a"); the diff is purely additive logging; the file still parses and the repro from Task 1 observes the new TRACEBACK/ERROR lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner process → run-log file (`data/pnl/logs/run_log.txt`) | The additive traceback dump writes process state to a durable file the operator reads; must not leak secrets |
| repro script → real runner subprocess | The repro spawns the production runner; the chosen task must not perform live betting actions, send real alerts, or mutate production workbooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | traceback dump in `main()` except → `run_log.txt` | mitigate | Write ONLY `traceback.format_exc()` (the Python stack), never `os.environ`/`env_value`/secret strings. Task 2 acceptance criterion greps the inserted block to confirm no `TELEGRAM_BOT_TOKEN`/`PRIZEPICKS_COOKIE`/`ODDS_API_IO_KEY` reference. Stack traces of stdlib exceptions (BrokenPipeError) carry no secrets. |
| T-01-02 | Tampering | repro spawning the real runner with a real task | mitigate | Target a side-effect-safe task (`verify`/`check_results`) that does not generate picks, fire line-move Telegram fanout, or mutate production pick rows. The repro closes stdout but does not pass production-mutating task args. Documented in the REPRO_TASK constant comment. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — repro and hook use stdlib + already-installed deps only. No legitimacy gate required. |
</threat_model>

<verification>
- `cd scripts && python3 repro_broken_pipe.py` exits 0 and prints a PASS line (DIAG-01 reproduction artifact exists and works)
- `cd scripts && python3 -c "import ast; ast.parse(open('sports_system_runner.py').read())"` succeeds (no syntax regression)
- `grep -n 'TRACEBACK task=' scripts/sports_system_runner.py` returns the single additive line inside main()'s except
- `git diff scripts/sports_system_runner.py` is purely additive logging (no gate/pick/schema change)
- The existing suite still passes: `cd scripts && python3 -m pytest -q` (the additive hook must not break existing tests)
</verification>

<success_criteria>
- DIAG-01 reproduction artifact: `scripts/repro_broken_pipe.py` deterministically reproduces the `[Errno 32] Broken pipe`, exits 0, and confirms the failing frame is the in-try JSON_RESULT= print (maps to ROADMAP § Phase 1 success criterion 1)
- DIAG-01 captured-trace mechanism: main()'s except now writes the real traceback to the run-log file sink, so a scheduled run leaves a stack trace (the kept D-03/D-04 change)
- Lead #1 (log/obsidian per-line) and Lead #3 (no SIGPIPE/BrokenPipeError handling → spurious TASK FAILED) are exercised by the repro and the trace, feeding the DIAGNOSIS.md synthesis (Plan 03)
- Minimal-invasive honored: exactly one additive logging block committed in source; everything else is a new file
</success_criteria>

<output>
Create `.planning/phases/01-diagnosis/01-01-SUMMARY.md` when done, recording: the repro's exit-code behavior and the failing frame it confirmed, the exact insertion line of the traceback hook, and a pointer to the run-log lines the repro produced (for Plan 03's DIAGNOSIS.md evidence section).
</output>
