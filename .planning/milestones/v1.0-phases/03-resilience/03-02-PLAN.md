---
phase: 03-resilience
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/test_res01_subprocess_retry.py
  - scripts/test_res03_task_timeout.py
autonomous: true
requirements: [RES-01, RES-03, RES-04]
must_haves:
  truths:
    - "A fault-injection test proves the subprocess stage re-runs exactly once on non-zero exit"
    - "A fault-injection test proves exit 0 (empty board) is NOT retried"
    - "A fault-injection test proves two consecutive hard failures cause the stage to raise/propagate"
    - "A subprocess-isolated test proves a hung task self-terminates within its (patched-short) budget, fires ⏱ TASK TIMED OUT, kills the child (no orphan), and exits 1"
    - "A subprocess-isolated test proves a healthy task cancels its alarm and emits neither TIMED OUT nor TASK FAILED"
    - "Each new test fails on pre-fix code and passes on post-fix code (fault-injection by construction)"
    - "Implements locked decisions D-02 (test proves exit-0 not retried), D-05 / D-06 (test proves per-task budget self-termination + ⏱ TASK TIMED OUT), D-11 (fault-injection by construction — each test cannot pass without the fix) — see 03-CONTEXT.md"
  artifacts:
    - path: "scripts/test_res01_subprocess_retry.py"
      provides: "RES-01 subprocess re-run regression (monkeypatch subprocess.Popen)"
      contains: "class TestRes01"
    - path: "scripts/test_res03_task_timeout.py"
      provides: "RES-03 task timeout regression (subprocess-isolated SIGALRM via in-child shim)"
      contains: "TIMED OUT"
  key_links:
    - from: "test_res01_subprocess_retry.py"
      to: "runner.run_fetch_dfs_props / run_build_hit_rate_db / run_generate_projections"
      via: "importlib load + patch subprocess.Popen, assert Popen construction count"
      pattern: "call_count"
    - from: "test_res03_task_timeout.py"
      to: "generated child shim → sports_system_runner.main()"
      via: "subprocess.Popen spawns the shim + proc.wait(timeout=budget+30) + nonce-fence log scan"
      pattern: "TIMED OUT"
---

<objective>
Add the RES-01 and RES-03 regression tests as two independent `unittest` files. Each test injects the exact fault its fix addresses so it cannot pass without the Plan 01 runner changes (D-11 fault-injection-by-construction). RES-01's test is in-process (monkeypatch `subprocess.Popen` — the symbol the Plan 01 helper actually constructs — and assert Popen construction counts). RES-03's test MUST be subprocess-isolated (SIGALRM state is process-wide and bleeds between in-process test cases — Pitfall 3); it spawns a generated child shim that rebinds the `verify` task to hang and shortens its budget, then asserts clean self-termination + the `⏱ TASK TIMED OUT` alert.

Purpose: Satisfy RES-04 (every reliability fix lands with a fail-before/pass-after regression test) for the RES-01 and RES-03 behaviors and lock in the Phase-3 safety net against regression.
Output: `scripts/test_res01_subprocess_retry.py` and `scripts/test_res03_task_timeout.py`, both green against the Plan 01 runner and red against pre-fix code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-resilience/03-PATTERNS.md
@.planning/phases/03-resilience/03-VALIDATION.md
@.planning/phases/03-resilience/03-01-SUMMARY.md

<interfaces>
<!-- Test analogs — read these files directly; they ARE the templates. -->
- scripts/test_fix02_telegram_circuit_breaker.py — RES-01 analog (importlib runner load lines 41-45, setUp/tearDown reset lines 50-63, monkeypatch+call-count lines 68-84, log-capture lines 88-102).
- scripts/test_fix01_broken_pipe.py — RES-03 analog (module constants lines 37-51, drain-and-close-at-sentinel lines 54-69, nonce-fence `_count_nonce_signals` lines 72-93, subprocess.Popen spawn + proc.wait lines 111-184).

Runner symbols available after Plan 01 (load via `importlib.util.spec_from_file_location("sports_system_runner", SCRIPT)`):
- `runner.run_fetch_dfs_props(sport)` / `run_build_hit_rate_db(sport, date)` / `run_generate_projections(sport, date)` — route through `_subprocess_run_with_retry`, which constructs `subprocess.Popen` (NOT `subprocess.run`). The RES-01 test patches `subprocess.Popen`.
- `runner.verify` — module-level task function, referenced by name when `run_task` builds its dispatch `mapping` at call-time (`sports_system_runner.py:5559`). Rebindable in the child shim before `main()` runs.
- `runner.TASK_TIMEOUTS` — module-level dict read by `main()` at call-time; the child shim mutates `TASK_TIMEOUTS["verify"]` before calling `main()`.
- Alert strings emitted to the run log / Telegram: `⏱ TASK TIMED OUT`, `❌ SPORTS TASK FAILED`, `TIMEOUT task=`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_res01_subprocess_retry.py — subprocess re-run fault injection (RES-01)</name>
  <files>scripts/test_res01_subprocess_retry.py</files>
  <read_first>
    - scripts/test_fix02_telegram_circuit_breaker.py (entire file) — the canonical importlib-load + monkeypatch + call-count + log-capture analog
    - scripts/sports_system_runner.py lines 1278-1408 (the three stage functions + the `_subprocess_run_with_retry` helper added in Plan 01) — what is being exercised; read the implemented helper to confirm the exact fake-Popen attribute set it reads (`.wait(timeout=)`, `.returncode`, `.stdout`, `.stderr`, `.kill()`)
    - .planning/phases/03-resilience/03-PATTERNS.md "test_res01_subprocess_retry.py" section (lines 22-105) and "Adaptation for test_res01" (lines 105) — the CORRECTED Popen patch target
    - .planning/phases/03-resilience/03-RESEARCH.md Q5 test_res01 stubs (lines 591-648, the CORRECTED `_FakePopen` + `patch("subprocess.Popen", ...)` stubs) and Pitfall 4 (lines 743-746, the empty-board D-02 guard)
  </read_first>
  <action>
    Create a plain `unittest.TestCase` (`class TestRes01SubprocessRetry`) — no pytest fixtures. Start with `#!/usr/bin/env python3`, a docstring, `from __future__ import annotations`, then the importlib runner-load block copied from test_fix02 (lines 41-45). The Plan 01 helper constructs `subprocess.Popen` (NOT `subprocess.run`), so the monkeypatch target MUST be `subprocess.Popen` — use `patch("subprocess.Popen", side_effect=...)` (or `patch.object(runner.subprocess, "Popen", ...)`). Patching `subprocess.run` is WRONG here: the real `Popen` would still run, `call_count` would never increment, and the retry test would pass even on pre-fix code (silently violating D-11). The side_effect returns a fake-Popen object exposing exactly the attributes the helper reads: `.wait(timeout=...)` (returns the simulated returncode, or raises `subprocess.TimeoutExpired` when you want the timeout path), `.returncode`, `.stdout`/`.stderr` (small `io.BytesIO`/`io.StringIO` matching how the helper reads them — confirm from the implemented helper), and a no-op `.kill()`. Mirror the corrected `_FakePopen` stub in 03-RESEARCH.md lines 604-615; match its attribute access to the implemented helper.
    Write three test methods: (1) `test_subprocess_retry_on_nonzero_exit` — first Popen construction returns returncode 1, second returns 0; assert exactly 2 Popen constructions (`call_count == 2`) and the stage succeeds (run_fetch_dfs_props returns without raising). (2) `test_no_retry_on_clean_exit` — every Popen returns returncode 0 with empty stdout; assert exactly 1 Popen construction (`call_count == 1`) (D-02: empty board is NOT retried — Pitfall 4). (3) `test_after_one_retry_fails_propagates` — both Popen constructions return returncode 1; assert the stage raises (run_fetch_dfs_props re-raises RuntimeError after the single re-run, preserving the `fetch_dfs_props failed for {sport}: exit=` message). Patch `runner.log` and `time.sleep` (so the 5 s backoff does not slow the test) via the log-capture/monkeypatch pattern from test_fix02; restore in `tearDown` or `finally`. Each assertion message must name the RES-01 regression and the expected vs actual call count. End with `if __name__ == "__main__": unittest.main()`. Do not paste the helper implementation — only the test. Keep the test fast (< 10 s) by patching sleep.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_res01_subprocess_retry.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 -m pytest test_res01_subprocess_retry.py -x` exits 0 (all 3 tests pass against the Plan 01 runner)
    - The patch target is `subprocess.Popen` — the file contains `patch("subprocess.Popen"` OR `patch.object(runner.subprocess, "Popen"` and does NOT patch `subprocess.run` for the retry-count assertions; the fake returns a fake-Popen exposing `.wait(timeout=...)`, `.returncode`, `.kill()`, `.stdout`/`.stderr`
    - File contains `class TestRes01SubprocessRetry(unittest.TestCase)` and three `def test_` methods covering: retry-on-nonzero (call_count == 2), no-retry-on-exit-0 (call_count == 1), two-failures-propagate (assertRaises)
    - File starts with `#!/usr/bin/env python3` and `from __future__ import annotations`; ends with `if __name__ == "__main__": unittest.main()`
    - Test patches `time.sleep` (or the runner's sleep call) so the backoff does not add 5 s/retry; total runtime < 15 s
    - Fault-injection rigor (D-11): the no-retry test asserts exactly 1 Popen construction (still passes pre-fix) AND the retry test asserts exactly 2 (FAILS pre-fix — pre-fix code calls `subprocess.run` once with no retry helper, so a `subprocess.Popen` patch records 0/1 constructions, not 2). Note in the SUMMARY that the executor confirmed this test is red against pre-Plan-01 code (or red by construction because the helper symbol is absent).
  </acceptance_criteria>
  <done>The RES-01 test proves the subprocess re-run fires once on hard failure, never on exit 0, and propagates after the single re-run fails — patching `subprocess.Popen` (the symbol the helper constructs) so it would fail against pre-Plan-01 code.</done>
</task>

<task type="auto">
  <name>Task 2: test_res03_task_timeout.py — subprocess-isolated SIGALRM timeout via in-child shim (RES-03)</name>
  <files>scripts/test_res03_task_timeout.py</files>
  <read_first>
    - scripts/test_fix01_broken_pipe.py (entire file) — the subprocess.Popen spawn + proc.wait + nonce-fence log-scan analog
    - scripts/sports_system_runner.py main() (lines 5566-5631 after Plan 01) — the SIGALRM arming, `⏱ TASK TIMED OUT` alert, `except TaskTimeoutError`, `signal.alarm(0)` cancel
    - scripts/sports_system_runner.py line ~5559 (run_task builds its dispatch `mapping` at call-time, referencing `verify` by name) — why the in-child rebind of `r.verify` takes effect
    - .planning/phases/03-resilience/03-PATTERNS.md "test_res03_task_timeout.py" section (lines 224-279, the CONFIRMED in-child-shim mechanism) and Shared Patterns (nonce-fence lines 599-613)
    - .planning/phases/03-resilience/03-RESEARCH.md Q5 test_res03 stub (lines 692-731), Pitfall 3 (lines 737-741, why subprocess isolation is mandatory), Open Question 3 (RESOLVED — the confirmed in-child shim mechanism)
  </read_first>
  <action>
    Create a plain `unittest.TestCase` (`class TestRes03TaskTimeout`). Header same as test_fix01 (`#!/usr/bin/env python3`, docstring, `from __future__ import annotations`, imports: subprocess, sys, time, tempfile, os, unittest, uuid, pathlib). Copy the module-level constants (SCRIPTS_DIR, RUNNER, RUN_LOG) and the nonce-fence `_count_nonce_signals`-style helper from test_fix01 — but generalize the post-fence scan to count occurrences of arbitrary target substrings (so it can count `TIMED OUT`, `TASK FAILED`, and `TIMEOUT task=` separately). Define a module constant `_SHIM_BUDGET = 3` (the short patched budget the shim sets) and `_WAIT_TIMEOUT = _SHIM_BUDGET + 30` (≈33 s — gives the clean-shutdown sequence headroom but still fails fast if SIGALRM never fires; the test completes in well under the 90 s quick-suite target).
    The timeout MUST be exercised in a spawned child (Pitfall 3 — never in-process; SIGALRM is process-wide). Inject the hang via a generated child shim, with NO change to the production runner (this is the CONFIRMED mechanism from 03-PATTERNS.md "Adaptation for test_res03 — CONFIRMED mechanism" and 03-RESEARCH.md Open Question 3). In a helper, write a tiny temp `.py` shim to a `tempfile`-created path (clean it up in `addCleanup`/`finally`). The shim, when given an argument controlling whether to hang, does in the isolated child process: import the runner module (add SCRIPTS_DIR to `sys.path`, `import sports_system_runner as r`), set `r.TASK_TIMEOUTS["verify"] = <budget>`, and — for the hang case — rebind `r.verify = lambda: __import__("time").sleep(9999)`; then `sys.exit(r.main())`. The shim is spawned via `subprocess.Popen([sys.executable, shim_path, "--task", "verify"], stdout=PIPE, stderr=PIPE, cwd=str(SCRIPTS_DIR))`. This works because `run_task` builds its dispatch `mapping` at call-time referencing `verify` by name, so the rebind of `r.verify` takes effect, and `TASK_TIMEOUTS` is read by `main()` at call-time. Do NOT add any env-var hook or test-mode branch to the runner — the shim is the committed mechanism (honors minimal-invasive; 03-01 Task 3 stays clean).
    Write two test methods. (1) `test_timeout_fires_on_hung_task` — generate the HANG shim (`r.verify` rebound to `time.sleep(9999)`, `TASK_TIMEOUTS["verify"] = _SHIM_BUDGET`); write a uuid nonce fence to RUN_LOG (skipTest on infra failure); spawn the shim; `proc.wait(timeout=_WAIT_TIMEOUT)` — on `subprocess.TimeoutExpired` do `proc.kill()` + `proc.wait()` and `self.fail(...)` (SIGALRM did not fire — RES-03 regression). Assert: the process exited within `_WAIT_TIMEOUT` (no TimeoutExpired raised in the harness), `returncode == 1`, the post-fence run-log / captured stdout+stderr contains `TIMED OUT`, and does NOT contain `TASK FAILED`. (2) `test_healthy_task_cancels_alarm` — generate a NON-hang shim (real `verify`, leave its budget comfortable, e.g. `TASK_TIMEOUTS["verify"] = _SHIM_BUDGET` is fine because real `verify` finishes well under 3 s, or use the default 60); spawn it; assert exit 0 within `_WAIT_TIMEOUT` and the post-fence content contains neither `TIMED OUT` nor `TASK FAILED` (alarm cancelled cleanly in finally). Use `self.skipTest(...)` for genuine infra failures (cannot write fence / cannot write shim / cannot spawn) so infra flakiness is not a false RES-03 red. End with `if __name__ == "__main__": unittest.main()`.
    Pre-fix behavior (be aware; do not implement): pre-Plan-01 the runner has no SIGALRM arming, so the hang shim's `time.sleep(9999)` never completes — the child never exits, the harness `proc.wait(timeout=_WAIT_TIMEOUT)` raises `TimeoutExpired`, and the test fails. Post-fix SIGALRM fires at `_SHIM_BUDGET` (3 s) → `⏱ TASK TIMED OUT` → exit 1, so the test passes. This is the D-11 fail-before/pass-after rigor — do not weaken it with a SUMMARY deferral.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_res03_task_timeout.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 -m pytest test_res03_task_timeout.py -x` exits 0 and completes in well under 90 s (≈ _WAIT_TIMEOUT ≈ 33 s worst case; healthy run far faster)
    - The test spawns a generated child SHIM (`subprocess.Popen([sys.executable, <shim_path>, "--task", "verify"], ...)`) — NOT in-process SIGALRM (the file contains `subprocess.Popen` and does NOT call `signal.alarm` in-process)
    - The hang shim rebinds `r.verify` (to a `time.sleep(9999)`-style hang) AND sets `r.TASK_TIMEOUTS["verify"]` to the short `_SHIM_BUDGET` (3) before `r.main()` — confirm the shim source written by the test contains both the verify rebind and the TASK_TIMEOUTS mutation
    - `_WAIT_TIMEOUT == _SHIM_BUDGET + 30` (≈33 s) and `_SHIM_BUDGET == 3`
    - The hung-task test asserts: exits within `_WAIT_TIMEOUT` (no harness TimeoutExpired), `returncode == 1`, post-fence output contains `TIMED OUT`, post-fence output does NOT contain `TASK FAILED`
    - The healthy-task test asserts exit 0 and absence of both `TIMED OUT` and `TASK FAILED`
    - The generated temp shim is cleaned up (`addCleanup`/`finally` unlinks it)
    - No env-var hook / test-mode branch is referenced (the runner was not modified for testing — `grep -L 'HERMES_TEST' ` would show no such hook used)
    - File starts with `#!/usr/bin/env python3` / `from __future__ import annotations`; ends with `if __name__ == "__main__": unittest.main()`
    - Uses `self.skipTest` for infra failures (fence write / shim write / spawn) so infra issues are not false RES-03 reds
  </acceptance_criteria>
  <done>The RES-03 test proves, in an isolated child shim subprocess, that a hung task self-terminates within its 3 s patched budget with the `⏱ TASK TIMED OUT` alert and exit 1 (and would hang forever / fail the harness on pre-fix code), while a healthy task cancels its alarm and emits neither timeout nor failure alerts. No production-runner change is required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → runner module (in-process importlib) | RES-01 test loads and monkeypatches runner internals; no untrusted input |
| test → generated child shim subprocess | RES-03 test writes a temp shim and spawns it as a child; reads its stdout/run log |
| test → production run_log.txt | Tests append a nonce fence and scan only post-fence content (must not corrupt the operator's log) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-T1 | Tampering | Test polluting the production `run_log.txt` with spurious failure markers | mitigate | Reuse the test_fix01 nonce-fence: write a uuid fence line, scan only content after the fence, never write fake `ERROR`/`TASK FAILED` markers; this is the WR-03-hardened pattern already in use |
| T-03-T2 | Repudiation | A flaky infra failure (cannot spawn / cannot write fence / cannot write shim) masquerading as a RES-03 regression | mitigate | Use `self.skipTest(...)` for genuine infra failures so only real fault-injection outcomes assert; `_INFRA_FAILURE` sentinel from the analog |
| T-03-T3 | Denial of Service | Orphaned child shim / grandchild processes left by the RES-03 subprocess test | mitigate | `proc.kill()` + `proc.wait()` in the test's TimeoutExpired branch (analog pattern); the runner's own SIGALRM handler kills its grandchild subprocess; the generated temp shim is unlinked in cleanup |

No `npm`/`pip`/`cargo` installs in this phase → Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- Quick command: `cd scripts && python3 -m pytest test_res01_subprocess_retry.py test_res03_task_timeout.py -x` exits 0.
- Tests are plain `unittest.TestCase` (no pytest fixtures), run from `scripts/` with `python3`.
- RES-03 test isolates SIGALRM in a spawned child shim (no in-process `signal.alarm`); `_WAIT_TIMEOUT = _SHIM_BUDGET + 30` ≈ 33 s keeps it well under the 90 s quick-suite target; RES-01 test patches `subprocess.Popen` and `time.sleep` so it stays fast.
- No new pre-existing-baseline failures introduced (baseline = `2 failed, 202 passed`).
</verification>

<success_criteria>
- RES-01 regression: re-run-once on hard failure, no-retry on exit 0, propagate after second failure — all asserted with Popen construction counts via `subprocess.Popen` patching; red against pre-Plan-01 code.
- RES-03 regression: subprocess-isolated (in-child-shim) proof a hung task self-terminates with `⏱ TASK TIMED OUT` + exit 1 within a 3 s patched budget, and a healthy task cancels the alarm cleanly; no production-runner test hook added.
- Both files are plain unittest, fault-injection-by-construction, fast (< 90 s), and CI-friendly.
</success_criteria>

<output>
Create `.planning/phases/03-resilience/03-02-SUMMARY.md` when done. Record the confirmation that the RES-03 in-child-shim mechanism produces fail-before/pass-after behavior (hang forever pre-fix → `TIMED OUT` exit 1 post-fix) and that no production-runner change was needed.
</output>
