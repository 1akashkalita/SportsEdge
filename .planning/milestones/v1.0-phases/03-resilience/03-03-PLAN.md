---
phase: 03-resilience
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/test_res02_pipe_reclassify.py
  - .planning/phases/03-resilience/03-PHASE2-AUDIT.md
autonomous: true
requirements: [RES-02, RES-04]
must_haves:
  truths:
    - "A subprocess-isolated test proves a BrokenPipeError AFTER task completion produces no ❌ SPORTS TASK FAILED alert, logs a warning, and exits 0"
    - "A subprocess-isolated test proves a BrokenPipeError / failure DURING run_task() (task NOT complete) still fires ❌ SPORTS TASK FAILED and exits 1"
    - "The test suite distinguishes post-completion pipe close (no alert) from a pre-completion failure (still alerts)"
    - "All four Phase-2 regression tests are audited and confirmed to genuinely fail-before / pass-after (D-10)"
    - "The full suite is green relative to the known baseline (2 failed, 202 passed) with all Phase-3 tests added"
    - "Implements locked decisions D-08 (test proves reclassify only after task completed; pre-completion still alerts), D-10 (audit both — confirm the four Phase-2 tests fail-before/pass-after), D-11 (fault-injection by construction) — see 03-CONTEXT.md"
  artifacts:
    - path: "scripts/test_res02_pipe_reclassify.py"
      provides: "RES-02 pipe-reclassification regression — post-completion (no alert) + pre-completion (still alerts)"
      contains: "TASK FAILED"
    - path: ".planning/phases/03-resilience/03-PHASE2-AUDIT.md"
      provides: "D-10 audit record: each Phase-2 test fails-before/passes-after"
      contains: "test_fix01_broken_pipe"
  key_links:
    - from: "test_res02_pipe_reclassify.py"
      to: "sports_system_runner.py --task verify subprocess"
      via: "Popen + reader thread closes stdout at sentinel + nonce-fence scan for TASK FAILED"
      pattern: "TASK FAILED"
    - from: "03-PHASE2-AUDIT.md"
      to: "test_fix01/fix02/def01/def02"
      via: "documented fail-before/pass-after evidence per Phase-2 fix"
      pattern: "fail-before"
---

<objective>
Close out RES-02 and RES-04. (1) Add the RES-02 pipe-reclassification regression test with BOTH cases (VALIDATION.md rows 3-RES02-a and 3-RES02-b): a subprocess-isolated post-completion case (close the runner's stdout AFTER the task body completes → no `❌ SPORTS TASK FAILED`, exit 0) AND a pre-completion case (inject a failure DURING `run_task()`, before the completion sentinel → `❌ SPORTS TASK FAILED` IS present, exit 1). The pre-completion case is the negative proof that the `_task_result` gate does not mask genuine mid-task failures (D-08). (2) Perform the D-10 Phase-2 test audit: confirm each of the four existing Phase-2 regression tests genuinely fails on pre-fix code and passes on post-fix code, recording the evidence in a short audit document. (3) Run the full suite as the phase-gate regression sweep and confirm it is green relative to the known baseline.

Purpose: Satisfy RES-02 (broken-pipe gracefully tolerated, never a spurious TASK FAILED — but a real mid-task failure STILL alerts) with fault-injection tests for both directions, and satisfy RES-04's "scope = both" requirement — audit the Phase-2 tests AND add Phase-3 tests — plus prove the whole phase did not regress the suite.
Output: `scripts/test_res02_pipe_reclassify.py` (green, two test methods), `.planning/phases/03-resilience/03-PHASE2-AUDIT.md` (the D-10 audit record), and a recorded full-suite result at the baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-resilience/03-PATTERNS.md
@.planning/phases/03-resilience/03-VALIDATION.md
@.planning/phases/03-resilience/03-01-SUMMARY.md

<interfaces>
<!-- Analogs — read directly; they are the templates. -->
- scripts/test_fix01_broken_pipe.py — RES-02 analog (constants lines 37-51, drain-and-close-at-sentinel lines 54-69, nonce-fence `_count_nonce_signals` lines 72-93, Popen+reader+wait+assertions lines 111-184). RES-02 extends it with a `TASK FAILED` post-fence scan.
- scripts/test_fix01_broken_pipe.py, test_fix02_telegram_circuit_breaker.py, test_def01_no_duplicate_defs.py, test_def02_path_resolution.py — the four Phase-2 tests to audit (D-10).

Runner behavior after Plan 01 (RES-02): in `main()`'s except branch, `if _task_result is not None and isinstance(e, BrokenPipeError):` logs `WARNING: BrokenPipeError after task completion — cron pipe closed; task data persisted` and returns 0 WITHOUT sending `❌ SPORTS TASK FAILED`. A pre-completion exception (any exception, OR a BrokenPipeError before `_task_result` is set) still sends the `❌ SPORTS TASK FAILED` alert and returns 1.

Known baseline: `2 failed, 202 passed` — the 2 failures are pre-existing in `test_generate_projections.py` and MUST NOT be "fixed" here. A clean Phase-3 run adds no new failures beyond that baseline. Full suite ~34 min — run it once at phase end, not per task.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_res02_pipe_reclassify.py — post-completion (no alert) + pre-completion (still alerts) regression (RES-02)</name>
  <files>scripts/test_res02_pipe_reclassify.py</files>
  <read_first>
    - scripts/test_fix01_broken_pipe.py (entire file) — the subprocess + reader-thread sentinel-close + nonce-fence analog to copy and extend
    - scripts/sports_system_runner.py main() lines 5566-5631 (after Plan 01) — the `_task_result` guard + suppressed `❌ SPORTS TASK FAILED`; note that a pre-completion error leaves `_task_result is None` so the alert still fires
    - .planning/phases/03-resilience/03-PATTERNS.md "test_res02_pipe_reclassify.py" section (lines 109-220) — exact adaptation (add a `TASK FAILED` scan; no in-process send_telegram patch needed because the subprocess model captures everything via the log)
    - .planning/phases/03-resilience/03-RESEARCH.md Q5 test_res02 stub (lines 652-690) and Q4 (lines 494-570, pre-fix behavior that makes the post-completion test fail)
    - .planning/phases/03-resilience/03-VALIDATION.md rows 3-RES02-a (post-completion: no alert, exit 0) and 3-RES02-b (pre-completion DURING run_task: still fires TASK FAILED)
  </read_first>
  <action>
    Create a plain `unittest.TestCase` (`class TestRes02PipeReclassify`). Header and imports identical to test_fix01 (`#!/usr/bin/env python3`, docstring, `from __future__ import annotations`, subprocess/sys/threading/time/unittest/uuid/pathlib). Copy the module constants (SCRIPTS_DIR, RUNNER, RUN_LOG, `REPRO_TASK = "verify"`, the `verify`-task completion sentinel string, `_WAIT_TIMEOUT`, `_INFRA_FAILURE`), the `_drain_and_close_at_sentinel(proc)` reader-thread function, and the nonce-fence helper — but generalize the post-fence scan so it counts `"TASK FAILED"` occurrences (in addition to / instead of the `ERROR task=`/`Broken pipe` counts the analog uses).

    Write TWO test methods.

    (1) `test_no_task_failed_alert_on_post_completion_pipe_close` (row 3-RES02-a): write a uuid nonce fence to RUN_LOG (skipTest on infra failure), spawn `[sys.executable, "-u", str(RUNNER), "--task", "verify"]` with stdout/stderr PIPE and `cwd=str(SCRIPTS_DIR)`, start the reader thread that closes `proc.stdout` when it sees the `verify` COMPLETION sentinel (forcing a BrokenPipeError on the runner's subsequent stdout write — AFTER the task body completed, which is exactly the RES-02 post-completion condition), `proc.wait(timeout=_WAIT_TIMEOUT)` (kill+fail on TimeoutExpired). Then assert: `returncode == 0` (clean exit, NOT 1), AND the post-fence run-log content contains ZERO `"TASK FAILED"` occurrences (the RES-02 suppression assertion). Optionally also assert a `BrokenPipeError after task completion` warning line appears post-fence (defense-in-depth). Use `_INFRA_FAILURE` guarding like the analog so a missing fence does not false-fail.

    (2) `test_task_failed_fires_on_pre_completion_error` (row 3-RES02-b — the negative proof for D-08): inject a failure DURING `run_task()`, BEFORE the completion sentinel, so `_task_result` is still `None`. Use a reader thread that closes `proc.stdout` at the FIRST line of the runner's output (or at a known EARLY pre-completion marker line — NOT the completion sentinel) so the BrokenPipeError fires while the task is still running; this leaves `_task_result is None`, so the runner's except branch must take the real-failure path. (If closing stdout that early does not reliably produce a pre-completion BrokenPipeError for the `verify` task, fall back to the in-child-shim approach used in Plan 02's RES-03 test: write a temp shim that rebinds `r.verify` to raise an exception mid-run before completion, spawn it, and assert the alert fires — but prefer the early-stdout-close approach because it most directly models the cron pipe.) Write a uuid nonce fence, spawn, run the reader thread closing stdout pre-completion, `proc.wait(timeout=_WAIT_TIMEOUT)`. Then assert: `returncode == 1` (NOT 0) AND the post-fence run-log content contains `"TASK FAILED"` at least once (the alert IS present — proving the `_task_result` gate does not mask genuine mid-task failures). Use `self.skipTest` for genuine infra failures (cannot write fence / cannot spawn).

    End with `if __name__ == "__main__": unittest.main()`.
    Pre-fix / negative-control awareness (do not implement, just be aware): without the `_task_result` guard, the except branch fires `send_telegram("❌ SPORTS TASK FAILED: ...")` on the post-completion BrokenPipeError too → test (1) would see `TASK FAILED` count ≥ 1 and returncode 1, so it is red pre-fix. Test (2) is the negative control that asserts the alert STILL fires when the failure is genuine (pre-completion) — it should pass both pre- and post-fix (it guards against an over-broad guard that would suppress real failures).
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_res02_pipe_reclassify.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 -m pytest test_res02_pipe_reclassify.py -x` exits 0 (both test methods pass against the Plan 01 runner)
    - The file contains TWO `def test_` methods: `test_no_task_failed_alert_on_post_completion_pipe_close` (post-completion) and `test_task_failed_fires_on_pre_completion_error` (pre-completion)
    - Both methods spawn the runner as a SUBPROCESS with a reader thread (`subprocess.Popen` + `threading.Thread` reader); neither runs SIGALRM/pipe logic in-process
    - Post-completion assertions: `returncode == 0` AND post-fence `TASK FAILED` count == 0 (reader closes stdout at the COMPLETION sentinel)
    - Pre-completion assertions: `returncode == 1` AND post-fence `TASK FAILED` count ≥ 1 (reader closes stdout BEFORE completion, leaving `_task_result is None`)
    - Uses the nonce-fence isolation (writes a uuid fence, scans only post-fence content); `self.skipTest` on infra failure
    - File starts with `#!/usr/bin/env python3` / `from __future__ import annotations`; ends with `if __name__ == "__main__": unittest.main()`
    - The post-completion method would FAIL on pre-Plan-01 code (no `_task_result` guard → TASK FAILED fires + exit 1); the pre-completion method is the negative control that the guard does not over-suppress
  </acceptance_criteria>
  <done>The RES-02 test proves (a) a BrokenPipeError AFTER task completion yields exit 0 and zero TASK FAILED alerts (red against pre-fix code) and (b) a failure DURING run_task() still fires TASK FAILED and exits 1 (the D-08 negative proof that the `_task_result` gate does not mask genuine mid-task failures).</done>
</task>

<task type="auto">
  <name>Task 2: D-10 Phase-2 test audit + record 03-PHASE2-AUDIT.md</name>
  <files>.planning/phases/03-resilience/03-PHASE2-AUDIT.md</files>
  <read_first>
    - scripts/test_fix01_broken_pipe.py, scripts/test_fix02_telegram_circuit_breaker.py, scripts/test_def01_no_duplicate_defs.py, scripts/test_def02_path_resolution.py — the four tests to audit
    - .planning/phases/03-resilience/03-RESEARCH.md Q5 Phase-2 audit table (lines 576-587) — the expected fail-before/pass-after per test and the conclusion (no gaps found); WR-03 status (line 587)
    - .planning/phases/03-resilience/03-CONTEXT.md D-10/D-11 (lines 98-109) — audit scope and rigor
  </read_first>
  <action>
    For each of the four Phase-2 tests, confirm two things by reading the test body: (a) it injects/asserts the exact pre-fix fault (the fail-before mechanism: test_fix01 — pre-fix runner exits 1 with broken-pipe signals; test_fix02 — pre-fix has no `_telegram_breaker` so AttributeError / 100s stall; test_def01 — AST finds 2 defs pre-fix; test_def02 — source contains the hardcoded `akashkalita` path pre-fix), and (b) it passes on the current post-fix code. Run all four (`cd scripts && python3 -m pytest test_fix01_broken_pipe.py test_fix02_telegram_circuit_breaker.py test_def01_no_duplicate_defs.py test_def02_path_resolution.py`) and capture the pass result. Write `.planning/phases/03-resilience/03-PHASE2-AUDIT.md` containing: a row per test with columns [test file | Phase-2 fix | fail-before mechanism (the specific assertion/injection that would fail on pre-fix code) | passes-after (pytest result) | gap?]; the WR-03 status (repro_broken_pipe.py + test_fix01 already hardened with the nonce fence — confirm by reading); and an explicit conclusion sentence stating whether any gaps were found and, if so, what remediation is needed. If the audit finds a genuine gap (a test that would NOT fail on pre-fix code), record it AND raise it in the SUMMARY as a follow-up rather than silently passing — but per RESEARCH.md the expectation is "no gaps found." Do NOT modify the Phase-2 test files in this task; this is verify-and-document (D-10), not rewrite.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_fix01_broken_pipe.py test_fix02_telegram_circuit_breaker.py test_def01_no_duplicate_defs.py test_def02_path_resolution.py</automated>
  </verify>
  <acceptance_criteria>
    - `03-PHASE2-AUDIT.md` exists and contains a row for each of `test_fix01_broken_pipe`, `test_fix02_telegram_circuit_breaker`, `test_def01_no_duplicate_defs`, `test_def02_path_resolution`
    - Each row names the SPECIFIC fail-before mechanism (not "it fails") and the passes-after pytest result
    - The document records WR-03 status (nonce-fence hardening confirmed) and an explicit gaps conclusion
    - The four Phase-2 tests pass: `python3 -m pytest <the four files>` exits 0
    - No Phase-2 test file was modified (audit is verify-and-document only)
  </acceptance_criteria>
  <done>All four Phase-2 regression tests are confirmed fail-before/pass-after with the specific mechanism documented in 03-PHASE2-AUDIT.md; no gaps (or any gap explicitly flagged for follow-up).</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite phase-gate regression sweep at the known baseline</name>
  <files>.planning/phases/03-resilience/03-PHASE2-AUDIT.md</files>
  <read_first>
    - .planning/phases/03-resilience/03-VALIDATION.md (Sampling Rate + Test Infrastructure) — the baseline (`2 failed, 202 passed`), full-suite command, ~34 min runtime
    - $HOME/.claude/projects/-Users-akashkalita-sports-picks/memory/pre-existing-projection-test-failures.md — the 2 known projection failures that are NOT to be "fixed"
  </read_first>
  <action>
    Run the full suite once as the phase gate (this is the slow ~34 min run — do it after Tasks 1-2 and after Plans 01/02 are merged into the working tree). Command: `cd scripts && python3 -m pytest`. Confirm the result is `2 failed, 202+ passed` (the 2 failures are the pre-existing `test_generate_projections.py` failures from memory — they MUST remain and MUST NOT be touched) plus all new RES-* tests passing (test_res01, test_res02 with BOTH its methods, test_res03). If the count of failures exceeds 2, or any failure is NOT one of the two known projection failures, that is a Phase-3 regression — record it and STOP (do not mask it). Append the full-suite result (pass/fail counts + confirmation the only failures are the 2 known projection ones + the list of new RES-* tests now passing) as a short "Full-Suite Regression Sweep" section to `03-PHASE2-AUDIT.md`. Do NOT attempt to fix the 2 pre-existing projection failures (out of scope; memory + VALIDATION.md baseline).
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Full suite run recorded; failure count == 2 and both are the known `test_generate_projections.py` failures (no new failures introduced by Phase 3)
    - All three new RES-* tests (`test_res01_subprocess_retry.py`, `test_res02_pipe_reclassify.py` — both methods, `test_res03_task_timeout.py`) are in the passed set
    - The 4 Phase-2 audit tests are in the passed set
    - `03-PHASE2-AUDIT.md` has a "Full-Suite Regression Sweep" section with the pass/fail counts and the explicit confirmation the only failures are the 2 pre-existing projection ones
    - The 2 pre-existing projection failures were NOT modified/"fixed"
  </acceptance_criteria>
  <done>The full suite is green relative to the known baseline: exactly the 2 pre-existing projection failures, all Phase-2 and Phase-3 regression tests passing, no new regressions, and the result recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → spawned runner subprocess | RES-02 test spawns the runner and closes its stdout (post- or pre-completion) |
| test → production run_log.txt | The pipe test appends a nonce fence and scans only post-fence content |
| audit doc → planning record | The audit asserts test rigor; a false "no gaps" would let a weak test through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-T4 | Tampering | RES-02 test polluting the production `run_log.txt` | mitigate | Nonce-fence isolation (WR-03-hardened analog); scan only post-fence; no fake markers written |
| T-03-T5 | Repudiation | A weak Phase-2 test passing the D-10 audit despite not failing pre-fix | mitigate | Audit records the SPECIFIC fail-before mechanism per test (not "it fails"); any genuine gap flagged in SUMMARY, not silently passed |
| T-03-T6 | Repudiation | Masking a real Phase-3 regression by accepting >2 full-suite failures | mitigate | Task 3 asserts failure count == 2 AND both are the known projection failures; any other failure stops the gate |
| T-03-T7 | Repudiation | An over-broad `_task_result` guard suppressing a genuine mid-task failure alert | mitigate | The pre-completion RES-02 test (3-RES02-b) asserts `TASK FAILED` IS present and exit 1 when the failure fires before completion — the negative control proving the guard does not mask real failures (D-08) |

No `npm`/`pip`/`cargo` installs in this phase → Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- `cd scripts && python3 -m pytest test_res02_pipe_reclassify.py -x` exits 0 (both post- and pre-completion methods).
- The four Phase-2 audit tests pass.
- Full suite: exactly the 2 pre-existing projection failures, everything else (including all RES-* and FIX/DEF tests) green.
- `03-PHASE2-AUDIT.md` records both the per-test audit and the full-suite sweep.
</verification>

<success_criteria>
- RES-02 regression: subprocess-isolated proof that (a) a post-completion BrokenPipeError yields exit 0 and zero TASK FAILED alerts (red against pre-fix code) AND (b) a pre-completion failure still fires TASK FAILED and exits 1 (the D-08 negative control).
- RES-04 (audit half): all four Phase-2 tests confirmed fail-before/pass-after with documented mechanisms; no undocumented gaps.
- Full suite green at the known baseline (2 failed projection-only, 202+ passed) with all Phase-3 tests passing.
</success_criteria>

<output>
Create `.planning/phases/03-resilience/03-03-SUMMARY.md` when done.
</output>
