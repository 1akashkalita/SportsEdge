---
phase: 04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02
plan: 03
type: execute
wave: 2
depends_on: ["04.1-02"]
files_modified:
  - scripts/sports_system_runner.py
  - scripts/test_sync_slip_bankroll.py
  - scripts/test_weekly_metrics.py
autonomous: true
requirements: [BANKROLL-02]
must_haves:
  truths:
    - "D-06: after a non-dry-run daily grade/sync (sync_slip_bankroll), the Prop Accuracy sheet reflects the just-graded results — it stays fresh all week at zero extra lock cost"
    - "D-06: weekly_metrics_task stays read-only on master_pnl.xlsx (no write lock, no save) — its only write remains calibration.json"
    - "D-07: a weekly_metrics status=partial run produces a visibly degraded Telegram digest every time, and two consecutive partials trip the OBS-03 repeated-failure alert; a single transient partial does NOT hard-fail the task or fire the alert"
    - "All four v2.0 audit fixes (BANKROLL-02 forward staking, D-06 prop-accuracy refresh, D-08 calibration dedup, D-07 WR-03 visibility) land with no gate-logic / verdict / schema change and the full suite holds the 2-failed / 202+-passed baseline (METRICS-03 integrity intact)"
  artifacts:
    - path: "scripts/sports_system_runner.py"
      provides: "refresh_prop_accuracy(wb) inside sync_slip_bankroll if-not-dry_run before save; weekly_metrics partial -> degraded digest + JSONL partial status + OBS-03 repeated-partial alert; trailing_failure_streak counts partial"
      contains: "refresh_prop_accuracy(wb)"
    - path: "scripts/test_sync_slip_bankroll.py"
      provides: "TestPropAccuracyRefresh — Prop Accuracy populated after non-dry-run sync; dry-run does not populate"
      contains: "class TestPropAccuracyRefresh"
    - path: "scripts/test_weekly_metrics.py"
      provides: "TestPartialVisibility (degraded digest, streak counts partial, single partial < threshold) + TestReadOnly (no master_pnl write lock)"
      contains: "class TestPartialVisibility"
  key_links:
    - from: "scripts/sports_system_runner.py sync_slip_bankroll"
      to: "scripts/sports_system_runner.py refresh_prop_accuracy"
      via: "in-memory Prop Accuracy rewrite before save_workbook_atomic (mirrors rebuild_slip_bankroll:5681)"
      pattern: "refresh_prop_accuracy\\(wb\\)"
    - from: "scripts/sports_system_runner.py weekly_metrics_task / main finally"
      to: "scripts/sports_system_runner.py trailing_failure_streak"
      via: "status=partial propagated to run_log.jsonl and counted toward OBS-03"
      pattern: "partial"
---

<objective>
Land the two `sports_system_runner.py` fixes from the v2.0 audit, then gate the whole phase. Gap 2 (D-06): the `Prop Accuracy` sheet only refreshes inside `rebuild_slip_bankroll` (line 5681), never on the daily `grade_slips` -> `sync_slip_bankroll` path, so `weekly_metrics` can report a stale prop hit-rate. Gap 4 (D-07/WR-03): `weekly_metrics_task` returns normally on `status=partial` (lines 7309/7331), so the run reads green — no degraded digest, and the OBS-03 streak never sees it. This plan adds a one-line `refresh_prop_accuracy(wb)` on the daily path, makes a partial visible (degraded Telegram marker + JSONL partial status + repeated-partial OBS-03 alert), and runs the full suite to confirm all four fixes (this plan + Plans 01/02) preserve the baseline.

Both Gap 2 and Gap 4 modify the same file (`sports_system_runner.py`), so they are one plan (cannot be parallel). This plan runs in Wave 2 because it shares `test_weekly_metrics.py` with Plan 02 (Wave 1) — depends_on 04.1-02 to avoid a test-file conflict.

Purpose: Close the last two v2.0 audit gaps on the runner and prove the full phase regression-free.
Output: `sync_slip_bankroll` + `weekly_metrics_task`/`main`/`trailing_failure_streak` edits, two new test classes + one extended, and the phase full-suite gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-CONTEXT.md
@.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-RESEARCH.md
@.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-PATTERNS.md
@.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-VALIDATION.md

<interfaces>
<!-- Verified contracts — no codebase exploration needed. -->

From scripts/sports_system_runner.py:
- sync_slip_bankroll(date, dry_run=False, _wb_override=None, _master_override=None, _bankroll_override=None) -> dict (line 5132).
  Has an `if not dry_run:` block ending at line ~5293 with `save_workbook_atomic(wb, master)` then `bankroll_path.write_text(...)`. The `wb` object is already loaded; refresh_performance_breakdown(wb, ...) already runs in the block.
- refresh_prop_accuracy(wb: Any) -> None (line 5299): pure in-memory rewrite of the "Prop Accuracy" sheet from "Pick History" in the same wb. Zero extra I/O.
- rebuild_slip_bankroll (line 5362): the PATTERN TO MIRROR — at line 5681 it calls refresh_prop_accuracy(wb) immediately before save_workbook_atomic(wb, master) at line 5683 (inside its own not-dry-run block).
- weekly_metrics_task() -> dict (line 7271): sets result["status"]="partial" at 7309 (report aggregation fail) and 7331 (calibration recompute fail); returns normally. Builds `digest` and calls result["telegram_sent"] = send_telegram(digest) at line 7339. Returns `result` at the end (line ~7362). Read-only on master_pnl.xlsx (its only write is calibration.json).
- trailing_failure_streak(task) -> int (line 394): walks run_log.jsonl backward, counts trailing status in {"error","timeout"} (line 424), stops at "ok"; returns 0 on any error. Reads PRIOR records only (current run's record is written later in finally).
- REPEATED_FAILURE_THRESHOLD (line 389): int, default 2.
- main() (line ~7400): `_task_result: dict|None = None` (7410), `_run_status="ok"` (7412); on success path `_task_result = result` (7432) then dispatch_alerts + return 0. The except TaskTimeoutError / except Exception branches fire the `🔁 REPEATED FAILURE` alert via `_obs03_streak = trailing_failure_streak(args.task) + 1; if _obs03_streak >= REPEATED_FAILURE_THRESHOLD: send_telegram("🔁 REPEATED FAILURE...")`. The finally block (lines 7503-7511) writes ONE JSONL record with `"status": _run_status`.
- dispatch_alerts(task, result) (line ~1251): returns early when result.get("status") != "ok" (so it does NOT double-send the weekly digest; weekly_metrics_task sends its own).

From scripts/test_weekly_metrics.py:
- importlib + Workbook fixture header (lines 1-67): runner = exec_module(sports_system_runner); `from openpyxl import Workbook`; `_make_pick_history_wb(rows)` builds a wb with "Pick History" sheet from runner.RESULT_HEADERS.
- RUN_LOG_JSONL path constant on runner; trailing_failure_streak reads it.

From scripts/test_build_slips.py / test_stake_sizing.py: existing, green (Plan 01 adds TestForwardStaking).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prop-Accuracy refresh on the daily path + TestPropAccuracyRefresh</name>
  <files>scripts/sports_system_runner.py, scripts/test_sync_slip_bankroll.py</files>
  <read_first>
    - scripts/sports_system_runner.py lines 5132-5165 (sync_slip_bankroll signature + override params), 5205-5294 (the not-dry-run block, refresh_performance_breakdown, save_workbook_atomic at 5293, bankroll_path.write_text at 5294), 5299-5360 (refresh_prop_accuracy body — what it reads/writes), 5660-5684 (rebuild_slip_bankroll: refresh_prop_accuracy(wb) at 5681 then save at 5683 — the exact pattern to mirror), 299 (PROP_ACCURACY_HEADERS)
    - scripts/test_weekly_metrics.py lines 1-67 (importlib + Workbook fixture + _make_pick_history_wb)
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-PATTERNS.md "modify sync_slip_bankroll" + "if not dry_run: guard" shared pattern + "importlib + Workbook fixture test pattern"
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-RESEARCH.md Pattern 2 + Pitfall 3 (dry_run guard)
  </read_first>
  <action>
    Two parts.
    (a) Source fix: in sports_system_runner.py sync_slip_bankroll, inside the existing `if not dry_run:` block (lines 5251-5294), add a single line `refresh_prop_accuracy(wb)   # D-06: keep Prop Accuracy live on daily grade path` immediately BEFORE `save_workbook_atomic(wb, master)` at line 5293 — exactly mirroring rebuild_slip_bankroll:5681. It MUST be inside `if not dry_run:` (Pitfall 3: dry-run is used by tests and must not mutate the wb). No second workbook load, no lock change (sync_slip_bankroll already holds the master_pnl cooperative lock). Do NOT touch refresh_prop_accuracy itself, weekly_metrics's read-only contract, gate logic, verdicts, or schema.
    (b) Test (Wave 0): create scripts/test_sync_slip_bankroll.py using the importlib + Workbook fixture idiom from test_weekly_metrics.py:1-67. Class TestPropAccuracyRefresh(unittest.TestCase). Build an in-memory master_pnl-style Workbook with "Slip History" rows AND "Pick History" rows (so refresh_prop_accuracy has data to aggregate). Call runner.sync_slip_bankroll(date, dry_run=False, _wb_override=wb, _master_override=<tmp path or None>, _bankroll_override=<tmp path>) and assert the "Prop Accuracy" sheet exists and is populated (more than just the header row) after the call. Add a second test asserting that with dry_run=True the "Prop Accuracy" sheet is NOT populated/mutated (guards Pitfall 3). Use unittest, no pytest fixtures; runnable via both `python3 test_sync_slip_bankroll.py` and pytest. Coordinate fixture columns with PROP_ACCURACY_HEADERS / RESULT_HEADERS so refresh_prop_accuracy produces rows.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_sync_slip_bankroll.py::TestPropAccuracyRefresh -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "refresh_prop_accuracy(wb)" scripts/sports_system_runner.py` returns 2 (rebuild_slip_bankroll:5681 + the new sync_slip_bankroll call)
    - The new call is inside sync_slip_bankroll's not-dry-run block and BEFORE its save: verify by reading the diff that `refresh_prop_accuracy(wb)` precedes `save_workbook_atomic(wb, master)` within sync_slip_bankroll
    - `grep -c "class TestPropAccuracyRefresh" scripts/test_sync_slip_bankroll.py` returns 1
    - `cd scripts && python3 -m pytest test_sync_slip_bankroll.py::TestPropAccuracyRefresh -x -q` exits 0 (non-dry-run populates Prop Accuracy; dry-run does not)
    - No edit to refresh_prop_accuracy body, no lock change, no gate/verdict/schema change
  </acceptance_criteria>
  <done>sync_slip_bankroll refreshes Prop Accuracy on the daily non-dry-run path mirroring rebuild_slip_bankroll:5681, the sheet is verified populated after a non-dry-run sync and untouched on dry-run, and nothing else changed.</done>
</task>

<task type="auto">
  <name>Task 2: WR-03 partial visibility — degraded digest + JSONL partial + repeated-partial alert + TestPartialVisibility/TestReadOnly</name>
  <files>scripts/sports_system_runner.py, scripts/test_weekly_metrics.py</files>
  <read_first>
    - scripts/sports_system_runner.py lines 394-431 (trailing_failure_streak — extend the failure set), 7271-7362 (weekly_metrics_task: status=partial at 7309/7331, digest assembly + send_telegram at 7336-7339, return at ~7362), 7426-7515 (main: _task_result=result at 7432, the except branches' `_obs03_streak = trailing_failure_streak(args.task)+1` + `🔁 REPEATED FAILURE` send_telegram pattern, finally JSONL write at 7503-7511 with "status": _run_status), 389 (REPEATED_FAILURE_THRESHOLD)
    - scripts/test_weekly_metrics.py lines 1-67 (importlib + Workbook fixture), 532-635 (existing weekly_metrics test idioms, send_telegram mocking, RUN_LOG_JSONL usage if present)
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-PATTERNS.md "modify weekly_metrics_task + main() finally + trailing_failure_streak" (Fix A/B/C exact edits) + "append_run_record call structure"
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-RESEARCH.md Pattern 4 (sub-parts A & B) + Pitfall 5 (double JSONL record) + single-transient-partial safety note
  </read_first>
  <action>
    Three source edits + tests. Honor D-07: visible-degraded EVERY partial run; repeated partials trip OBS-03; a single transient partial does NOT hard-fail (no non-zero exit) and does NOT fire the alert.
    Source A (degraded digest): in weekly_metrics_task, immediately before `result["telegram_sent"] = send_telegram(digest)` at line 7339, prepend a visible degraded marker to `digest` when `result.get("status") == "partial"` (e.g. "⚠️ WEEKLY METRICS DEGRADED (partial data)\n\n" + digest). Every partial run, no exception.
    Source B (JSONL propagation): in main()'s finally block JSONL write (lines 7503-7511), change `"status": _run_status` to propagate the task's own status when the task returned a result without raising — i.e. `(_task_result.get("status", "ok") if _task_result is not None else _run_status)`. This writes ONE record with status="partial" for a partial run (Pitfall 5: do NOT also append a record inside weekly_metrics_task — single writer only).
    Source C (streak counts partial): in trailing_failure_streak, extend the failure set at line 424 from {"error","timeout"} to {"error","timeout","partial"}.
    Source D (fire the repeated-partial alert — PLANNER-FOUND, beyond RESEARCH): the OBS-03 `🔁 REPEATED FAILURE` alert currently only fires in main()'s except branches; a partial returns normally and never reaches them, so Sources B+C alone make the streak countable but never raise the alert. On the success-return path in main(), AFTER `_task_result = result` (line 7432) and before/around dispatch_alerts, when `result.get("status") == "partial"` compute `_obs03_streak = trailing_failure_streak(args.task) + 1` (the +1 accounts for the current run, whose JSONL record is written later in finally — same +1 idiom as the except branches) and, if `_obs03_streak >= REPEATED_FAILURE_THRESHOLD`, `send_telegram("🔁 REPEATED FAILURE: {task} ... partial ...")`. Keep the normal exit code 0 (return 0) — do NOT hard-fail on a single transient partial (D-07). With one prior "ok"/no prior partial, streak=1 < 2 → no alert.
    Tests (Wave 0) in scripts/test_weekly_metrics.py, importlib + Workbook fixture idiom:
    - class TestPartialVisibility(unittest.TestCase): (1) test_degraded_digest — mock send_telegram to capture the digest, force weekly_metrics_task to partial (e.g. monkeypatch the metrics-report aggregation or calibration recompute to raise), assert the captured digest contains "DEGRADED"/"degraded". (2) test_streak_counted — write two status=partial records for task "weekly_metrics" to a temp run_log.jsonl (patch RUN_LOG_JSONL), assert runner.trailing_failure_streak("weekly_metrics") == 2. (3) test_single_partial_no_hard_fail — write one status=partial record, assert trailing_failure_streak == 1 and 1 < runner.REPEATED_FAILURE_THRESHOLD.
    - class TestReadOnly(unittest.TestCase): structural assertion that weekly_metrics_task does not write master_pnl.xlsx — e.g. run it with send_telegram/obsidian mocked and assert save_workbook_atomic is never called with the master_pnl path (mock/spy save_workbook_atomic), or assert task_workbook_paths("weekly_metrics") returns the master_pnl path for the lock but no write occurs. Use unittest, no pytest fixtures; runnable via both entry points.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_weekly_metrics.py::TestPartialVisibility test_weekly_metrics.py::TestReadOnly test_weekly_metrics.py::TestSigmaInjection test_weekly_metrics.py::TestIntegrityNoVerdictChange test_weekly_metrics.py::TestIntegrityGateOutput -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Degraded marker: `grep -ic "degraded" scripts/sports_system_runner.py` >= 1 and the marker is applied only when status=="partial" before send_telegram (verify in diff)
    - trailing_failure_streak counts partial: `grep -c '"error", "timeout", "partial"' scripts/sports_system_runner.py` >= 1 (or equivalent set membership including "partial")
    - JSONL propagation: the finally block uses `_task_result.get("status"` for the record status (verify in diff); no append_run_record added inside weekly_metrics_task (Pitfall 5) — `grep -c "append_run_record" scripts/sports_system_runner.py` is unchanged from before this task
    - Repeated-partial alert wired on the success-return path: a second `trailing_failure_streak(args.task) + 1` + `🔁 REPEATED FAILURE` send_telegram appears outside the except branches (verify in diff); single partial (streak 1) does not fire and the task returns 0
    - `cd scripts && python3 -m pytest test_weekly_metrics.py::TestPartialVisibility test_weekly_metrics.py::TestReadOnly -x -q` exits 0
    - METRICS-03 / D-13 intact: `cd scripts && python3 -m pytest test_weekly_metrics.py::TestSigmaInjection test_weekly_metrics.py::TestIntegrityNoVerdictChange test_weekly_metrics.py::TestIntegrityGateOutput -x -q` exits 0
    - No gate-logic / verdict / schema change; weekly_metrics still read-only on master_pnl.xlsx
  </acceptance_criteria>
  <done>A weekly_metrics partial yields a visibly degraded Telegram digest every run, propagates status=partial to a single run_log.jsonl record, is counted by trailing_failure_streak, fires the OBS-03 repeated-failure alert only on 2+ consecutive partials, never hard-fails on a single transient partial, and stays read-only on master_pnl.xlsx — all verified by TestPartialVisibility + TestReadOnly with integrity tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite + baseline confirmation across all four fixes</name>
  <files>scripts/sports_system_runner.py</files>
  <read_first>
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-VALIDATION.md (Sampling Rate, Known baseline "2 failed, 202+ passed", Per-Task Verification Map)
    - $HOME/.claude/projects/-Users-akashkalita-sports-picks/memory/pre-existing-projection-test-failures.md (clean baseline = "2 failed, 202 passed", failures live in test_generate_projections.py)
    - $HOME/.claude/projects/-Users-akashkalita-sports-picks/memory/test-suite-is-slow.md (full pytest ~34 min — run once, at the gate)
    - 04.1-RESEARCH.md § Validation Architecture (full suite command)
  </read_first>
  <action>
    Phase gate verification only — no production-code change in this task (the `files_modified` entry is nominal; do not edit source here). Confirm Plans 01, 02, and this plan's edits are all in place, then run the full test suite from scripts/ and confirm it holds the known clean baseline. The ONLY acceptable failures are the 2 pre-existing failures in test_generate_projections.py (per MEMORY: "2 failed, 202+ passed"). Any NEW failure is a regression and must be fixed in the owning plan/task before the gate passes — specifically re-confirm: (i) BANKROLL-02 forward staking tests (test_build_slips.py::TestForwardStaking, test_stake_sizing.py), (ii) calibration dedup + integrity (test_weekly_metrics.py::TestSigmaInjection / TestIntegrityNoVerdictChange / TestIntegrityGateOutput — METRICS-03/D-13), (iii) D-06 prop-accuracy (test_sync_slip_bankroll.py::TestPropAccuracyRefresh), (iv) D-07 WR-03 (test_weekly_metrics.py::TestPartialVisibility / TestReadOnly), (v) grade tests untouched (test_grade_slips_aggregate.py, test_grade_slips_backfill.py — D-05). Record the exact passed/failed counts and the names of the 2 expected failures in the SUMMARY. Note any task that approaches the 660s cron budget (none expected — all edits are negligible additions).
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Full suite result is at the baseline or better: exactly the 2 known pre-existing failures in test_generate_projections.py, and 0 NEW failures (METRICS-03 integrity intact)
    - The phase-specific suites are all green: `cd scripts && python3 -m pytest test_build_slips.py test_stake_sizing.py test_sync_slip_bankroll.py test_weekly_metrics.py test_grade_slips_aggregate.py test_grade_slips_backfill.py -q` exits 0
    - SUMMARY records the exact "N passed, 2 failed" line and names the 2 expected failures
    - No new production-code change introduced by this task (verify `git diff` for this task touches only the SUMMARY / no source under scripts/ beyond Tasks 1-2 of this plan)
  </acceptance_criteria>
  <done>The full suite holds the clean "2 failed, 202+ passed" baseline with zero new failures across all four v2.0 audit fixes; the 2 expected failures are named in the SUMMARY and METRICS-03 integrity is confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_log.jsonl -> OBS-03 health signal | a status=partial record influences whether the 🔁 REPEATED FAILURE alert fires |
| in-memory wb -> master_pnl.xlsx | refresh_prop_accuracy rewrites the Prop Accuracy sheet under the existing cooperative write lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-06 | Denial of Service | a single transient weekly_metrics partial falsely trips OBS-03 / hard-fails the task | mitigate | REPEATED_FAILURE_THRESHOLD default 2 (single partial -> streak 1 < 2, no alert); the partial path returns 0 (no hard-fail); single JSONL writer in finally (Pitfall 5) prevents double-counting that would push a single partial to streak 2 |
| T-04.1-07 | Tampering | refresh_prop_accuracy mutates the workbook on a dry-run/test path | mitigate | call placed strictly inside `if not dry_run:` (Pitfall 3), mirroring rebuild_slip_bankroll:5681; dry-run test asserts no mutation |
| T-04.1-08 | Repudiation | partial run reads green and a degraded cycle goes unnoticed | mitigate | every partial run prepends a visible "DEGRADED" marker to the Telegram digest (Source A) and writes status=partial to run_log.jsonl (Source B) — the cycle is always visible |
| T-04.1-09 | Tampering | weekly_metrics acquires a write lock / writes master_pnl.xlsx | accept->verify | weekly_metrics stays read-only by contract (only writes calibration.json); TestReadOnly asserts no master_pnl write — no change to its write surface |
| T-04.1-SC | Tampering | npm/pip/cargo installs | accept | no packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); all touched code is existing in-repo |

ASVS V2/V3/V4/V6 are N/A (no auth, session, access-control, or crypto surface). V5 input validation: trailing_failure_streak already swallows malformed JSONL lines and returns 0 on any error; refresh_prop_accuracy operates on the already-validated in-memory workbook.
</threat_model>

<verification>
- `cd scripts && python3 -m pytest test_sync_slip_bankroll.py::TestPropAccuracyRefresh test_weekly_metrics.py::TestPartialVisibility test_weekly_metrics.py::TestReadOnly -x -q` exits 0
- `cd scripts && python3 -m pytest test_weekly_metrics.py::TestSigmaInjection test_weekly_metrics.py::TestIntegrityNoVerdictChange test_weekly_metrics.py::TestIntegrityGateOutput -x -q` exits 0 (METRICS-03 / D-13)
- Full suite (Task 3) holds the "2 failed, 202+ passed" baseline with no new failures
- refresh_prop_accuracy appears exactly twice (rebuild path + new daily path), both inside not-dry-run blocks before save
- weekly_metrics remains read-only on master_pnl.xlsx; single transient partial returns 0 and fires no alert; two consecutive partials fire OBS-03
- All edits are negligible additions; every task stays well under the 660s cron budget
</verification>

<success_criteria>
- D-06: Prop Accuracy is fresh after a daily grade run; weekly_metrics stays read-only on master_pnl.xlsx
- D-07: a partial produces a visibly degraded digest; two consecutive partials trip OBS-03; a single transient partial does not hard-fail or alert
- All four v2.0 audit fixes (BANKROLL-02 forward staking, D-06 refresh, D-08 dedup, D-07 WR-03) land with no gate-logic / verdict / schema change
- Full suite holds the clean 2-failed / 202+-passed baseline (METRICS-03 integrity intact); every task under 660s
</success_criteria>

<output>
Create `.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-03-SUMMARY.md` when done
</output>
