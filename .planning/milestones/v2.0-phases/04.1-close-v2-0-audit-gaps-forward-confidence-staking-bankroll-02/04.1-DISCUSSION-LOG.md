# Phase 04.1: Close v2.0 Audit Gaps — Functional Fixes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-23
**Phase:** 04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02
**Areas discussed:** Forward-staking go-live, Prop-accuracy freshness
**Areas taken at Claude's discretion:** WR-03 dead-loop visibility, Calibration dedup ownership

---

## Scope Gate (pre-discussion, from plan-phase hand-off)

| Option | Description | Selected |
|--------|-------------|----------|
| Run discuss-phase first | Lock decisions before planning (real-money sensitivity, TBD reqs) | ✓ |
| Continue without context | Plan straight from the audit doc | |

| Option | Description | Selected |
|--------|-------------|----------|
| Keep functional fixes only | 4 functional fixes; RESULTS-07/SLIPS-03 → /gsd:verify-work | ✓ |
| Include everything in 04.1 | Also drive RESULTS-07/SLIPS-03 to closure here | |
| Verification debt only | Defer functional fixes | |

**User's choice:** Functional fixes only; verification debt routed to verify-work.

---

## Forward-staking go-live

### Gating
| Option | Description | Selected |
|--------|-------------|----------|
| On by default, no flag | Matches P4 D-09; stake bounded ≤2.5%/slip; simplest | ✓ |
| Env flag, default ON (kill-switch) | Live immediately but one env line reverts to flat-1.0 | |
| Env flag, default OFF | Opt-in; leaves BANKROLL-02 bug live until flipped | |

**User's choice:** On by default, no flag → **D-01**.

### Bankroll snapshot source
| Option | Description | Selected |
|--------|-------------|----------|
| Live bankroll.json, accept lag | Size off current_bankroll at build time; rebuild authoritative | ✓ |
| Derive start-of-day from Slip History | Robust to drift; duplicates rebuild logic; costs daily path | |
| Fall back to flat 1.0 if snapshot missing | Safety sub-rule (combinable) | |

**User's choice:** Live bankroll.json, accept lag → **D-02 / D-03**.
**Notes:** Missing-snapshot fallback to flat 1.0 + log taken as a Claude's-discretion
money-safe default (**D-04**), consistent with the "never crash" task contract.

---

## Prop-accuracy freshness

| Option | Description | Selected |
|--------|-------------|----------|
| Daily grade path | Refresh where master_pnl is already loaded+saved under write lock; weekly_metrics stays read-only | ✓ |
| Weekly task self-refreshes | Minimal footprint but flips weekly_metrics to read-write; sheet stale between runs | |
| Both | Most robust but redundant + weekly takes the write-lock change | |

**User's choice:** Daily grade path → **D-06**.

---

## Claude's Discretion

- **WR-03 dead-loop visibility (D-07):** mark the Telegram digest "⚠ degraded" on
  any `status=partial`, and feed persistent partials into the OBS-03 failure-streak /
  health signal; do not hard-fail on a single transient partial.
- **Calibration dedup (D-08):** `calibration.py` canonical; `generate_projections.py`
  imports `load_calibration_factor` and deletes its local copy; behavior byte-identical;
  test asserts parity; preserves P4 D-13.
- Module/task/test naming and placement, subject to the locked contracts.

## Deferred Ideas

- RESULTS-07 / SLIPS-03 verification debt → `/gsd-verify-work 1` and `/gsd-verify-work 2`.
- IN-01..04 P4 code-review info items (triplicated INCEPTION_DATE, unclosed read-only
  workbooks, fragile short-row guard, unoriented OVER/UNDER MOP averaging) — non-blocking.
- Daily total-slip-exposure budget (none, per P3 D-08).
- Rolling-window / per-stat calibration granularity (deferred from P4).
