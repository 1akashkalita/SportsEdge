---
phase: 01-trustworthy-results
plan: 10
type: execute
wave: 3
depends_on: [01-9]
files_modified:
  - scripts/sports_system_runner.py
  - scripts/test_fantasy_score.py
autonomous: true
gap_closure: true
requirements: [RESULTS-02, RESULTS-07]
must_haves:
  truths:
    - "Hitter Fantasy Score and Pitcher Fantasy Score props derive an actual value from box-score components and grade over-style (WIN if actual>line, LOSS if <, PUSH if =)"
    - "The PrizePicks and Underdog MLB scoring tables are encoded exactly (SB/W/QS differ by platform)"
    - "Platform (PrizePicks vs Underdog) is recovered from the source prop/projection (Reasoning e.g. 'PrizePicks baseline'); rows are tagged generic 'DFS' in Results"
    - "When platform is genuinely unknown AND the two formulas would disagree on the grade (the row has a SB, W, or QS component) → abstain to MANUAL REVIEW; when they agree → grade"
    - "When a required component is genuinely unavailable from the box (e.g. an uncountable HBP/SB/W) → abstain rather than score a wrong total"
  artifacts:
    - path: "scripts/sports_system_runner.py"
      provides: "fantasy_score derivation (hitter/pitcher, both platforms) + platform disambiguation, wired into stat_value_for_prop"
      contains: "fantasy"
    - path: "scripts/test_fantasy_score.py"
      provides: "RED-first tests: exact PP/UD tables, over-style grading, platform recovery, disagreement-abstain, missing-component-abstain"
  key_links:
    - from: "stat_value_for_prop"
      to: "fantasy-score derivation"
      via: "Hitter/Pitcher Fantasy Score routed to the formula instead of NOT-DERIVABLE"
      pattern: "fantasy"
    - from: "platform recovery"
      to: "PrizePicks vs Underdog weights"
      via: "source prop Reasoning/Platform → SB/W/QS weight selection"
      pattern: "prizepicks|underdog"
---

<objective>
Encode the PrizePicks + Underdog MLB Fantasy-Score formulas into the grader so
Hitter/Pitcher Fantasy Score props derive an actual value from box-score components
and grade automatically (over-style), instead of falling to MANUAL REVIEW. This is
the 46-row June 8 residue class. Closes GAP 2.

Purpose: Remove the last large unencoded residue class so RESULTS-07's Fantasy-Score
rows grade in code with money-safety (abstain when platform is ambiguous AND the
formulas would disagree, or when a required component is unavailable).
Output: A fantasy-score derivation (hitter + pitcher, both platforms) with platform
disambiguation, wired into `stat_value_for_prop`, grading over-style.

EXACT SCORING TABLES (operator-supplied, LOCKED):
  HITTER (both platforms): Single 3.0, Double 5.0, Triple 8.0, HR 10.0, R 2.0,
    RBI 2.0, BB 2.0, HBP 2.0; Stolen Base = PrizePicks 5.0 / Underdog 4.0.
    (singles = hits − doubles − triples − home-runs)
  PITCHER (both platforms): Out 1.0 (= 3.0 per full IP), K 3.0, ER −3.0;
    Win = PrizePicks 6.0 / Underdog 5.0; Quality Start = PrizePicks 4.0 / Underdog 5.0
    (QS = ≥6 IP AND ≤3 ER).
  Grade direction = the system's existing over-rule (WIN if actual>line, LOSS if <,
  PUSH if =), consistent with every other prop row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trustworthy-results/01-UAT.md

<read_first>
- scripts/sports_system_runner.py:4318-4619 — `stat_value_for_prop` disposition table.
  - :4406 — "hitter fantasy score", "pitcher fantasy score" are CURRENTLY in `_NOT_DERIVABLE`.
    They must be REMOVED from that set and routed to the new derivation.
  - :4540-4566 — MLB DIRECT batting/pitching keys (hits, runs, rbis, homeruns, walks, strikeouts;
    pit hits/earnedruns/walks/strikeouts/pitches).
  - :4571-4607 — MLB DERIVED: Total Bases / Singles/Doubles/Triples from `bat["_hit_counts"]`
    (single/double/triple/home-run); Pitching Outs from `_innings_to_outs_grading(pit["fullinnings.partinnings"])`.
  - The available batting components from the ESPN summary box (confirmed against the fixture):
    hits, runs, rbis, homeruns, walks, strikeouts, atbats, AND `_hit_counts` (single/double/triple/home-run).
    HBP is NOT a summary batting label; SB is a play type ("stolen-base") not a box label.
  - Pitching components: earnedruns, strikeouts, fullinnings.partinnings (→ outs). Win-decision
    and Quality-Start are derivable: QS = outs≥18 AND earnedruns≤3; Win is NOT a box label.
- scripts/sports_system_runner.py:5913-5972 — grade_prop / prop loop. grade_prop applies the
  over-rule from the same `actual_val` that stat_value_for_prop returns — so routing fantasy
  score through stat_value_for_prop automatically inherits the correct grade direction.
- scripts/sports_system_runner.py PROPS_HEADERS (:284-290) — the source prop carries `Reasoning`
  and `Platform`. Platform intent ("PrizePicks baseline" / "Underdog ...") lives in Reasoning;
  Results rows are tagged generic "DFS" (result_record_from_source :4816).
- scripts/sports_system_runner.py:3536-3600 — `name_match` and `_canonical_name` (for player resolution).
- scripts/verify_results.py:60-134, :246-302 — markdown box parser captures `sb → stolen_bases`
  and pitcher IP; usable as the Layer-2 source for SB/W when the summary box lacks them.
- 01-3-SUMMARY.md — how _hit_counts and the disposition table are structured; the over-rule.
- 01-UAT.md Test 4 — the 46 rows resolved with exactly these weights (27 WIN / 19 LOSS);
  one cross-source ER disagreement (Arrighetti 2 vs 3) was WIN either way.
</read_first>

<interfaces>
Proposed contract (implementer chooses helper names/placement; semantics are LOCKED):
```python
# Platform recovery from the source prop row — money-safe:
def _prop_platform(row: dict) -> str:   # "prizepicks" | "underdog" | "unknown"
    # Inspect row["Platform"] then row["Reasoning"] (e.g. "PrizePicks baseline").
    # Return "unknown" when neither names a platform unambiguously.

# Component-based fantasy score with per-platform weights:
def _hitter_fantasy_score(bat: dict, platform: str) -> tuple[float|None, bool]:
    # Returns (score, used_divergent_component). used_divergent=True iff an SB
    # contributed (SB weight differs PP5/UD4). Abstain (None) if a required
    # component is genuinely unavailable.
def _pitcher_fantasy_score(pit: dict, platform: str) -> tuple[float|None, bool]:
    # Win (PP6/UD5) and QS (PP4/UD5) are the divergent components.
    # QS derived: outs>=18 AND earnedruns<=3.

# Money-safe disambiguation rule (LOCKED):
#   if platform == "unknown":
#       compute score under BOTH PP and UD weights;
#       if both weightings yield the SAME grade vs the line (WIN/LOSS/PUSH agree)
#           -> grade (any weighting; they agree)
#       else (a SB/W/QS component makes them disagree)
#           -> ABSTAIN (None, "manual", 0.0)  # stays MANUAL REVIEW
#   else:
#       grade under the recovered platform's weights.
```
Provenance: derived from the summary box → ("api", 0.8). If SB/W must come from the
Layer-2 scrape (play-by-play), provenance is ("scraped", 0.5). If components are
unavailable → (None, "manual", 0.0) → MANUAL REVIEW.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — encode PP/UD tables, over-style grading, platform recovery, disagreement-abstain, missing-component-abstain</name>
  <files>scripts/test_fantasy_score.py</files>
  <behavior>
    - Hitter PP: 1×single(3) + 1×double(5) + 1×HR(10) + 1R(2) + 2RBI(4) + 1BB(2) = 26.0
      (using _hit_counts + batting labels). Same components UD = 26.0 (hitter table identical
      except SB). With 1 SB added: PP = 26+5 = 31.0; UD = 26+4 = 30.0.
    - Pitcher PP: 18 outs(18) + 7K(21) + 2ER(−6) = 33.0; QS (outs≥18 & ER≤3) adds PP4/UD5;
      Win adds PP6/UD5. A QS+Win pitcher: PP = 33+4+6 = 43.0; UD = 33+5+5 = 43.0 (coincidental
      — pick weights that DIFFER for the disagreement test, e.g. a QS-only line near the boundary).
    - Over-style grading via grade_prop: actual 26.0 vs line 24.5 Over → WIN; vs 27.5 → LOSS;
      vs 26.0 → PUSH.
    - Platform recovery: row Reasoning "PrizePicks baseline" → "prizepicks"; "Underdog value"
      → "underdog"; empty/ambiguous → "unknown".
    - Disagreement-abstain: a hitter row WITH a stolen base, platform "unknown", and a line
      sitting BETWEEN the PP score and UD score (e.g. line 30.5 with PP=31 WIN but UD=30 LOSS)
      → stat_value_for_prop abstains (None,"manual",0.0) → MANUAL REVIEW. Same idea for a
      pitcher Win/QS row near the boundary.
    - Agreement-grade: same SB row but line 20.5 (both PP=31 and UD=30 are WIN) → grades WIN.
    - Missing-component-abstain: a hitter Fantasy Score with no _hit_counts and no hits data
      → abstain (cannot compute) → MANUAL REVIEW.
    - Use the espn_summary MLB fixture (or a hand-built batting/pitching sub-dict) so the test
      is offline and deterministic.
  </behavior>
  <action>
    Create `scripts/test_fantasy_score.py` (stdlib unittest, importlib-loaded). Drive
    `stat_value_for_prop` directly with synthesized player_stats sub-dicts AND via grade_prop
    for the over-style assertions. Reuse fixture-loading patterns from test_stat_value_for_prop.py.
    This test MUST FAIL initially (Fantasy Score is currently NOT-DERIVABLE → None). Run from
    scripts/ with python3.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_fantasy_score.py -x 2>&1 | tail -20  # MUST FAIL (RED)</automated>
  </verify>
  <done>test_fantasy_score.py exists and fails, pinning the exact PP/UD weights, over-style grading, platform recovery, and both abstain rules</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — fantasy-score derivation + platform disambiguation wired into stat_value_for_prop</name>
  <files>scripts/sports_system_runner.py</files>
  <action>
    1. Remove "hitter fantasy score" and "pitcher fantasy score" from `_NOT_DERIVABLE` (:4406).
       Leave NBA "fantasy score"/"fantasy points" NOT-DERIVABLE (out of scope — MLB only here).
    2. Add `_hitter_fantasy_score(bat, platform)` and `_pitcher_fantasy_score(pit, platform)`
       implementing the LOCKED tables:
         HITTER: single·3 + double·5 + triple·8 + HR·10 + R·2 + RBI·2 + BB·2 + HBP·2 + SB·(PP5/UD4).
           Hit-type counts from bat["_hit_counts"] (single/double/triple/home-run); singles =
           hits − doubles − triples − HR if _hit_counts lacks "single" but hits/2B/3B/HR exist.
           R = bat["runs"], RBI = bat["rbis"], BB = bat["walks"]. HBP and SB are NOT in the summary
           box: if the component is present from the Layer-2 scrape (verify_results sb/hbp), use it;
           otherwise treat the count as 0 ONLY when the box is fully known to lack it — if it cannot
           be confirmed 0 vs unknown, ABSTAIN (return None). Return (score, used_divergent) where
           used_divergent is True iff a non-zero SB contributed.
         PITCHER: outs·1 (outs from _innings_to_outs_grading(pit["fullinnings.partinnings"])) +
           K·3 + ER·(−3) + Win·(PP6/UD5) + QS·(PP4/UD5). QS = (outs≥18 AND earnedruns≤3).
           Win is NOT a box label: use the Layer-2 scrape decision if available, else treat as
           0 only if confirmable, else ABSTAIN. used_divergent is True iff Win or QS contributes.
    3. Add `_prop_platform(row)` → "prizepicks"|"underdog"|"unknown" (inspect Platform then
       Reasoning; "prizepicks"/"pp"/"prize picks" → prizepicks; "underdog"/"ud" → underdog).
       NOTE: stat_value_for_prop currently takes (player_stats, player, stat). To recover platform
       it needs the source row. Thread the row through: either add an optional `row=None` param to
       stat_value_for_prop and pass it from grade_prop (:4645), OR resolve platform in grade_prop
       and pass it down. Preserve all existing call sites (default keeps current behavior for
       non-fantasy stats). Do NOT change non-fantasy grading paths.
    4. In stat_value_for_prop, route s in ("hitter fantasy score","pitcher fantasy score"):
         platform = _prop_platform(row) if row else "unknown"
         if platform == "unknown":
             compute score under BOTH PP and UD; if the GRADE vs line agrees under both →
             grade (return one score, "api"/"scraped" conf); else → return (None,"manual",0.0).
             [Grade comparison needs the line: do the agreement check in grade_prop where the line
              is known, OR return a small structure stat_value can compare. Cleanest: compute both
              scores in the helper; in grade_prop, if platform unknown AND used_divergent, compare
              both grades vs line and abstain on disagreement. Keep the money-safe rule from the
              interface exactly.]
         else: return the platform's score.
       Provenance: ("api", 0.8) when all components come from the summary box; ("scraped", 0.5)
       if an SB/W/QS component required the Layer-2 scrape; (None,"manual",0.0) on abstain.
    5. Keep additive schema; daily run < 660s (derivation is arithmetic; any SB/W scrape reuses
       the 01-5/01-9 cache + budget). Add inline comments citing GAP 2 / RESULTS-02 / the LOCKED weights.
    Run from scripts/ with python3.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_fantasy_score.py -x 2>&1 | tail -15  # MUST PASS (GREEN)</automated>
    <automated>cd scripts && python3 -m pytest test_stat_value_for_prop.py test_provenance_plumbing.py -x 2>&1 | tail -8  # 01-3 disposition/provenance not regressed</automated>
    <automated>cd scripts && grep -n "hitter fantasy score\|pitcher fantasy score" sports_system_runner.py | head</automated>
  </verify>
  <done>Hitter/Pitcher Fantasy Score derive from box components, grade over-style, recover platform from Reasoning, abstain on PP/UD disagreement (SB/W/QS) and on unavailable components; 01-3 tests still pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source prop Reasoning → platform weights | platform intent is free-text; mis-read picks the wrong SB/W/QS weight |
| box components → fantasy total | a missing/uncountable component (HBP/SB/W) would silently undercount the score |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-G2-01 | Spoofing | platform recovery | mitigate | "unknown" when ambiguous; abstain when PP/UD grades disagree (SB/W/QS) — pinned by disagreement test |
| T-01-G2-02 | Tampering | fantasy total from partial box | mitigate | Abstain when a required component cannot be confirmed (vs confirmed 0); pinned by missing-component test |
| T-01-G2-03 | Elevation | wrong-platform grade on real money | mitigate | When platform known → exact table; when unknown but grades agree → safe to grade; else MANUAL REVIEW |
| T-01-G2-SC | Tampering | firecrawl-cli@1.19.2 (existing, for SB/W) | accept | No new package; SB/W reuse the pinned 01-5 scrape lane (T-01-SC) when needed |
</threat_model>

<verification>
- `python3 -m pytest test_fantasy_score.py -x` passes (exact PP/UD weights, over-style grades, both abstain rules).
- `python3 -m pytest test_stat_value_for_prop.py test_provenance_plumbing.py -x` still pass (01-3 not regressed).
- Fantasy Score is no longer in `_NOT_DERIVABLE` for MLB; NBA fantasy stays NOT-DERIVABLE.
</verification>

<success_criteria>
- Hitter/Pitcher Fantasy Score props grade automatically, over-style, with exact PP/UD weights.
- Platform recovered from the source prop; ambiguous + divergent → MANUAL REVIEW (abstain).
- Unavailable component → MANUAL REVIEW (no wrong total).
- No regression to the 01-3 disposition table / provenance; daily run < 660s.
</success_criteria>

<output>
Create `.planning/phases/01-trustworthy-results/01-10-SUMMARY.md` when done
</output>
