---
phase: 04-dual-metrics-and-feedback
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - scripts/generate_projections.py
  - scripts/sports_system_runner.py
  - scripts/test_weekly_metrics.py
autonomous: true
requirements: [METRICS-01, METRICS-02, METRICS-03]
must_haves:
  truths:
    - "D-01: the weekly report is delivered to BOTH surfaces — a compact Telegram digest (send_telegram) AND a persistent Obsidian note (via the weekly-recap scaffold / obsidian_sync), filling the previously-blank recap"
    - "D-02: a standalone weekly_metrics runner task is wired into TASK_TIMEOUTS, task_workbook_paths, and the run_task mapping mirroring grade_slips/rebuild_bankroll; the cron schedule entry lives in ~/.hermes outside the repo and the operator must add it (documented in the SUMMARY)"
    - "D-07: the per-sport calibration factor is applied at projection time via the sigma path in generate_projections.py — sigma_eff = estimate_sigma(...) * factor, factor > 1.0 widening sigma — with model_over_probability and evaluate_no_bet_gates left byte-for-byte unchanged"
    - "D-09: calibration auto-applies and is ON by default — generate_projections.py reads calibration.json with no feature flag; absence/corruption falls back to neutral 1.0"
    - "D-12: the weekly recompute cadence is weekly and aligned with the report — the report aggregation and the calibration recompute run inside the single weekly_metrics task"
    - "D-13: the feedback loop never touches graded verdicts, the Results/Pick History sheets, or evaluate_no_bet_gates — a verdict-snapshot test asserts no WIN/LOSS/PUSH/VOID changes and a gate-output test asserts evaluate_no_bet_gates output is unchanged regardless of calibration.json"
    - "METRICS-01: running the weekly_metrics task produces the week×sport ROI + hit-rate report on Telegram + Obsidian"
    - "METRICS-02: the weekly_metrics task recomputes and persists the per-sport sigma scaler from realized outcomes, and generate_projections.py consumes it"
    - "METRICS-03: an integrity test confirms the loop changes no graded verdict and leaves evaluate_no_bet_gates output unchanged (Design A verdict snapshot + Design C gate output)"
  artifacts:
    - path: "scripts/generate_projections.py"
      provides: "load_calibration_factor + sigma injection in build_projection"
      contains: "load_calibration_factor"
    - path: "scripts/sports_system_runner.py"
      provides: "weekly_metrics task wiring + weekly_metrics_task() function"
      contains: "weekly_metrics"
    - path: "scripts/test_weekly_metrics.py"
      provides: "TestSigmaInjection, TestIntegrityNoVerdictChange, TestIntegrityGateOutput"
      min_lines: 60
  key_links:
    - from: "scripts/generate_projections.py build_projection"
      to: "data/research/calibration.json"
      via: "load_calibration_factor(sport) read at projection time; sigma *= factor"
      pattern: "load_calibration_factor"
    - from: "scripts/sports_system_runner.py run_task"
      to: "weekly_metrics_task"
      via: "run_task mapping + TASK_TIMEOUTS + task_workbook_paths"
      pattern: "weekly_metrics"
    - from: "scripts/sports_system_runner.py weekly_metrics_task"
      to: "Telegram + Obsidian"
      via: "send_telegram(format_telegram_digest(...)) + obsidian_sync(fill_obsidian_recap_markdown(...))"
      pattern: "send_telegram"
---

<objective>
Wire the feedback loop end-to-end: inject the per-sport calibration factor into projection sigma (METRICS-02 / D-07 / D-09), add the standalone `weekly_metrics` runner task that recomputes calibration and delivers the report to Telegram + Obsidian (METRICS-01 / D-01 / D-02 / D-12), and prove the integrity guarantee with verdict-snapshot + gate-output tests (METRICS-03 / D-13).

Purpose: Close the loop from realized outcomes → calibration.json → future projections through the SAME unchanged gate gauntlet, observable and reversible, while a test structurally and behaviorally confirms no graded verdict or gate output changes.
Output: modified `generate_projections.py` (sigma injection), modified `sports_system_runner.py` (task wiring + `weekly_metrics_task`), integrity + sigma-injection test classes in `test_weekly_metrics.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dual-metrics-and-feedback/04-CONTEXT.md
@.planning/phases/04-dual-metrics-and-feedback/04-RESEARCH.md
@.planning/phases/04-dual-metrics-and-feedback/04-PATTERNS.md
@.planning/phases/04-dual-metrics-and-feedback/04-VALIDATION.md
@.planning/phases/04-dual-metrics-and-feedback/04-01-SUMMARY.md
@.planning/phases/04-dual-metrics-and-feedback/04-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — use directly. -->

From scripts/generate_projections.py (the injection point — confirmed):
  BASE = Path.home() / "sports_picks"; DATA = BASE / "data"   (lines 26-27; json imported at line 11)
  def build_projection(player, team, stat_name, pp_line, hit_rec, sport, pace_values, platform="DFS")  (line 380 — `sport` param ALREADY present)
  def estimate_sigma(stat, stat_name, sigma_floor=0.75) -> tuple[float, str]   (line 277)
  def model_over_probability(projection, line, sigma) -> float   (line 289 — MUST stay byte-for-byte unchanged)
  Current lines 393-394:
    sigma, sigma_source = estimate_sigma(stat, stat_name)
    over_prob = round(model_over_probability(projection, pp_line, sigma), 4)

From scripts/sports_system_runner.py:
  TASK_TIMEOUTS dict at line 120 (grade_slips:660 at 134, rebuild_bankroll:660 at 137)
  task_workbook_paths: `if task == "grade_slips": ...` at 7208; `if task == "rebuild_bankroll": return [PNL_DIR / "master_pnl.xlsx"]` at 7212
  run_task mapping: "grade_slips": lambda ... at 7276; "rebuild_bankroll": lambda: rebuild_slip_bankroll() at 7279
  helper functions _grade_slips_then_sync at 7232 (placement model for weekly_metrics_task; uses lazy __import__ + try/except non-crash)
  send_telegram(message, retries=2, backoff=5) -> bool  (line 428)
  obsidian_sync(payload) -> dict  (line 605); obsidian_create_weekly_recap(date=None, summary=None) -> Path  (line 1034 — guards `if path.exists(): return path`; bypass via obsidian_sync per RESEARCH Pitfall 5)
  evaluate_no_bet_gates(pick, suppressed_edges) -> tuple[bool, dict|None, list]  (the gate gauntlet — MUST stay byte-for-byte unchanged)
  RESULT_HEADERS (line 292); PNL_DIR; today_str(); log(); safe_print(); main() emits `JSON_RESULT=`

From scripts/calibration.py (Plan 01): compute_and_update_calibration(...), load_calibration_factor(sport)
From scripts/metrics_report.py (Plan 02): build_weekly_report, aggregate_slip_roi_by_week_sport,
  read_prop_hit_rate_by_week_sport, format_telegram_digest, fill_obsidian_recap_markdown

From scripts/test_weekly_metrics.py (Plan 01 scaffold): importlib runner bootstrap (runner.*), _make_pick_history_wb,
  stub classes TestSigmaInjection, TestIntegrityNoVerdictChange, TestIntegrityGateOutput (fill these here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sigma injection in generate_projections.py (D-07 / D-09) + TestSigmaInjection</name>
  <read_first>
    - scripts/generate_projections.py lines 1-32 (imports + BASE/DATA constants), 277-294 (estimate_sigma + model_over_probability — DO NOT change model_over_probability), 380-400 (build_projection body, the two-line injection site at 393-394)
    - .planning/phases/04-dual-metrics-and-feedback/04-PATTERNS.md "scripts/generate_projections.py (modified — sigma injection)" section (the exact two-line change + helper)
    - .planning/phases/04-dual-metrics-and-feedback/04-RESEARCH.md §6 (injection point rationale, anti-pattern: no import-time read)
    - scripts/test_generate_projections.py lines 15-16 (direct module import for the test)
  </read_first>
  <files>scripts/generate_projections.py, scripts/test_weekly_metrics.py</files>
  <action>
    Add a module-level `load_calibration_factor(sport: str) -> float` to generate_projections.py (after imports, before estimate_sigma) implementing the fail-safe read from `DATA / "research" / "calibration.json"` per PATTERNS: try/except → return 1.0 on any failure; read `cfg.get("factors", {}).get(sport.upper(), 1.0)`; clamp the returned value into [0.85, 1.20] before returning (input validation, defense in depth, matching calibration.py). Read at CALL time, never at import time (anti-pattern). In build_projection at lines 393-394, after `sigma, sigma_source = estimate_sigma(stat, stat_name)`, add `cal_factor = load_calibration_factor(sport)` and `if cal_factor != 1.0: sigma = sigma * cal_factor; sigma_source = f"{sigma_source} × cal={cal_factor:.4f}"` then call model_over_probability with the adjusted sigma. Do NOT modify model_over_probability or estimate_sigma bodies. `json` and `DATA` already exist — add no new imports, no new dependency. Fill TestSigmaInjection in test_weekly_metrics.py: `import generate_projections as gp`, monkeypatch gp.DATA to a tmpdir, write a calibration.json with MLB factor 1.10, assert load_calibration_factor("MLB") == 1.10 and load_calibration_factor("NBA") == 1.0; assert a corrupt/absent file yields 1.0; assert a 5.0 file is clamped to 1.20. Optionally assert that with factor 1.10 the resulting over_prob for a fixed projection/line is pulled toward 0.5 versus factor 1.0 (sigma widened).
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest "test_weekly_metrics.py::TestSigmaInjection" -x -q && python3 -c "import pathlib; src=pathlib.Path('generate_projections.py').read_text(); assert 'def model_over_probability' in src and 'load_calibration_factor' in src; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - generate_projections.py defines load_calibration_factor and calls it inside build_projection between estimate_sigma and model_over_probability.
    - model_over_probability and estimate_sigma function bodies are unchanged (no new logic inside them).
    - load_calibration_factor returns 1.0 for missing/corrupt file, the stored factor for a valid sport, and a clamped 1.20 for a 5.0 file.
    - No new third-party import added to generate_projections.py.
    - The generate_projections test baseline gains no NEW failures (still the 2 known pre-existing failures).
  </acceptance_criteria>
  <done>Calibration factor applied multiplicatively to sigma at projection time (D-07/D-09), fail-safe + clamped; model_over_probability/gates untouched; TestSigmaInjection green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire weekly_metrics task + weekly_metrics_task() with Telegram + Obsidian delivery (D-01/D-02/D-12)</name>
  <read_first>
    - scripts/sports_system_runner.py lines 120-138 (TASK_TIMEOUTS), 7192-7214 (task_workbook_paths grade_slips/rebuild_bankroll branches), 7232-7256 (_grade_slips_then_sync helper shape + lazy __import__ + try/except non-crash pattern), 7261-7283 (run_task mapping), 428-463 (send_telegram), 605-640 + 1034-1098 (obsidian_sync + obsidian_create_weekly_recap scaffold)
    - .planning/phases/04-dual-metrics-and-feedback/04-PATTERNS.md "scripts/sports_system_runner.py (modified — task wiring)" section (exact TASK_TIMEOUTS / task_workbook_paths / run_task edits + weekly_metrics_task placement + JSON_RESULT contract)
    - .planning/phases/04-dual-metrics-and-feedback/04-RESEARCH.md §4 (one-task recommendation, lock behavior, idempotency, 660s budget) + Pitfall 5 (obsidian path.exists guard bypass)
    - scripts/calibration.py + scripts/metrics_report.py (Plan 01/02 interfaces consumed here)
  </read_first>
  <files>scripts/sports_system_runner.py</files>
  <action>
    Add `"weekly_metrics": 660` to TASK_TIMEOUTS (mirroring grade_slips/rebuild_bankroll). Add `if task == "weekly_metrics": return [PNL_DIR / "master_pnl.xlsx"]` to task_workbook_paths immediately after the rebuild_bankroll branch (read-only data, but hold the cooperative lock so it cannot race grade_slips/check_results). Add `"weekly_metrics": lambda: weekly_metrics_task()` to the run_task mapping. Define `weekly_metrics_task() -> dict[str, Any]` near _grade_slips_then_sync (~line 7232): lazy-import calibration and metrics_report inside the function (mirroring `_grade_slips = __import__("grade_slips")`) to avoid import-time coupling. Build the report: roi_agg = metrics_report.aggregate_slip_roi_by_week_sport(); prop_rates = metrics_report.read_prop_hit_rate_by_week_sport(); report = metrics_report.build_weekly_report(roi_agg, prop_rates). Recompute calibration: cal_summary = calibration.compute_and_update_calibration() wrapped in try/except so a calibration failure never blocks the report. Compose calibration_note from cal_summary (per-sport old→new factor, n, reason — the D-13 observable log). Deliver to BOTH surfaces (D-01): send_telegram(metrics_report.format_telegram_digest(report)) wrapped in try/except (degrades to no-op per existing pattern); deliver the Obsidian note by calling obsidian_sync directly with the filled markdown from metrics_report.fill_obsidian_recap_markdown(report, calibration_note) (bypassing the obsidian_create_weekly_recap `if path.exists()` guard so re-runs overwrite — RESEARCH Pitfall 5), wrapped in try/except. Return a dict {status, weeks, sports, calibration: cal_summary, telegram_sent, obsidian_sent}; main() emits the JSON_RESULT= line — the function only log()s and returns; it must never raise (defensive SKIP on any missing data). Add a code comment AND a SUMMARY note that the Monday-morning cron schedule entry must be added by the operator in ~/.hermes (outside the repo, D-02).
  </action>
  <verify>
    <automated>cd scripts && python3 -c "import importlib.util,pathlib; spec=importlib.util.spec_from_file_location('r', pathlib.Path('sports_system_runner.py')); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.TASK_TIMEOUTS.get('weekly_metrics')==660; assert callable(getattr(m,'weekly_metrics_task',None)); assert m.task_workbook_paths('weekly_metrics'); print('wired')"</automated>
  </verify>
  <acceptance_criteria>
    - TASK_TIMEOUTS["weekly_metrics"] == 660; task_workbook_paths("weekly_metrics") returns [master_pnl.xlsx]; run_task mapping contains "weekly_metrics".
    - weekly_metrics_task is callable, returns a dict (never raises), and both the Telegram and Obsidian delivery calls are wrapped in try/except (no-op on failure).
    - The Obsidian delivery bypasses the obsidian_create_weekly_recap path.exists guard (re-runs overwrite the note).
    - A code comment documents that the operator must add the Monday cron entry in ~/.hermes.
  </acceptance_criteria>
  <done>weekly_metrics is a wired standalone task (D-02) that aggregates the report, recomputes calibration (D-12), and delivers to Telegram + Obsidian (D-01); the cron-entry caveat is documented for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: METRICS-03 integrity tests — verdict snapshot (Design A) + gate output unchanged (Design C)</name>
  <read_first>
    - .planning/phases/04-dual-metrics-and-feedback/04-RESEARCH.md "Validation Architecture" Design A + Design C (verdict snapshot before/after; gate output unchanged) and Pitfall 1 (gate on n_with_mop)
    - .planning/phases/04-dual-metrics-and-feedback/04-VALIDATION.md "Detailed Test Designs for METRICS-03"
    - scripts/test_weekly_metrics.py (Plan 01 scaffold: importlib runner bootstrap, _make_pick_history_wb, stub TestIntegrityNoVerdictChange / TestIntegrityGateOutput)
    - scripts/test_dynamic_gate8.py (an existing evaluate_no_bet_gates invocation pattern with a populated pick dict)
    - scripts/calibration.py compute_and_update_calibration signature (accepts _wb_override / path overrides)
  </read_first>
  <files>scripts/test_weekly_metrics.py</files>
  <behavior>
    - TestIntegrityNoVerdictChange (Design A): build an in-memory master workbook with ≥30 terminal MLB PROP rows (known WIN/LOSS + MOP) plus a few PUSH/VOID rows; snapshot every Pick History Result value; run calibration.compute_and_update_calibration with the workbook override and a tmp calibration.json; snapshot Result values again; assert the two snapshots are identical (no verdict mutated).
    - TestIntegrityGateOutput (Design C): construct a fully-populated prop pick dict; call runner.evaluate_no_bet_gates(pick, {}) once, then write a calibration.json with a non-neutral factor (e.g. MLB 1.15) and call evaluate_no_bet_gates(pick, {}) again; assert identical (ok, skipped, passed) — the gate reads the stored probability, never calibration.json, so output is invariant.
    - Both tests confirm the loop touches neither graded verdicts nor gate logic (D-13 / METRICS-03), complementing the Plan 01 AST import check (Design B).
  </behavior>
  <action>
    Fill TestIntegrityNoVerdictChange: use _make_pick_history_wb to seed a master workbook with ≥30 terminal MLB PROP rows (WIN/LOSS) with non-null Model Over Probability plus 2-3 PUSH and VOID rows; capture verdicts via a helper that reads the Result column by RESULT_HEADERS index for every Pick History data row; call calibration.compute_and_update_calibration(_wb_override=wb, path=Path(tmp/"cal.json")); re-capture verdicts; assertEqual(before, after). Also assert the resulting calibration.json factors changed for MLB (proving the loop actually ran and still left verdicts intact). Fill TestIntegrityGateOutput: build a populated prop pick dict (kind="prop", edge, model_over_probability≥0.58, injury_status="ACTIVE"/cleared, platform, line_timing pregame, sample size, plus any other required fields per test_dynamic_gate8.py); run runner.evaluate_no_bet_gates(pick, {}); write calibration.json with MLB 1.15 to the path generate_projections reads (or simply assert determinism since gate ignores the file); run evaluate_no_bet_gates(pick, {}) again; assertEqual the (ok, skipped, passed) tuple across runs. Do not modify evaluate_no_bet_gates. Use the importlib runner bootstrap from the scaffold (runner.evaluate_no_bet_gates / runner.RESULT_HEADERS).
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest "test_weekly_metrics.py::TestIntegrityNoVerdictChange" "test_weekly_metrics.py::TestIntegrityGateOutput" "test_weekly_metrics.py::TestIntegrityNoGateImport" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - TestIntegrityNoVerdictChange: verdict snapshot before == after running compute_and_update_calibration, AND the MLB factor in the written calibration.json differs from 1.0 (loop ran and changed nothing graded).
    - TestIntegrityGateOutput: evaluate_no_bet_gates returns an identical (ok, skipped, passed) tuple regardless of calibration.json content.
    - No test modifies or imports evaluate_no_bet_gates' internals; the gate is invoked, not patched.
    - All three integrity classes (A + C here, B from Plan 01) are green together.
  </acceptance_criteria>
  <done>METRICS-03 is proven: the loop changes no graded verdict (Design A) and leaves gate output unchanged (Design C), complementing the AST import check (Design B); D-13 guarantee confirmed by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → generate_projections.py | calibration.json is untrusted input read at projection time; a corrupt/out-of-range file must not crash or skew projections |
| outcomes → selection | the ONLY coupling is calibration.json → sigma; the gate gauntlet and graded sheets are downstream-isolated by design (D-13) |
| runner task → external (Telegram/Obsidian) | side-effecting delivery that must degrade to no-op, never crash the task |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | generate_projections.load_calibration_factor reading calibration.json | mitigate | try/except → neutral 1.0 on any failure (missing/corrupt/malformed); read at call time, never import time |
| T-04-02 | Input Validation (V5) | factor value applied to sigma | mitigate | clamp the read factor into [0.85, 1.20] before multiplying sigma, even if the file is out of range |
| T-04-09 | Tampering / Elevation | feedback loop altering a graded verdict or gate logic | mitigate | calibration writes only calibration.json; sigma injection is upstream of evaluate_no_bet_gates; TestIntegrityNoVerdictChange (Design A) + TestIntegrityGateOutput (Design C) + Plan 01 AST check (Design B) assert no verdict/gate change (METRICS-03 / D-13) |
| T-04-10 | Denial of Service | weekly_metrics task exceeding the 660s budget | mitigate | TASK_TIMEOUTS["weekly_metrics"]=660; read-only aggregation + 1 JSON write + 1 Telegram call + 1 Obsidian sync (<45s per RESEARCH §4) |
| T-04-11 | Denial of Service | a delivery failure crashing the task | mitigate | Telegram and Obsidian delivery wrapped in try/except; task returns a dict, never raises (defensive SKIP) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | no package installs — stdlib + openpyxl only |
</threat_model>

<verification>
- `cd scripts && python3 -m pytest test_weekly_metrics.py -x -q` passes TestSigmaInjection + all three integrity classes.
- `python3 -c "import generate_projections"` clean; model_over_probability + estimate_sigma bodies unchanged.
- Runner bootstrap confirms weekly_metrics wired into TASK_TIMEOUTS, task_workbook_paths, run_task.
- Phase-end full suite: `cd scripts && python3 -m pytest -x` shows no NEW failures beyond the known "2 failed, 202 passed" baseline (plus the new green test_weekly_metrics + test_metrics_report).
</verification>

<success_criteria>
- The per-sport calibration factor is consumed at projection time via the sigma path (D-07/D-09); model_over_probability and evaluate_no_bet_gates are byte-for-byte unchanged.
- The standalone weekly_metrics task (D-02) recomputes calibration (D-12) and delivers the week×sport ROI + hit-rate report to Telegram + Obsidian (D-01/METRICS-01).
- METRICS-03 integrity is proven by test (Design A verdict snapshot + Design C gate output + Design B AST import check from Plan 01) — D-13 guarantee holds.
- The operator-must-add-cron caveat (D-02) is recorded in the SUMMARY.
- No NEW suite failures beyond the known baseline.
</success_criteria>

<output>
Create `.planning/phases/04-dual-metrics-and-feedback/04-03-SUMMARY.md` when done.
</output>
