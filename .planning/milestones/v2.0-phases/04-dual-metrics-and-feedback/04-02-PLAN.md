---
phase: 04-dual-metrics-and-feedback
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/metrics_report.py
  - scripts/test_metrics_report.py
autonomous: true
requirements: [METRICS-01]
must_haves:
  truths:
    - "D-03: each week × sport row shows slip ROI and prop hit-rate plus a week-over-week delta and a ↑/→/↓ arrow; no auto-computed improving/stagnant verdict line is emitted"
    - "D-04: slip ROI and slip win-rate are computed over STAKED slips only (stake > 0); zero-stake recorded-not-bet slips are reported as a separate informational count, never blended into the money metric"
    - "D-05: slip ROI = Σ Net PnL / Σ Stake (dollar-weighted) over staked slips; prop hit-rate reuses the existing wins/(wins+losses) PUSH-excluded definition read from the Prop Accuracy sheet"
    - "D-06: sport attribution is derived from the slip's legs by reading the per-sport workbooks (data/nba, data/mlb); any future cross-sport slip maps to a MIXED bucket"
    - "METRICS-01: a report aggregates slip ROI and prop hit-rate by ISO-week × sport (NBA/MLB) so improving-vs-stagnant is answerable from data"
  artifacts:
    - path: "scripts/metrics_report.py"
      provides: "aggregate_slip_roi_by_week_sport, read_prop_hit_rate_by_week_sport, wow_arrow, build_weekly_report, format_telegram_digest, fill_obsidian_recap_markdown"
      min_lines: 140
    - path: "scripts/test_metrics_report.py"
      provides: "TestSlipRoiAggregation, TestPropHitRateAggregation, TestWowArrow, TestZeroStakeSeparation"
      min_lines: 90
  key_links:
    - from: "scripts/metrics_report.py aggregate_slip_roi_by_week_sport"
      to: "data/nba/nba_*.xlsx + data/mlb/mlb_*.xlsx Slip History"
      via: "per-sport workbook glob + SLIP_HISTORY_HEADERS index lookup"
      pattern: "Slip History"
    - from: "scripts/metrics_report.py read_prop_hit_rate_by_week_sport"
      to: "data/pnl/master_pnl.xlsx Prop Accuracy"
      via: "PROP_ACCURACY_HEADERS read (reuses refresh_prop_accuracy output)"
      pattern: "Prop Accuracy"
---

<objective>
Build `scripts/metrics_report.py` — the week × sport aggregation and rendering layer for the dual-metrics report (METRICS-01). Pure read-only aggregation + string formatting; no runner import, no workbook writes, no Telegram/Obsidian side effects (the runner task in Plan 03 calls send_telegram / obsidian_sync with the strings this module produces).

Purpose: Aggregate slip ROI (Σ Net PnL / Σ Stake over staked slips, D-04/D-05) by ISO-week × sport from the per-sport workbooks (D-06), pull prop hit-rate from the existing Prop Accuracy sheet (D-05), compute week-over-week deltas with ↑/→/↓ arrows (D-03), and render both a compact Telegram digest and an Obsidian recap markdown body.
Output: `scripts/metrics_report.py`, aggregation + arrow + zero-stake-separation test classes in `scripts/test_metrics_report.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dual-metrics-and-feedback/04-CONTEXT.md
@.planning/phases/04-dual-metrics-and-feedback/04-RESEARCH.md
@.planning/phases/04-dual-metrics-and-feedback/04-PATTERNS.md
@.planning/phases/04-dual-metrics-and-feedback/04-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — use directly. -->

From scripts/slip_payouts.py:
  SLIP_HISTORY_HEADERS = ["Date","Slip ID","Platform","Slip Type","Number of Legs","Legs",
    "Stake Units","Winning Legs","Losing Legs","Push/Void/DNP Legs","Contains Demon",
    "Contains Goblin","Special Line Count","Slip Result","Standard Payout Multiplier",
    "Estimated Payout Multiplier","Actual Payout Multiplier","Payout Confidence",
    "Gross Return","Net PnL","Needs Payout Reconciliation","Graded At","Notes"]
  → resolve columns via SLIP_HISTORY_HEADERS.index("Net PnL") etc.

From scripts/sports_system_runner.py:
  PROP_ACCURACY_HEADERS = ["Week","Sport","Total Props","Wins","Losses","Pushes","Hit Rate","Updated At"]
  (Prop Accuracy sheet rows already cover 2026-W24 NBA+MLB, 2026-W25 MLB, 2026-W26 MLB.)

From scripts/workbook_io.py:
  safe_load_workbook(path, retries=5, delay=1.0, **kwargs) — pass read_only=True, data_only=True.

ISO-week key format (refresh_prop_accuracy:5332):
  iso_week = f"{d.isocalendar().year}-W{d.isocalendar().week:02d}"

Path constants for metrics_report.py:
  ROOT = Path(__file__).resolve().parents[1]; DATA = ROOT/"data"
  NBA_DIR = DATA/"nba"; MLB_DIR = DATA/"mlb"; PNL_DIR = DATA/"pnl"
  MASTER_PNL = PNL_DIR/"master_pnl.xlsx"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: metrics_report.py module + slip ROI aggregation (D-04/D-05/D-06) + zero-stake separation</name>
  <read_first>
    - scripts/sports_system_runner.py lines 5126-5190 (sync_slip_bankroll: Slip History header-index reads via SLIP_HISTORY_HEADERS) and 5313-5343 (refresh_prop_accuracy ISO-week iter_rows loop shape)
    - scripts/slip_payouts.py lines 1-24 (module shape + SLIP_HISTORY_HEADERS)
    - scripts/grade_slips.py lines 598-627 (slips_by_sport routing → why per-sport workbooks carry single-sport slips, D-06)
    - .planning/phases/04-dual-metrics-and-feedback/04-RESEARCH.md §3 (sport attribution Approach A), §5 (aggregate_slip_roi_by_week_sport pseudo-code), Pitfall 3 ("GRADED" legacy rows) + Pitfall 6
    - .planning/phases/04-dual-metrics-and-feedback/04-PATTERNS.md "scripts/metrics_report.py" section
  </read_first>
  <files>scripts/metrics_report.py, scripts/test_metrics_report.py</files>
  <behavior>
    - aggregate_slip_roi_by_week_sport iterates data/nba/nba_*.xlsx and data/mlb/mlb_*.xlsx Slip History (single-sport per workbook, D-06); keys by (iso_week, "NBA"|"MLB").
    - Staked slips only (stake > 0) feed ROI and win-rate; zero-stake slips counted separately as zero_stake, never added to total_stake/total_pnl.
    - Rows with Needs Payout Reconciliation truthy are excluded entirely.
    - ROI per (week,sport) = total_pnl / total_stake (Σ Net PnL / Σ Stake); guarded against zero stake (None when no staked slips).
    - Legacy Slip Result == "GRADED" rows: included in ROI (Net PnL) but excluded from win/loss-rate counting (ambiguous label, Pitfall 3).
    - Date before INCEPTION_DATE 2026-06-08 excluded.
  </behavior>
  <action>
    Create scripts/metrics_report.py mirroring the slip_payouts.py module shape: shebang, docstring stating "Read-only … never writes workbooks, Pick History, Results, or gate logic.", `from __future__ import annotations`, imports (json, os, collections, datetime.date as _date_cls, datetime, timezone, pathlib, typing.Any) plus `from workbook_io import safe_load_workbook` and `from slip_payouts import SLIP_HISTORY_HEADERS`. No sports_system_runner import (avoids circular import; PROP_ACCURACY_HEADERS is re-declared locally as a constant). Declare ROOT/DATA/NBA_DIR/MLB_DIR/PNL_DIR/MASTER_PNL and INCEPTION_DATE="2026-06-08". Implement `aggregate_slip_roi_by_week_sport(inception=INCEPTION_DATE) -> dict[tuple[str,str], dict]` per RESEARCH §5: for sport in ("nba","mlb"), `safe_load_workbook(wb_path, read_only=True, data_only=True)` over `sorted((NBA_DIR if sport=='nba' else MLB_DIR).glob(f"{sport}_*.xlsx"))`, SKIP workbooks without a "Slip History" sheet, iter_rows(min_row=2, values_only=True), resolve Date/Stake Units/Net PnL/Slip Result/Needs Payout Reconciliation via SLIP_HISTORY_HEADERS.index, skip recon-flagged rows, compute iso_week, bucket by (iso_week, sport.upper()). Accumulate staked/zero_stake counts, total_stake, total_pnl, wins/losses/pushes (wins/losses only for Slip Result in {WIN,LOSS}; "GRADED" excluded from W/L but its Net PnL still in total_pnl). Add a small `slip_roi(rec)` helper returning total_pnl/total_stake or None when total_stake<=0. Fill TestSlipRoiAggregation + TestZeroStakeSeparation in scripts/test_metrics_report.py using in-memory openpyxl workbooks written to a tmp data tree (monkeypatch NBA_DIR/MLB_DIR module attrs to a tmpdir, or pass explicit dirs) — assert ROI == Σ Net PnL / Σ Stake over staked rows only and that a zero-stake row increments zero_stake without touching total_stake.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest "test_metrics_report.py::TestSlipRoiAggregation" "test_metrics_report.py::TestZeroStakeSeparation" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Given two staked MLB slips (stake 1.0 net +2.0; stake 1.0 net −1.0) and one zero-stake slip in the same ISO-week: ROI == (2.0−1.0)/(1.0+1.0) == 0.5; zero_stake == 1; total_stake == 2.0.
    - A row with Needs Payout Reconciliation truthy is excluded from all counts.
    - A "GRADED" Slip Result row contributes its Net PnL to ROI but not to wins/losses.
    - `python3 -c "import metrics_report"` imports cleanly with no sports_system_runner import.
  </acceptance_criteria>
  <done>Slip ROI aggregation reads per-sport workbooks, staked-only ROI = Σ Net PnL / Σ Stake, zero-stake separated; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prop hit-rate read + WoW arrow + report builders (Telegram digest + Obsidian markdown)</name>
  <read_first>
    - scripts/sports_system_runner.py lines 5293-5355 (refresh_prop_accuracy + PROP_ACCURACY_HEADERS:299 — the Prop Accuracy sheet this reads) and 1034-1098 (obsidian_create_weekly_recap scaffold sections to fill)
    - .planning/phases/04-dual-metrics-and-feedback/04-RESEARCH.md §5 (wow_arrow helper, Telegram digest format, Obsidian scaffold fill, Pitfall 5 about the path.exists guard)
    - .planning/phases/04-dual-metrics-and-feedback/04-CONTEXT.md D-01, D-03
    - scripts/metrics_report.py (the Task 1 module being extended)
  </read_first>
  <files>scripts/metrics_report.py, scripts/test_metrics_report.py</files>
  <behavior>
    - read_prop_hit_rate_by_week_sport reads master_pnl Prop Accuracy sheet → {(iso_week, SPORT): hit_rate} reusing the persisted wins/(wins+losses) Hit Rate column (PUSH excluded already); SKIP-returns empty if sheet absent.
    - wow_arrow(current, prev, threshold=0.005) → "↑" if current-prev>thr, "↓" if <-thr, "→" otherwise; "→" when either is None.
    - build_weekly_report merges slip ROI + prop hit-rate per (week,sport), computes WoW deltas/arrows against the prior ISO-week for the same sport, and returns a structured report dict (no verdict line, D-03).
    - format_telegram_digest renders a compact multi-line message: header with latest ISO-week, one line per sport with "Slip ROI x% arrow | Hits y% arrow", plus a zero-stake informational count line; no improving/stagnant verdict.
    - fill_obsidian_recap_markdown renders an Obsidian markdown body with an Overview + By Sport (week×sport table with ROI/hit-rate/WoW arrows) section and an Adjustments-for-Next-Week placeholder (calibration note is appended by the Plan 03 task).
  </behavior>
  <action>
    Extend metrics_report.py: declare PROP_ACCURACY_HEADERS locally (Week, Sport, Total Props, Wins, Losses, Pushes, Hit Rate, Updated At). Implement `read_prop_hit_rate_by_week_sport(master_path=MASTER_PNL, _wb_override=None) -> dict[tuple[str,str], float]` reading the Prop Accuracy sheet via safe_load_workbook(read_only=True, data_only=True), resolving columns by header name, returning {(week, sport.upper()): float(hit_rate)} and SKIP-returning {} when the sheet is missing. Implement `wow_arrow(current, prev, threshold=0.005) -> str` exactly per RESEARCH §5 (ARROW unicode ↑/→/↓). Implement `build_weekly_report(roi_agg=None, prop_rates=None) -> dict[str, Any]` that merges the two aggregations, sorts by ISO-week, computes per-sport WoW deltas + arrows for both ROI and hit-rate, and returns rows plus the latest-week summary and a zero_stake total. Implement `format_telegram_digest(report) -> str` producing the compact D-03 digest (no verdict line). Implement `fill_obsidian_recap_markdown(report, calibration_note="") -> str` producing the markdown body to pass through obsidian_sync (the Plan 03 task supplies calibration_note and bypasses the obsidian_create_weekly_recap path.exists guard per Pitfall 5). Fill TestPropHitRateAggregation (reads a synthetic Prop Accuracy sheet → correct {(week,sport):rate}) and TestWowArrow (increasing→↑, flat→→, decreasing→↓, None→→) in test_metrics_report.py.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest "test_metrics_report.py::TestPropHitRateAggregation" "test_metrics_report.py::TestWowArrow" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - wow_arrow(0.52, 0.47) == "↑"; wow_arrow(0.47, 0.52) == "↓"; wow_arrow(0.50, 0.50) == "→"; wow_arrow(0.5, None) == "→".
    - read_prop_hit_rate_by_week_sport over a 2-row Prop Accuracy fixture (W25 MLB 0.55, W26 MLB 0.60) returns {("2026-W25","MLB"):0.55, ("2026-W26","MLB"):0.60}.
    - format_telegram_digest output contains the latest ISO-week, both ROI and Hits per sport with an arrow, and a zero-stake count line; contains no "improving"/"stagnant" verdict text.
    - fill_obsidian_recap_markdown output contains a By Sport section with NBA and MLB rows and an Adjustments-for-Next-Week heading.
  </acceptance_criteria>
  <done>Prop hit-rate reads from Prop Accuracy; WoW arrows correct; Telegram digest + Obsidian markdown render week×sport ROI + hit-rate with no verdict line; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → metrics_report.py | reads per-sport workbooks + master_pnl (untrusted/possibly half-written during a concurrent daily run) |
| metrics_report.py → operator (Telegram/Obsidian) | rendered strings are delivered downstream by the Plan 03 runner task |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | reading a half-written per-sport workbook during a concurrent daily run | mitigate | use safe_load_workbook (retry-on-stale) with read_only=True, data_only=True; SKIP sheets/rows that are malformed rather than crash |
| T-04-07 | Information Disclosure | Telegram digest could leak nothing sensitive (ROI %, hit-rate) | accept | low-value local operator data; same channel already receives all picks |
| T-04-08 | Denial of Service | iterating ~30 dated per-sport workbooks per run | accept | small files, read-only, <5s per RESEARCH A4; well under the 660s budget |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | no package installs — stdlib + openpyxl only |
</threat_model>

<verification>
- `cd scripts && python3 -m pytest test_metrics_report.py -x -q` passes all four classes.
- `python3 -c "import metrics_report"` clean; no sports_system_runner import (no circular dependency).
- No new third-party dependency; no workbook writes performed by this module.
</verification>

<success_criteria>
- metrics_report.py aggregates slip ROI (staked-only Σ Net PnL / Σ Stake, D-04/D-05) and prop hit-rate (Prop Accuracy sheet) by ISO-week × sport from per-sport workbooks (D-06).
- WoW delta + ↑/→/↓ arrows render with no auto verdict line (D-03).
- Telegram digest + Obsidian markdown bodies are produced as strings for the runner task to deliver (D-01).
- All report test classes pass; no NEW suite failures beyond the known baseline.
</success_criteria>

<output>
Create `.planning/phases/04-dual-metrics-and-feedback/04-02-SUMMARY.md` when done.
</output>
