---
phase: 03-resilience
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/test_res01_subprocess_retry.py
  - scripts/test_res03_task_timeout.py
autonomous: true
requirements: [RES-01, RES-03, RES-04]
must_haves:
  truths:
    - "A fault-injection test proves the subprocess stage re-runs exactly once on non-zero exit"
    - "A fault-injection test proves exit 0 (empty board) is NOT retried"
    - "A fault-injection test proves two consecutive hard failures cause the stage to raise/propagate"
    - "A subprocess-isolated test proves a hung task self-terminates within its (patched-short) budget, fires ⏱ TASK TIMED OUT, kills the child (no orphan), and exits 1"
    - "A subprocess-isolated test proves a healthy task cancels its alarm and emits neither TIMED OUT nor TASK FAILED"
    - "Each new test fails on pre-fix code and passes on post-fix code (fault-injection by construction)"
  artifacts:
    - path: "scripts/test_res01_subprocess_retry.py"
      provides: "RES-01 subprocess re-run regression (monkeypatch subprocess)"
      contains: "class TestRes01"
    - path: "scripts/test_res03_task_timeout.py"
      provides: "RES-03 task timeout regression (subprocess-isolated SIGALRM)"
      contains: "TIMED OUT"
  key_links:
    - from: "test_res01_subprocess_retry.py"
      to: "runner.run_fetch_dfs_props / run_build_hit_rate_db / run_generate_projections"
      via: "importlib load + patch subprocess.Popen/run, assert call count"
      pattern: "call_count"
    - from: "test_res03_task_timeout.py"
      to: "sports_system_runner.py --task verify subprocess"
      via: "subprocess.Popen spawn + proc.wait(timeout=budget+margin) + nonce-fence log scan"
      pattern: "TIMED OUT"
---

<objective>
Add the RES-01 and RES-03 regression tests as two independent `unittest` files. Each test injects the exact fault its fix addresses so it cannot pass without the Plan 01 runner changes (D-11 fault-injection-by-construction). RES-01's test is in-process (monkeypatch the subprocess call inside the three stage functions, assert call counts). RES-03's test MUST be subprocess-isolated (SIGALRM state is process-wide and bleeds between in-process test cases — Pitfall 3), spawning the runner as a child with a short patched budget and asserting clean self-termination + the `⏱ TASK TIMED OUT` alert.

Purpose: Satisfy RES-04 (every reliability fix lands with a fail-before/pass-after regression test) for the RES-01 and RES-03 behaviors and lock in the Phase-3 safety net against regression.
Output: `scripts/test_res01_subprocess_retry.py` and `scripts/test_res03_task_timeout.py`, both green against the Plan 01 runner and red against pre-fix code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-resilience/03-PATTERNS.md
@.planning/phases/03-resilience/03-VALIDATION.md
@.planning/phases/03-resilience/03-01-SUMMARY.md

<interfaces>
<!-- Test analogs — read these files directly; they ARE the templates. -->
- scripts/test_fix02_telegram_circuit_breaker.py — RES-01 analog (importlib runner load lines 41-45, setUp/tearDown reset lines 50-63, monkeypatch+call-count lines 68-84, log-capture lines 88-102).
- scripts/test_fix01_broken_pipe.py — RES-03 analog (module constants lines 37-51, drain-and-close-at-sentinel lines 54-69, nonce-fence `_count_nonce_signals` lines 72-93, subprocess.Popen spawn + proc.wait lines 111-184).

Runner symbols available after Plan 01 (load via `importlib.util.spec_from_file_location("sports_system_runner", SCRIPT)`):
- `runner.run_fetch_dfs_props(sport)` / `run_build_hit_rate_db(sport, date)` / `run_generate_projections(sport, date)` — route through `_subprocess_run_with_retry` (uses `subprocess.Popen`).
- `runner.TASK_TIMEOUTS` — patchable dict (e.g. `patch.dict(runner.TASK_TIMEOUTS, {"verify": 3})` for an in-process check, but RES-03 prefers a subprocess child).
- Alert strings emitted to the run log / Telegram: `⏱ TASK TIMED OUT`, `❌ SPORTS TASK FAILED`, `TIMEOUT task=`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_res01_subprocess_retry.py — subprocess re-run fault injection (RES-01)</name>
  <files>scripts/test_res01_subprocess_retry.py</files>
  <read_first>
    - scripts/test_fix02_telegram_circuit_breaker.py (entire file) — the canonical importlib-load + monkeypatch + call-count + log-capture analog
    - scripts/sports_system_runner.py lines 1278-1408 (the three stage functions + the `_subprocess_run_with_retry` helper added in Plan 01) — what is being exercised
    - .planning/phases/03-resilience/03-PATTERNS.md "test_res01_subprocess_retry.py" section (lines 22-105) — exact adaptation guidance
    - .planning/phases/03-resilience/03-RESEARCH.md Q5 test_res01 stubs (lines 591-638) and Pitfall 4 (lines 743-746, the empty-board D-02 guard)
  </read_first>
  <action>
    Create a plain `unittest.TestCase` (`class TestRes01SubprocessRetry`) — no pytest fixtures. Start with `#!/usr/bin/env python3`, a docstring, `from __future__ import annotations`, then the importlib runner-load block copied from test_fix02 (lines 41-45). Because the Plan 01 helper uses `subprocess.Popen` (not `subprocess.run`), the monkeypatch target is `subprocess.Popen` (patch it where the runner uses it — `patch("subprocess.Popen", ...)` or `patch.object(runner.subprocess, "Popen", ...)`). Use a fake Popen object (or a closure with a nonlocal `call_count`) whose `.wait(timeout=...)` and `.returncode`/`.stdout`/`.stderr` are controllable. Write three test methods: (1) `test_subprocess_retry_on_nonzero_exit` — first attempt returncode 1, second attempt returncode 0; assert exactly 2 Popen constructions and the stage succeeds (run_fetch_dfs_props returns without raising). (2) `test_no_retry_on_clean_exit` — every attempt returncode 0 with empty stdout; assert exactly 1 Popen construction (D-02: empty board is NOT retried — Pitfall 4). (3) `test_after_one_retry_fails_propagates` — both attempts returncode 1; assert the stage raises (run_fetch_dfs_props re-raises RuntimeError after the single re-run). Patch `runner.log` and `time.sleep` (so the 5 s backoff does not slow the test) via the log-capture/monkeypatch pattern from test_fix02; restore in `tearDown` or `finally`. Each assertion message must name the RES-01 regression and the expected vs actual call count. End with `if __name__ == "__main__": unittest.main()`. Do not paste the helper implementation — only the test. Keep the test fast (< 10 s) by patching sleep.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_res01_subprocess_retry.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 -m pytest test_res01_subprocess_retry.py -x` exits 0 (all 3 tests pass against the Plan 01 runner)
    - File contains `class TestRes01SubprocessRetry(unittest.TestCase)` and three `def test_` methods covering: retry-on-nonzero (call_count == 2), no-retry-on-exit-0 (call_count == 1), two-failures-propagate (assertRaises)
    - File starts with `#!/usr/bin/env python3` and `from __future__ import annotations`; ends with `if __name__ == "__main__": unittest.main()`
    - Test patches `time.sleep` (or the runner's sleep call) so the backoff does not add 5 s/retry; total runtime < 15 s
    - Fault-injection rigor: the no-retry test asserts exactly 1 invocation (would still pass pre-fix) AND the retry test asserts exactly 2 (FAILS pre-fix, where there is no retry helper → 1 invocation)
  </acceptance_criteria>
  <done>The RES-01 test proves the subprocess re-run fires once on hard failure, never on exit 0, and propagates after the single re-run fails — and would fail against pre-Plan-01 code.</done>
</task>

<task type="auto">
  <name>Task 2: test_res03_task_timeout.py — subprocess-isolated SIGALRM timeout (RES-03)</name>
  <files>scripts/test_res03_task_timeout.py</files>
  <read_first>
    - scripts/test_fix01_broken_pipe.py (entire file) — the subprocess.Popen spawn + proc.wait + nonce-fence log-scan analog
    - scripts/sports_system_runner.py main() (lines 5566-5631 after Plan 01) — the SIGALRM arming, `⏱ TASK TIMED OUT` alert, `except TaskTimeoutError`, `signal.alarm(0)` cancel
    - .planning/phases/03-resilience/03-PATTERNS.md "test_res03_task_timeout.py" section (lines 224-256) and Shared Patterns (nonce-fence lines 577-591)
    - .planning/phases/03-resilience/03-RESEARCH.md Q5 test_res03 stub (lines 680-719), Pitfall 3 (lines 737-741, why subprocess isolation is mandatory), Open Question 3 (lines 925-928)
  </read_first>
  <action>
    Create a plain `unittest.TestCase` (`class TestRes03TaskTimeout`). Header same as test_fix01 (`#!/usr/bin/env python3`, docstring, `from __future__ import annotations`, imports: subprocess, sys, time, unittest, uuid, pathlib). Copy the module-level constants (SCRIPTS_DIR, RUNNER, RUN_LOG) and the nonce-fence `_count_nonce_signals`-style helper from test_fix01 — but generalize the scan to count occurrences of arbitrary target substrings after the fence (so it can count `TIMED OUT`, `TASK FAILED`, and `TIMEOUT task=` separately). The timeout MUST be exercised in a spawned child (Pitfall 3 — never in-process; SIGALRM is process-wide). Inject a hung task without modifying the runner: pass the short patched budget through an environment variable the test sets on the child (e.g. set the child's `TASK_TIMEOUTS["verify"]` to a few seconds is not possible across processes via patch.dict, so instead spawn `--task verify` and rely on the real `verify` budget of 60 s only if `verify` cannot be made to hang). Preferred approach (Open Question 3): spawn the runner with an env-var the runner could honor to shorten the budget AND hang the task — if the Plan 01 runner exposes no such hook, fall back to: spawn `--task verify`, accept the real 60 s budget, and force a hang by monkeypatching is not available cross-process. Therefore implement the env-var hook decision in the test as: set `HERMES_TEST_HANG_TASK=verify` and `HERMES_TEST_TASK_BUDGET=3` in the child env IF the runner reads them; if the runner does not read them (no test hook was added in Plan 01), instead structure the test to spawn `--task verify` with a patched-short budget via a tiny test shim. Write two test methods: (1) `test_timeout_fires_on_hung_task` — child hangs, assert `proc.wait(timeout=budget+10)` returns (NOT TimeoutExpired in the test harness), assert returncode == 1, the post-fence log/stdout contains `TIMED OUT`, and does NOT contain `TASK FAILED`. (2) `test_healthy_task_cancels_alarm` — spawn `--task verify` normally (no hang), assert it exits 0 within its budget, and the post-fence log contains neither `TIMED OUT` nor `TASK FAILED` (alarm cancelled cleanly in finally). Use `self.skipTest(...)` for genuine infra failures (cannot write fence / cannot spawn) so infra flakiness is not a false RES-03 red. End with `if __name__ == "__main__": unittest.main()`.

    NOTE for the executor: confirm against the Plan 01 SUMMARY/runner whether a test hang/budget hook exists. If Plan 01 added no env-var hook, the simplest robust hang injection is a dedicated test that spawns the runner with a tiny budget for a real task whose normal runtime exceeds that tiny budget — OR coordinate by adding a minimal, clearly-test-only env-gated hang into the runner is OUT OF SCOPE for this test plan (do not edit the runner here; this plan owns only the two test files). If no clean cross-process hang injection is achievable without a runner hook, raise this in the SUMMARY as a coordination note rather than weakening the assertion.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_res03_task_timeout.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 -m pytest test_res03_task_timeout.py -x` exits 0
    - The test runs the runner as a SUBPROCESS (`subprocess.Popen([sys.executable, ... str(RUNNER) ...])`) — NOT in-process SIGALRM (grep the file: contains `subprocess.Popen` and does NOT call `signal.alarm` in-process)
    - The hung-task test asserts: returncode == 1, post-fence output contains `TIMED OUT`, post-fence output does NOT contain `TASK FAILED`
    - The healthy-task test asserts exit 0 and absence of both `TIMED OUT` and `TASK FAILED`
    - File starts with `#!/usr/bin/env python3` / `from __future__ import annotations`; ends with `if __name__ == "__main__": unittest.main()`
    - Uses `self.skipTest` for infra failures (fence write / spawn) so infra issues are not false RES-03 reds
  </acceptance_criteria>
  <done>The RES-03 test proves, in an isolated subprocess, that a hung task self-terminates within budget with the `⏱ TASK TIMED OUT` alert and exit 1, while a healthy task cancels its alarm and emits neither timeout nor failure alerts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → runner module (in-process importlib) | RES-01 test loads and monkeypatches runner internals; no untrusted input |
| test → spawned runner subprocess | RES-03 test spawns the runner as a child and reads its stdout/run log |
| test → production run_log.txt | Tests append a nonce fence and scan only post-fence content (must not corrupt the operator's log) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-T1 | Tampering | Test polluting the production `run_log.txt` with spurious failure markers | mitigate | Reuse the test_fix01 nonce-fence: write a uuid fence line, scan only content after the fence, never write fake `ERROR`/`TASK FAILED` markers; this is the WR-03-hardened pattern already in use |
| T-03-T2 | Repudiation | A flaky infra failure (cannot spawn / cannot write fence) masquerading as a RES-03 regression | mitigate | Use `self.skipTest(...)` for genuine infra failures so only real fault-injection outcomes assert; `_INFRA_FAILURE` sentinel from the analog |
| T-03-T3 | Denial of Service | Orphaned child runner processes left by the RES-03 subprocess test | mitigate | `proc.kill()` + `proc.wait()` in the test's TimeoutExpired branch (analog pattern); the runner's own SIGALRM handler kills its grandchild subprocess |

No `npm`/`pip`/`cargo` installs in this phase → Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- Quick command: `cd scripts && python3 -m pytest test_res01_subprocess_retry.py test_res03_task_timeout.py -x` exits 0.
- Tests are plain `unittest.TestCase` (no pytest fixtures), run from `scripts/` with `python3`.
- RES-03 test isolates SIGALRM in a spawned subprocess; RES-01 test patches sleep so it stays fast.
- No new pre-existing-baseline failures introduced (baseline = `2 failed, 202 passed`).
</verification>

<success_criteria>
- RES-01 regression: re-run-once on hard failure, no-retry on exit 0, propagate after second failure — all asserted with call counts; red against pre-Plan-01 code.
- RES-03 regression: subprocess-isolated proof a hung task self-terminates with `⏱ TASK TIMED OUT` + exit 1, and a healthy task cancels the alarm cleanly.
- Both files are plain unittest, fault-injection-by-construction, fast, and CI-friendly.
</success_criteria>

<output>
Create `.planning/phases/03-resilience/03-02-SUMMARY.md` when done. If no clean cross-process hang-injection hook exists in the Plan 01 runner, record the coordination note in the SUMMARY (do not weaken the RES-03 assertion silently).
</output>
