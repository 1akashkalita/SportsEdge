---
phase: 02-read-views
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/templates/history.html
autonomous: true
requirements: [VIEW-03]
must_haves:
  truths:
    - "GET /history renders W/L overall and per sport (NBA/MLB) plus a per-confidence-tier breakdown table (A/B/C/UNKNOWN) showing W-L, hit-rate %, ROI %, and sample n per tier (D-09, VIEW-03)"
    - "The UNKNOWN tier row is shown, not hidden — pre-v2.0 graded picks (Confidence Tier None) are legitimate and counted (Pitfall 5, D-09, VIEW-03)"
    - "GET /history renders a bankroll/ROI time-series Chart.js line chart from the persisted Bankroll Chart Data, with a daily (default) / ISO-week toggle (D-08, VIEW-03)"
    - "Chart.js loads from the pinned CDN URL chart.js@4.5.1 with the verified SHA-384 SRI integrity hash and crossorigin=anonymous (RESEARCH Pitfall 11, security control)"
  artifacts:
    - path: "scripts/templates/history.html"
      provides: "W/L + per-sport + per-tier tables and the bankroll/ROI Chart.js chart with daily/weekly toggle"
      contains: "bankroll-chart"
  key_links:
    - from: "scripts/templates/history.html"
      to: "data.by_tier (A/B/C/UNKNOWN)"
      via: "Jinja loop over the four tiers into a breakdown table"
      pattern: "by_tier"
    - from: "scripts/templates/history.html"
      to: "data.chart_daily / data.chart_weekly"
      via: "tojson serialization into Chart.js script block"
      pattern: "tojson"
---

<objective>
Build the History page (`history.html`) rendering the running win/loss record overall + per sport, a per-confidence-tier calibration breakdown, and a bankroll/ROI time-series Chart.js chart with a daily/weekly toggle — all from the Plan 01 `get_history_data()` accessor. Delivers VIEW-03. The `/history` route already exists (Plan 02 owns `dashboard.py`); this plan ships only the template, so it has zero file overlap with Plan 02 and runs in the same wave.

Purpose: Give the operator the calibration signal — do higher-confidence tiers actually win and profit more — alongside the bankroll trajectory since inception. This is the dashboard's answer to the core question "can I tell whether the model is improving."
Output: New `scripts/templates/history.html` with W/L tables + Chart.js chart loaded via the pinned, SRI-verified CDN tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-read-views/02-RESEARCH.md
@.planning/phases/02-read-views/02-PATTERNS.md
@.planning/phases/02-read-views/02-VALIDATION.md
@.planning/phases/02-read-views/02-CONTEXT.md
@.planning/phases/02-read-views/02-01-SUMMARY.md

<interfaces>
Plan 01 get_history_data() return shape (consume this; the /history route from Plan 02 passes the dict as `data`):

get_history_data() returns a dict with keys:
  "overall"  — {W, L, push, hit_pct (float or None), roi_pct (float or None), n}
  "by_sport" — {"NBA": {W,L,hit_pct,roi_pct,n}, "MLB": {W,L,hit_pct,roi_pct,n}}
  "by_tier"  — {"A": {...}, "B": {...}, "C": {...}, "UNKNOWN": {...}} (same sub-dict shape)
  "chart_daily"  — {labels: [date strings], bankroll: [numbers], roi: [numbers]}
  "chart_weekly" — {labels: [ISO-week strings], bankroll: [numbers], roi: [numbers]}
  "locked" — bool

hit_pct and roi_pct are fractions (0.697 = 69.7%); render with a *100 percent format, em dash when None.

Shell contract:
From scripts/templates/base.html: content block line 68, scripts block line 72 (reserved for Chart.js — empty in Phase 1).
The /history route (Plan 02, scripts/dashboard.py) renders history.html with data plus freshness context.

Chart.js CDN tag (LOCKED — RESEARCH Pitfall 11 / Pattern 2; do not change version or hash):
  src  = https://cdn.jsdelivr.net/npm/chart.js@4.5.1/dist/chart.umd.min.js
  integrity = sha384-jb8JQMbMoBUzgWatfe6COACi2ljcDdZQ2OxczGA3bGNeWe+6DChMTBJemed7ZnvJ
  crossorigin = anonymous
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: History page — history.html (VIEW-03)</name>
  <files>scripts/templates/history.html</files>
  <read_first>
    - scripts/templates/base.html (shell: content block line 68, the reserved scripts block line 72 for Chart.js)
    - .planning/phases/02-read-views/02-PATTERNS.md (section "scripts/templates/history.html (CREATE)" — W/L + per-tier table markup, Chart.js scripts block with SRI, canvas + toggle buttons, empty state)
    - .planning/phases/02-read-views/02-RESEARCH.md (Pattern 2 server-to-client Chart.js JSON; VIEW-03 mapping tables; Pitfall 5 None tier as UNKNOWN; Pitfall 8 None serializes to null; Pitfall 11 SRI hash)
  </read_first>
  <action>Create scripts/templates/history.html extending base.html with a History title block and a content block. Render three sections. (1) Overall and per-sport W/L: a small table per scope (Overall, NBA, MLB) showing W, L, Hit % (a percent of hit_pct times 100 when not none else em dash), ROI % (a percent of roi_pct times 100 when not none else em dash), and n — reading data.overall and data.by_sport["NBA"] / data.by_sport["MLB"] (D-09). (2) Per Confidence Tier breakdown: a table looping the fixed tier order ['A','B','C','UNKNOWN'], each row reading data.by_tier[tier] and showing Tier, W-L (the W and L joined with a hyphen), Hit %, ROI %, and n — the UNKNOWN row MUST always render (Pitfall 5; label it e.g. "UNKNOWN / pre-v2.0"). (3) Bankroll Over Time: Daily and Weekly toggle buttons (ids toggle-daily, toggle-weekly) above a canvas with id bankroll-chart; wrap the canvas in a guard so when data.chart_daily.labels is empty it shows "No chart data yet." instead. In the scripts block, FIRST emit the Chart.js CDN script tag with the EXACT pinned src, integrity (sha384-jb8JQMbMoBUzgWatfe6COACi2ljcDdZQ2OxczGA3bGNeWe+6DChMTBJemed7ZnvJ), and crossorigin anonymous from the interfaces block. THEN a script that serializes the four series via the Jinja tojson filter (data.chart_daily.labels, data.chart_daily.bankroll, data.chart_weekly.labels, data.chart_weekly.bankroll — tojson handles None to null and special chars, Pitfall 8), builds a Chart.js line chart defaulting to the daily series with spanGaps false, and wires the two toggle buttons to swap labels+data and call chart.update(). Add a data.locked guard showing "Data is updating…" as a secondary note. CRITICAL: tier/sport label strings rendered with double-brace expressions are auto-escaped; only the numeric chart series use tojson (never the safe filter) — XSS / Tampering mitigation T-02-01.</action>
  <verify>
    <automated>cd scripts && python3 -m pytest "test_dashboard_views.py::TestRoutes::test_history_200" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 -m pytest "test_dashboard_views.py::TestRoutes::test_history_200" -x` exits 0 (GET /history returns 200, body contains the Chart.js script tag).
    - `grep -c "bankroll-chart" scripts/templates/history.html` is at least 1 (chart canvas present).
    - `grep -c "sha384-jb8JQMbMoBUzgWatfe6COACi2ljcDdZQ2OxczGA3bGNeWe+6DChMTBJemed7ZnvJ" scripts/templates/history.html` is exactly 1 (pinned SRI hash).
    - `grep -c "chart.js@4.5.1" scripts/templates/history.html` is at least 1 (pinned version); `grep -c "crossorigin" scripts/templates/history.html` is at least 1.
    - `grep -c "by_tier" scripts/templates/history.html` is at least 1 and `grep -c "UNKNOWN" scripts/templates/history.html` is at least 1 (tier breakdown incl. UNKNOWN).
    - `grep -c "tojson" scripts/templates/history.html` is at least 1; `grep -c "| safe" scripts/templates/history.html` is 0.
  </acceptance_criteria>
  <done>history.html renders Overall/NBA/MLB W/L tables, the A/B/C/UNKNOWN tier breakdown, and the bankroll/ROI Chart.js chart with daily/weekly toggle loaded via the pinned SRI-verified CDN tag; GET /history smoke green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → CDN (jsDelivr) | Chart.js JS fetched from a third-party CDN and executed in the page |
| workbook content → Chart.js series / tier labels | Bankroll/ROI numbers and tier/sport labels rendered into HTML + JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Chart.js loaded from jsDelivr CDN | mitigate | Subresource Integrity: pinned chart.js@4.5.1 with SHA-384 integrity hash sha384-jb8JQMbMoBUzgWatfe6COACi2ljcDdZQ2OxczGA3bGNeWe+6DChMTBJemed7ZnvJ and crossorigin=anonymous — the browser refuses a tampered/altered CDN file (asserted by the grep-for-hash acceptance check) |
| T-02-01 | Tampering | tier/sport labels + chart series in history.html | mitigate | Jinja2 autoescaping on double-brace expressions; numeric series serialized only via the tojson filter (handles None and special chars); the safe filter is forbidden (grep-for-safe is 0) |
| T-02-03 | Information disclosure | /history route | accept | 127.0.0.1 bind enforced in dashboard.py (DASH-03, Phase 1); GET-only, no auth for a solo-operator localhost tool (design doc §8) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Chart.js is CDN-loaded, not installed; no new Python packages (RESEARCH: no new packages) |
</threat_model>

<verification>
- `cd scripts && python3 -m pytest test_dashboard_views.py::TestRoutes::test_history_200 -x` — history smoke green (Chart.js tag in body).
- `cd scripts && python3 -m pytest test_dashboard.py test_dashboard_data.py test_dashboard_views.py -x` — full dashboard suite green.
- `grep -c "sha384-jb8JQMbMoBUzgWatfe6COACi2ljcDdZQ2OxczGA3bGNeWe+6DChMTBJemed7ZnvJ" scripts/templates/history.html` == 1 (SRI present).
- `grep -c "| safe" scripts/templates/history.html` == 0 (no autoescape bypass).
- Manual (per 02-VALIDATION.md Manual-Only): launch the dashboard, open /history in a browser, confirm the bankroll line draws and the daily/weekly toggle swaps series — not exercised by test_client().
</verification>

<success_criteria>
- GET /history renders W/L overall + per sport + the A/B/C/UNKNOWN tier breakdown (UNKNOWN never hidden) (VIEW-03, D-09).
- GET /history renders the bankroll/ROI Chart.js chart with a daily-default / ISO-week toggle from persisted Bankroll Chart Data (VIEW-03, D-08).
- Chart.js loads via the pinned chart.js@4.5.1 CDN URL with the verified SHA-384 SRI hash + crossorigin (security control).
- history smoke green; no safe filter on workbook data.
</success_criteria>

<output>
Create `.planning/phases/02-read-views/02-03-SUMMARY.md` when done.
</output>
