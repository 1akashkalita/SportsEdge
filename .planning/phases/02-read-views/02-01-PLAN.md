---
phase: 02-read-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dashboard_data.py
  - scripts/test_dashboard_views.py
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03]
must_haves:
  truths:
    - "get_today_board() returns the evaluated set — approved Picks rows + Skipped Picks rows for today, each skipped row carrying status_label 'Skip: <GATE NAME>' from the Gate Failed column (D-01, D-04, VIEW-01)"
    - "get_today_board() returns locked=True when a workbook read returns None (mid-write lock), never raising (D-01-foundation lock tolerance, VIEW-01)"
    - "ev_float is None (not a crash) for skipped picks whose EV is the string 'unavailable' (VIEW-01)"
    - "get_all_slips() returns all slips from master_pnl.xlsx Slip History sorted date-descending, with legs_list split from the Legs string and a two-tier why_paired string (stored Reasoning/Correlation Group, else derived from the Slip ID category) (D-05, D-06, D-07, VIEW-02)"
    - "get_history_data() returns overall + per-sport W/L, a per-confidence-tier breakdown (A/B/C/UNKNOWN with W-L, hit-rate, ROI, sample n) treating None tier as UNKNOWN, and chart_daily + ISO-week chart_weekly bankroll series (D-08, D-09, VIEW-03)"
  artifacts:
    - path: "scripts/dashboard_data.py"
      provides: "get_today_board, get_all_slips, get_history_data view accessors + PNL_DIR constant"
      contains: "def get_today_board"
    - path: "scripts/test_dashboard_views.py"
      provides: "Wave-0 unit tests for all three accessors (TestTodayBoard, TestSlipsAccessor, TestHistoryAccessor)"
      contains: "class TestTodayBoard"
  key_links:
    - from: "scripts/dashboard_data.py:get_today_board"
      to: "read_sheet_rows"
      via: "lock-tolerant sheet read (never safe_load_workbook directly)"
      pattern: "read_sheet_rows\\("
    - from: "scripts/dashboard_data.py:get_all_slips"
      to: "PNL_DIR / master_pnl.xlsx Slip History"
      via: "read_sheet_rows on the master workbook"
      pattern: "master_pnl"
---

<objective>
Add the three read-only view accessors that shape the Today, Slips, and History page data, plus the Wave-0 test file that pins their contract. These accessors are the data foundation every Phase 2 route and template consumes — they MUST land first so routes and templates render against a written, tested contract (interface-first ordering).

Purpose: Centralize all data shaping (date filtering, status labels, EV coercion, leg splitting, why-paired derivation, W/L + tier aggregation, ISO-week chart rollup) in `dashboard_data.py`, keeping route handlers and Jinja templates free of computation (RESEARCH anti-patterns). Strictly read-only — no writes, no schema change.
Output: Three new accessors in `dashboard_data.py` (`get_today_board`, `get_all_slips`, `get_history_data`) + `PNL_DIR` constant; new test file `scripts/test_dashboard_views.py` with TestTodayBoard, TestSlipsAccessor, TestHistoryAccessor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-read-views/02-RESEARCH.md
@.planning/phases/02-read-views/02-PATTERNS.md
@.planning/phases/02-read-views/02-VALIDATION.md
@.planning/phases/02-read-views/02-CONTEXT.md

<interfaces>
<!-- Existing primitives in dashboard_data.py — call these, do NOT re-implement or call safe_load_workbook from accessor bodies. -->
From scripts/dashboard_data.py (Phase 1, already present):
```python
def today_str() -> str:                                          # naive-local "YYYY-MM-DD"; NEVER import zoneinfo
def read_json(path: Path | str) -> dict | list | None:           # lock-free JSON
def read_sheet_rows(xlsx: Path | str, sheet: str) -> list[dict[str, Any]] | None:
    # list[dict] = rows; [] = sheet absent/empty; None = locked/unreadable (serve last-known-good)
# Path constants present: HOME, ROOT, DATA, NBA_DIR, MLB_DIR, LOCK_DIR, RUN_LOG_JSONL
# PNL_DIR does NOT exist yet — add: PNL_DIR: Path = DATA / "pnl"
```

<!-- Column contract = runner header constants (NOT data/nxls_schema.txt, which is stale). Verified live. -->
Picks sheet (sports_system_runner.py:277-283, PICKS_HEADERS) relevant columns:
  "Date", "Sport", "Pick Type", "Selection", "Line", "Confidence", "Units", "Status",
  "Platform", "Player/Team", "Model Projection", "Edge", "Model Over Probability", "EV", "Injury Flag"
  — approved rows have Status == "APPROVED".
Skipped Picks sheet (sports_system_runner.py:295, SKIPPED_PICK_HEADERS) relevant columns:
  "Date", "Sport", "Pick", "Gate Failed", "Reason", "What Edge Would Have Been",
  "Pick Type", "Player/Team", "Line", "Probability", "EV", "Platform"
  — Gate Failed format: "GATE N — HUMAN GATE NAME".
Slip History sheet (slip_payouts.py:18-24, SLIP_HISTORY_HEADERS) relevant columns:
  "Date", "Slip ID", "Platform", "Slip Type", "Number of Legs", "Legs", "Slip Result",
  "Standard Payout Multiplier", "Estimated Payout Multiplier", "Net PnL", "Gross Return",
  "Winning Legs", "Losing Legs", "Contains Demon", "Contains Goblin", "Notes"
  — Legs is semicolon-delimited ("A; B; C"); Slip ID format "2026-06-08:correlated_upside:53254fd0".
Correlated Parlays sheet (sports_system_runner.py:294, PARLAY_HEADERS) relevant columns:
  "Reasoning", "Correlation Group", "Slip ID" — currently empty in live workbooks (Tier-2 derived is the normal path).
Pick History sheet (master_pnl.xlsx): "Date","Sport","Result","Units","PnL","Confidence Tier" + many more.
  Result values: WIN, LOSS, PUSH, PENDING, VOID, MANUAL REVIEW. Confidence Tier: A/B/C/None(→UNKNOWN).
Bankroll Chart Data sheet (master_pnl.xlsx): "Date","Bankroll","ROI","Updated At".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold — test_dashboard_views.py</name>
  <files>scripts/test_dashboard_views.py</files>
  <read_first>
    - scripts/test_dashboard_data.py (analog: sys.path bootstrap lines 31-33, _make_picks_wb fixture lines 48-56, tmpdir + NBA_DIR/MLB_DIR module-override pattern lines 85-102, unittest.main footer)
    - scripts/test_dashboard.py (analog: app.test_client() route smoke shape lines 87-103 — NOTE: TestRoutes belongs in Plan 02/03, not here; this task creates only the accessor test classes)
    - .planning/phases/02-read-views/02-PATTERNS.md (section "scripts/test_dashboard_views.py (CREATE)" — fixture builders, module-path override, header lists)
    - .planning/phases/02-read-views/02-VALIDATION.md (Per-Task Verification Map — exact test method names to create)
    - .planning/phases/02-read-views/02-RESEARCH.md (Per-View Data-Source Mapping — exact column headers for fixtures; live-data tier sample for TestHistoryAccessor calibration)
  </read_first>
  <behavior>
    - TestTodayBoard.test_approved_picks: get_today_board() returns approved Picks rows date-filtered to today_str(), each with status_label == "✓ Approved".
    - TestTodayBoard.test_skipped_picks_gate_label: a Skipped Picks row with Gate Failed "GATE 1 — MINIMUM EDGE" yields status_label == "Skip: MINIMUM EDGE".
    - TestTodayBoard.test_locked_state: when the workbook read path is forced to return None, get_today_board() returns locked=True and does not raise.
    - TestTodayBoard.test_ev_coercion: a skipped row with EV == "unavailable" yields ev_float == None (no exception).
    - TestSlipsAccessor.test_slips_sorted: get_all_slips() returns slips sorted Date descending.
    - TestSlipsAccessor.test_legs_parsed: a slip with Legs "A; B; C" yields legs_list == ["A","B","C"].
    - TestSlipsAccessor.test_why_paired_derived: Slip ID "...:correlated_upside:..." yields why_paired starting "Correlated upside"; an unknown category yields the independent-legs fallback string.
    - TestHistoryAccessor.test_tier_breakdown: per-tier dict has W, L, hit_pct, roi_pct, n keys for A/B/C/UNKNOWN.
    - TestHistoryAccessor.test_none_tier_as_unknown: a Pick History row with Confidence Tier None is counted under the UNKNOWN tier, not dropped.
    - TestHistoryAccessor.test_chart_daily: chart_daily has labels and bankroll lists of equal length from Bankroll Chart Data.
    - TestHistoryAccessor.test_chart_weekly: two Bankroll Chart Data rows in the same ISO week collapse to one weekly label (last point wins).
  </behavior>
  <action>Create scripts/test_dashboard_views.py in stdlib unittest style (no pytest fixtures). Copy the sys.path bootstrap from test_dashboard_data.py lines 31-33 (SCRIPT_DIR insert), then `import dashboard_data`. Build synthetic in-memory workbooks with openpyxl Workbook(), using the EXACT header strings from the `<interfaces>` block (Picks, Skipped Picks, Slip History, Pick History, Bankroll Chart Data sheets). Save fixtures into a tempfile.TemporaryDirectory and override dashboard_data.NBA_DIR / MLB_DIR / PNL_DIR to point at it inside try/finally (restore originals — mirror test_dashboard_data.py lines 94-102). For test_locked_state, monkeypatch dashboard_data.read_sheet_rows to return None for the duration of the call. Create the three accessor test classes (TestTodayBoard, TestSlipsAccessor, TestHistoryAccessor) with the methods named in <behavior>, which match 02-VALIDATION.md exactly. Do NOT create TestRoutes here — route smoke tests belong with the routes in Plan 02/03. End with the `if __name__ == "__main__": unittest.main()` footer. These tests are RED until Task 2 lands the accessors — that is the intended Wave-0 state.</action>
  <verify>
    <automated>cd scripts && python3 -c "import ast; ast.parse(open('test_dashboard_views.py').read())" && python3 -m pytest test_dashboard_views.py --collect-only -q 2>&1 | grep -E "TestTodayBoard|TestSlipsAccessor|TestHistoryAccessor"</automated>
  </verify>
  <acceptance_criteria>
    - `test_dashboard_views.py` exists and parses (python3 ast.parse exits 0).
    - `python3 -m pytest test_dashboard_views.py --collect-only -q` lists test_approved_picks, test_skipped_picks_gate_label, test_locked_state, test_ev_coercion, test_slips_sorted, test_legs_parsed, test_why_paired_derived, test_tier_breakdown, test_none_tier_as_unknown, test_chart_daily, test_chart_weekly.
    - File contains `class TestTodayBoard`, `class TestSlipsAccessor`, `class TestHistoryAccessor` and does NOT contain `class TestRoutes`.
    - File does NOT contain `import zoneinfo` and does NOT call `safe_load_workbook` directly (accessors under test do).
  </acceptance_criteria>
  <done>test_dashboard_views.py exists with the three accessor test classes (11 methods named per VALIDATION), collectible by pytest, RED pending Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three view accessors + PNL_DIR in dashboard_data.py</name>
  <files>scripts/dashboard_data.py</files>
  <read_first>
    - scripts/dashboard_data.py (the file being modified — existing read_sheet_rows lines 87-144, path constants lines 32-38, today_str lines 51-57; mirror their never-raise / lock-tolerant contract)
    - .planning/phases/02-read-views/02-PATTERNS.md (section "scripts/dashboard_data.py — three new view accessors" — exact EV coercion, status label, ISO-week, legs split, why-paired snippets to mirror)
    - .planning/phases/02-read-views/02-RESEARCH.md (Per-View Data-Source Mapping tables + "New accessors to add" return-shape docstrings + Pitfalls 1-8)
    - sports_system_runner.py lines 277-295 (PICKS_HEADERS, PARLAY_HEADERS, SKIPPED_PICK_HEADERS column contract) and slip_payouts.py lines 18-24 (SLIP_HISTORY_HEADERS)
  </read_first>
  <action>Add `PNL_DIR: Path = DATA / "pnl"` to the path-constants block (after RUN_LOG_JSONL). Add three module-level functions, all read-only, all returning dicts and NEVER raising (call read_sheet_rows; treat None return as locked=True with [] fallback; treat [] as empty state).

get_today_board(date: str | None = None) -> dict — reads Picks ("Picks" sheet, filter Status=="APPROVED" and Date==today) and Skipped Picks ("Skipped Picks" sheet, filter Date==today) from BOTH NBA_DIR/f"nba_{today}.xlsx" and MLB_DIR/f"mlb_{today}.xlsx" where today = date or today_str(). Returns {"approved": [...], "skipped": [...], "date": today, "locked": bool}. On each approved row set row["status_label"]="✓ Approved". On each skipped row set row["status_label"]="Skip: "+ (Gate Failed split on " — " keeping the part after, else the raw value). For BOTH approved and skipped rows add row["ev_float"] = float(EV) when EV not in (None, "", "unavailable") else None (wrap in try/except, per Pitfall 2). For skipped rows also coerce Probability the same way. If any read_sheet_rows returns None set locked=True.

get_all_slips() -> dict — reads "Slip History" from PNL_DIR/"master_pnl.xlsx" ONLY (never per-sport workbooks — master is the 88-slip superset, Pitfall 6). Returns {"slips": [...], "locked": bool} sorted by Date descending. For each slip set slip["legs_list"] = [leg for leg in str(slip.get("Legs") or "").split("; ") if leg.strip()] (Pitfall 4) and slip["why_paired"] = two-tier value: Tier-1 = look up the slip's Slip ID in the Correlated Parlays sheet of the per-sport workbook for the date parsed from the Slip ID prefix, returning stored "Reasoning" + "Correlation Group" when a non-empty match exists; Tier-2 fallback (the normal path) = map the middle Slip-ID segment via a module-level _WHY_PAIRED dict (correlated_upside, diversified, highest_ev, safest_2_leg, safest_3_leg, kat_based per 02-PATTERNS.md), defaulting to "Independent legs / no correlation flagged" (D-07). Tier-1 no-match must fall through to Tier-2 without raising (Pitfall 7).

get_history_data() -> dict — reads "Pick History", "Bankroll Chart Data" from PNL_DIR/"master_pnl.xlsx". Returns {"overall":{W,L,push,hit_pct,roi_pct,n}, "by_sport":{"NBA":{...},"MLB":{...}}, "by_tier":{"A":{...},"B":{...},"C":{...},"UNKNOWN":{...}}, "chart_daily":{labels,bankroll,roi}, "chart_weekly":{labels,bankroll,roi}, "locked":bool}. W = count Result=="WIN", L = count Result=="LOSS"; hit_pct = W/(W+L) or None; roi_pct = sum(PnL)/sum(Units) over graded (WIN/LOSS) rows or None; n = total rows in bucket. Bucket by Sport (NBA/MLB) and by Confidence Tier, coercing None tier to "UNKNOWN" (Pitfall 5 — show, never hide). chart_daily reads Bankroll Chart Data Date/Bankroll/ROI in row order. chart_weekly aggregates by ISO week via date.fromisoformat(d).isocalendar() into key "YYYY-Www" (zero-padded week), last row per week wins (02-PATTERNS.md ISO-week snippet). Do NOT import metrics_report.py (RESEARCH anti-pattern). Each tier/sport sub-dict: {"W","L","hit_pct","roi_pct","n"}.

Do NOT import zoneinfo anywhere (test_today_matches_runner guards this). Do NOT write to any file.</action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_dashboard_views.py -x -q && python3 -m pytest test_dashboard.py test_dashboard_data.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest test_dashboard_views.py -x` exits 0 (all 11 accessor tests green).
    - `python3 -m pytest test_dashboard.py test_dashboard_data.py -x` exits 0 (Phase 1 suite still green — no regression).
    - `grep -n "def get_today_board\|def get_all_slips\|def get_history_data\|PNL_DIR" scripts/dashboard_data.py` shows all four present.
    - `grep -c "zoneinfo" scripts/dashboard_data.py` returns 0 (no timezone import — Pitfall 1 guard).
    - `python3 -c "import dashboard_data; b=dashboard_data.get_today_board(); assert set(['approved','skipped','date','locked']) <= set(b)"` exits 0 against live data (never raises even with no workbook).
  </acceptance_criteria>
  <done>get_today_board, get_all_slips, get_history_data + PNL_DIR exist; all Wave-0 accessor tests green; Phase 1 suite unchanged; no zoneinfo import; accessors never raise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → dashboard_data accessors | Workbook/JSON content (player names, selection text, reasoning strings) read from disk; treated as data, never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | get_today_board/get_all_slips/get_history_data file paths | mitigate | Workbook paths are hardcoded as `DATA/"nba"/f"nba_{today}.xlsx"` etc. where `today` comes from `today_str()` (or a callable default), never from request/query input — no path traversal vector |
| T-02-02 | Denial of Service | accessor reading a locked/corrupt workbook | mitigate | Call only `read_sheet_rows`, which returns None on WorkbookAccessError/BadZipFile; accessors set locked=True and serve [] — never raise, never hang the request |
| T-02-03 | Information disclosure | reading outside localhost | accept | Out of scope for this plan — 127.0.0.1 bind is enforced in dashboard.py (DASH-03, Phase 1); accessors are pure local file reads |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure Python over already-installed stdlib + openpyxl (RESEARCH Package Legitimacy Audit: no new packages) |
</threat_model>

<verification>
- `cd scripts && python3 -m pytest test_dashboard_views.py -x` — all accessor tests green.
- `cd scripts && python3 -m pytest test_dashboard.py test_dashboard_data.py -x` — Phase 1 suite unaffected (no regression beyond the known 2 projection failures, which are in a different file).
- `grep -c zoneinfo scripts/dashboard_data.py` == 0.
- Accessors are read-only: no `.save(`, no `open(...,"w")`, no workbook write in any new function.
</verification>

<success_criteria>
- Three accessors return the documented dict shapes and never raise, even with missing/locked/empty workbooks.
- status_label, ev_float, legs_list, why_paired, by_tier (incl. UNKNOWN), chart_daily, chart_weekly all populated per D-01/D-04/D-05/D-06/D-07/D-08/D-09.
- All 11 Wave-0 accessor tests pass; Phase 1 tests still pass.
- VIEW-01, VIEW-02, VIEW-03 data contracts exist for Plan 02/03 to render against.
</success_criteria>

<output>
Create `.planning/phases/02-read-views/02-01-SUMMARY.md` when done.
</output>
