---
phase: 01-diagnosis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md
autonomous: true
requirements: [DIAG-02]
must_haves:
  truths:
    - "The dominant cron-timeout contributor is named with a measured duration, not assumed — via a broad timing sweep across all pipeline stages (D-06)"
    - "Lead #2 (stacked subprocess timeout totals) is explicitly confirmed or ruled out against the 1500s theoretical ceiling with run-log evidence"
    - "A single representative timed run exists for the heavy path (daily_picks nba + mlb) and mlb_prop_monitor, corroborated by existing >90s slow-run warnings (D-07), collected externally-first with throwaway in-runner instrumentation added only if the coarse numbers don't isolate the offender (D-11)"
    - "Timing findings are presented as a ranked-contributors table against the cron time budget, with the python3 3.14-alpha caveat stated"
  artifacts:
    - path: ".planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md"
      provides: "Timing sweep evidence: mined run-log durations + representative timed runs + ranked-contributors table feeding DIAGNOSIS.md timeout section"
      min_lines: 40
      contains: "Ranked"
  key_links:
    - from: ".planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md"
      to: "data/pnl/logs/run_log.txt"
      via: "mined 'completed in' durations and 'consider further optimization' slow-run warnings"
      pattern: "completed in"
---

<objective>
Commission the timing evidence for DIAG-02 via a broad sweep across all pipeline
stages (D-06): mine the existing 15k-line run log for per-task durations and the
`>90s` slow-run warnings, run a single representative timed invocation per
relevant task (D-07), and produce a ranked-contributors table (D-10) that names
which task/stage/subprocess exceeds the cron budget — with evidence, not
assumption.

Purpose: Independently establish which stage is the dominant timeout contributor
and explicitly confirm-or-rule-out the named Lead #2 (stacked subprocess timeout
totals) against the 1500s theoretical ceiling. The research finding (Telegram
retry loops + per-log-line obsidian_sync, NOT the subprocess ceiling) must be
corroborated on this machine. Collect timing EXTERNALLY first; only add temporary
throwaway in-runner instrumentation if the coarse numbers cannot isolate the
offender (D-11).

Output: `.planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md` — the timing
evidence artifact (mined log table + representative timed runs + ranked
contributors) that Plan 03 folds into the DIAGNOSIS.md timeout section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-diagnosis/01-CONTEXT.md
@.planning/phases/01-diagnosis/01-RESEARCH.md
@.planning/phases/01-diagnosis/01-PATTERNS.md

<interfaces>
<!-- Evidence sources and verified subprocess-timeout anchors. Use these directly. -->

Run log (the primary corroboration source — mine BEFORE running anything new, D-07/D-11):
  data/pnl/logs/run_log.txt
  - Duration lines:        grep 'completed in' data/pnl/logs/run_log.txt
  - Slow-run warnings:     grep 'consider further optimization' data/pnl/logs/run_log.txt
  - Network/Telegram stalls: grep -E 'Telegram alert failed|NameResolutionError|nodename nor servname|Broken pipe' data/pnl/logs/run_log.txt

Declared subprocess timeouts (VERIFIED in scripts/sports_system_runner.py — the Lead #2 ceiling):
  fetch_dfs_props        300s   (~line 1272)
  build_hit_rate_db      600s   (~line 1351)
  generate_projections   600s   (~line 1385)
  obsidian_sync (per call) 60s  (~line 411)
  Theoretical daily_picks ceiling = 300 + 600 + 600 = 1500s

The runner's own timing already exists (do NOT re-instrument unless needed):
  main() finally block logs `[{task}] completed in {elapsed:.1f}s` (line 5644)
  and `WARNING: {task} took {elapsed:.1f}s` when elapsed > 90 (line 5646)

The 11 runner tasks (target set for the sweep): nba_daily_picks, mlb_daily_picks,
nba_prop_monitor, mlb_prop_monitor, nba_injury_monitor, mlb_injury_monitor,
nba_clv_tracker, mlb_clv_tracker, game_completion_monitor, check_results, verify.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mine the existing run log for per-task durations and slow-run warnings</name>
  <files>.planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md</files>
  <read_first>
    - data/pnl/logs/run_log.txt (the 15k-line run log — the primary timing-evidence source)
    - .planning/phases/01-diagnosis/01-RESEARCH.md § "Timing Evidence Summary (from run log)" (lines 411–431, the task-duration profile table + cron-budget notes) and § "Evidence: Lead 2 — Stacked subprocess timeout totals" (lines 125–144) and § "Evidence: Root Cause of Extreme Timeout Durations" (lines 184–197)
    - .planning/phases/01-diagnosis/01-CONTEXT.md D-06/D-07/D-10/D-11 (broad sweep; single representative run sufficient; ranked-contributors table; external-first, throwaway instrumentation only if needed) and the timing caveat (python3 3.14 alpha is a variable)
    - ./CLAUDE.md (python3 from scripts/; never hardcode secrets; do not perturb gates/picks/schema)
  </read_first>
  <action>
    Mine `data/pnl/logs/run_log.txt` to build a per-task duration profile for all 11
    runner tasks. Use the grep targets from <interfaces>: extract the `completed in
    {N}s` durations grouped by task, the `consider further optimization` slow-run
    warnings (these are the corroborating evidence per D-07), and the network/Telegram
    stall lines (`Telegram alert failed`, `NameResolutionError`, `nodename nor
    servname`, `Broken pipe`). For each task, record fast / typical-slow / observed-extreme
    durations and note which extremes co-occur in the same time window with a
    network/Telegram stall line (this is the evidence that links the extremes to the
    Telegram-retry / obsidian_sync contributors, not to subprocess timeouts).

    Explicitly evaluate Lead #2 (stacked subprocess timeout totals): compare the
    largest observed `daily_picks` duration against the 1500s theoretical ceiling
    (fetch 300 + hit_rate 600 + projections 600). State whether the ceiling was ever
    reached and therefore whether Lead #2 is CONFIRMED or RULED OUT as the primary
    cause — with the specific duration figures as evidence. Per RESEARCH § "Evidence:
    Lead 2" the observed daily_picks runs are 7–356s nominal with extreme outliers
    (e.g. 7331s nba_daily_picks) explained by lock contention + network stall, NOT by
    the subprocess ceiling; confirm this independently from the log rather than
    restating it.

    Begin writing `.planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md` with: a "##
    Run-Log Duration Profile" section (per-task table with columns Task | Typical fast
    | Typical slow | Observed extreme | Cause of extreme, covering at minimum
    nba_daily_picks, mlb_daily_picks, and mlb_prop_monitor), a "## Slow-Run Warning
    Corroboration" section (count + a short sample of the >90s `consider further
    optimization` warnings), a "## Extreme-Duration / Network-Stall Correlation"
    section (the extremes paired with their co-occurring network/Telegram lines), and a
    "## Lead #2 Disposition" line (CONFIRMED or RULED OUT + the largest-daily_picks-vs-1500s
    ceiling comparison). Do NOT paste secrets or tokens from any log line — log lines
    are operational text but redact any value that looks like a token/cookie/key if
    present. Quote run-log lines verbatim as evidence (with timestamps), keeping
    excerpts short.

    This task is read-only mining — no source edits, no runner invocation yet.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md && grep -q 'Run-Log Duration Profile' .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md && grep -qi 'Lead #2' .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md` exists and contains a "## Run-Log Duration Profile" section with a per-task duration table covering the heavy path (daily_picks nba + mlb) and mlb_prop_monitor at minimum (assertable by grep for the section header + the three task names)
    - The file contains a "## Lead #2 Disposition" statement that names CONFIRMED or RULED OUT and cites the largest observed daily_picks duration vs the 1500s ceiling (assertable by grep for "Lead #2" + "1500")
    - The file cites at least one verbatim run-log excerpt (with a `[YYYY-MM-DDT...]` timestamp) showing an extreme duration co-occurring with a network/Telegram stall line (behavior assertion — visible in the "Extreme-Duration / Network-Stall Correlation" section)
    - No secret/token value appears in any quoted log excerpt — `grep -E 'TELEGRAM_BOT_TOKEN|PRIZEPICKS_COOKIE|ODDS_API_IO_KEY' .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md` returns nothing
  </acceptance_criteria>
  <done>The run-log mining is captured in 01-TIMING-EVIDENCE.md with a per-task duration profile, the >90s slow-run corroboration, the extreme/network correlation, and an evidence-backed Lead #2 disposition naming CONFIRMED/RULED OUT against the 1500s ceiling.</done>
</task>

<task type="auto">
  <name>Task 2: Run representative timed invocations and assemble the ranked-contributors table</name>
  <files>.planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md (the in-progress artifact from Task 1 — you are appending to it)
    - .planning/phases/01-diagnosis/01-RESEARCH.md § "Ranked contributors table (D-10)" (lines 455–463 — the exact table schema and the 5 ranked contributors) and § "DIAGNOSIS.md Shape" Section 2 (lines 447–453) and § "Cron time budget" (lines 429–431, the [ASSUMED] 60–120s window)
    - .planning/phases/01-diagnosis/01-PATTERNS.md § "Subprocess timing pattern — for optional D-11 per-stage instrumentation" (lines 219–235 — the throwaway `t0 = time.time()` idiom IF coarse numbers don't isolate the offender) and § "finally / slow-run warning pattern" (lines 207–217)
    - .planning/phases/01-diagnosis/01-CONTEXT.md D-07/D-10/D-11 (single representative run; ranked table; external-first; throwaway-only instrumentation) and the python3 3.14-alpha timing caveat
    - ./CLAUDE.md (run tasks from scripts/ with python3; JSON_RESULT contract; tasks are defensive — SKIP states not exceptions)
  </read_first>
  <action>
    Run a single representative timed invocation (D-07) for the heavy/relevant tasks
    using `time python3 sports_system_runner.py --task <task>` FROM `scripts/`. At
    minimum time the stacked-subprocess heavy path (`nba_daily_picks` and
    `mlb_daily_picks`) and `mlb_prop_monitor` (the named broken-pipe / alert-fanout
    path). Optionally time additional tasks at your discretion (e.g. a clv_tracker
    and an injury_monitor) if doing so sharpens the ranking. Capture wall-clock
    duration plus the `[{task}] completed in {N}s` line the runner already logs at
    line 5644 — the two should agree. Note any SKIP states (missing games / workbooks
    become explicit SKIPs, not failures — these are normal and make the timed run
    side-effect-light).

    BUDGET PER D-11: collect these coarse external numbers FIRST. ONLY if the coarse
    durations do not isolate the dominant offender (e.g. a task is slow but you cannot
    tell which internal stage owns the time), add TEMPORARY per-stage instrumentation
    around `run_build_hit_rate_db` / `run_generate_projections` / `run_fetch_dfs_props`
    following the PATTERNS.md throwaway idiom (`t0 = time.time()` before
    `subprocess.run(...)`, `log(f"stage X: {time.time()-t0:.1f}s")` after the
    `cp = subprocess.run(...)` line). Any such instrumentation is THROWAWAY — it must
    be reverted before the phase closes (Plan 03's traceability task will confirm only
    the Plan-01 traceback hook remains in source) and is distinct from the kept
    traceback hook in Plan 01. If the coarse numbers DO isolate the offender (the
    expected case per research — the Telegram retry loop dominates), add NO
    instrumentation and record that decision explicitly in the artifact.

    Append to `.planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md`: a "## Representative
    Timed Runs" section (the `time` output + corroborating `completed in` lines per
    task, plus any SKIP states observed), a "## Ranked Contributors" section using the
    D-10 schema (columns Rank | Contributor | Max observed overhead | Occurs when |
    Fix direction) seeded from the RESEARCH table (lines 457–463: send_telegram retry
    loop #1, obsidian_sync per-log-line #2, workbook lock contention #3, bare print in
    run_fetch_dfs_props #4, stacked subprocess timeouts #5) but with THIS machine's
    measured numbers substituted where you collected them, a "## D-11 Instrumentation
    Decision" line recording whether throwaway per-stage instrumentation was added (and
    that it was reverted) or explicitly not needed, and a "## Timing Caveat" line
    stating that python3 is 3.14 ALPHA and the interpreter is therefore a variable when
    interpreting absolute durations. State the cron time budget as best-known (RESEARCH
    § "Cron time budget" notes it is implied by the no_agent pipe closure, ~60–120s of
    quiet stdout) and mark it [ASSUMED] where not explicitly documented.
  </action>
  <verify>
    <automated>grep -q 'Representative Timed Runs' .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md && grep -qi 'Ranked Contributors' .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md && grep -qi 'alpha' .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md && grep -q 'ASSUMED' .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 01-TIMING-EVIDENCE.md contains a "## Representative Timed Runs" section with a timed result for at least nba_daily_picks, mlb_daily_picks, and mlb_prop_monitor, each showing both wall-clock (`time` output) and the runner's `completed in {N}s` line (assertable by grep for the section header + the three task names)
    - 01-TIMING-EVIDENCE.md contains a "## Ranked Contributors" table using the D-10 5-column schema (Rank | Contributor | Max observed overhead | Occurs when | Fix direction) with `send_telegram` retry loop ranked #1 (assertable by grep for the column headers + "send_telegram")
    - A "## D-11 Instrumentation Decision" line records either that no instrumentation was needed OR that throwaway instrumentation was added AND reverted (assertable by grep for "D-11 Instrumentation Decision")
    - A "## Timing Caveat" line names python3 3.14 ALPHA as a variable, and the cron budget is marked [ASSUMED] where not documented (assertable by grep for "alpha" and "ASSUMED")
    - No secret/token value appears anywhere in the file — `grep -E 'TELEGRAM_BOT_TOKEN|PRIZEPICKS_COOKIE|ODDS_API_IO_KEY' .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md` returns nothing
    - The existing suite is still green after any timed runs / throwaway instrumentation revert: `cd scripts && python3 -m pytest -q` passes
  </acceptance_criteria>
  <done>01-TIMING-EVIDENCE.md now has representative timed runs for the heavy path + mlb_prop_monitor, a D-10 ranked-contributors table with measured overhead and send_telegram ranked #1, a recorded (and reverted-if-added) D-11 instrumentation decision, the python3-alpha caveat, and the [ASSUMED] cron budget; the test suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| timed run → real runner tasks | Task 2 invokes the production runner against live tasks; those tasks can fire Telegram alerts to the operator's channel, mutate production workbooks, and (in daily_picks) generate picks — the timing sweep must not produce real betting actions or alert noise beyond normal task behavior |
| run-log excerpts → 01-TIMING-EVIDENCE.md | Mined/quoted log lines are copied into a planning artifact; any token/cookie/key shaped value present in the log must be redacted before it lands in the doc |
| throwaway D-11 instrumentation → scripts/sports_system_runner.py | Optional per-stage timing wrappers temporarily touch production source; they must be reverted before phase close so the only kept source change remains the Plan-01 traceback hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | run-log excerpts copied into 01-TIMING-EVIDENCE.md | mitigate | Redact any token/cookie/key-shaped value when quoting log lines. Both tasks' acceptance criteria grep the artifact for `TELEGRAM_BOT_TOKEN`/`PRIZEPICKS_COOKIE`/`ODDS_API_IO_KEY` and require zero matches. Run-log lines are operational text and do not normally contain secrets, but redaction is enforced as a gate. |
| T-02-02 | Tampering | timed runs invoke real runner tasks that fire Telegram + mutate workbooks | accept | The sweep deliberately times the REAL tasks (D-07 requires a representative run); this matches normal scheduled behavior. Tasks are defensive — missing games/workbooks become SKIP states, keeping the run side-effect-light. No production-mutating CLI flags beyond `--task` are passed; reruns clear their own GENERATED_MARKER rows first per the existing daily-exposure-cap contract. No live betting action exists in any task (the system only emits alerts/notes). Residual alert noise to the operator's own channel is accepted as the cost of a faithful timing measurement. |
| T-02-03 | Tampering | optional throwaway D-11 per-stage instrumentation in sports_system_runner.py | mitigate | Instrumentation is added ONLY if coarse numbers can't isolate the offender (expected: not needed). If added, it is additive `log()` timing only — no gate/pick/schema change — and MUST be reverted before phase close. Task 2 records the decision in a "## D-11 Instrumentation Decision" line; Plan 03's traceability task confirms the Plan-01 traceback hook is the only kept source change. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — mining uses grep/stdlib and the timed runs use the already-installed `python3` + deps. No legitimacy gate required. |
</threat_model>

<verification>
- `test -f .planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md` — the artifact exists
- `grep -q 'Run-Log Duration Profile' ...01-TIMING-EVIDENCE.md` and `grep -q 'Representative Timed Runs' ...` and `grep -qi 'Ranked Contributors' ...` — all required sections present
- `grep -qi 'Lead #2' ...01-TIMING-EVIDENCE.md` returns the CONFIRMED/RULED-OUT disposition with the 1500s ceiling comparison
- `grep -qi 'alpha' ...01-TIMING-EVIDENCE.md` and `grep -q 'ASSUMED' ...` — python3 3.14-alpha caveat present and cron budget marked assumed
- `grep -E 'TELEGRAM_BOT_TOKEN|PRIZEPICKS_COOKIE|ODDS_API_IO_KEY' ...01-TIMING-EVIDENCE.md` returns nothing — no secrets leaked in any excerpt
- The existing suite still passes: `cd scripts && python3 -m pytest -q` (no source perturbation survives; any throwaway D-11 instrumentation was reverted)
</verification>

<success_criteria>
- DIAG-02 timeout source named with evidence: 01-TIMING-EVIDENCE.md names which task/stage/subprocess exceeds the cron budget, backed by mined durations + a representative timed run (maps to ROADMAP § Phase 1 success criterion 2 — "names which task, stage, or subprocess exceeds the cron time budget, supported by timing evidence")
- The stacked-subprocess-timeout lead (Lead #2) is explicitly CONFIRMED or RULED OUT against the 1500s ceiling with run-log figures, and the per-log-line obsidian_sync slowness contributor is ranked in the D-10 table — together these address ROADMAP § Phase 1 success criterion 3 ("the log()/obsidian_sync() per-line subprocess lead and the stacked subprocess timeout totals are confirmed or ruled out as contributors with evidence")
- Findings are presented as a ranked-contributors table (D-10) against the cron budget so Phase 2 can trim more than one stage if the overrun is death-by-a-thousand-cuts
- Minimal-invasive honored: the artifact is a new doc; any throwaway D-11 instrumentation is reverted, leaving only Plan 01's kept traceback hook as the committed source change
</success_criteria>

<output>
Create `.planning/phases/01-diagnosis/01-02-SUMMARY.md` when done, recording: the
dominant timeout contributor named (with its measured duration), the Lead #2
disposition (CONFIRMED/RULED OUT + the largest daily_picks duration vs the 1500s
ceiling), whether any throwaway D-11 instrumentation was added and reverted, and a
pointer to `.planning/phases/01-diagnosis/01-TIMING-EVIDENCE.md` (the ranked-contributors
table) for Plan 03's DIAGNOSIS.md timeout section.
</output>
