---
phase: 04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_projections.py
  - scripts/test_weekly_metrics.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-08: load_calibration_factor has exactly one canonical owner (calibration.py); generate_projections.py imports it and no longer defines a local copy"
    - "D-08: the imported function resolves byte-identically — same CALIBRATION_PATH, same [0.85, 1.20] clamp, same default 1.0 — and behaves identically at projection time (read at call time, not import time)"
    - "METRICS-03 / P4 D-13: the calibration dedup changes no graded verdict and no gate output; the integrity tests still pass"
  artifacts:
    - path: "scripts/generate_projections.py"
      provides: "top-level `from calibration import load_calibration_factor`; local duplicate (lines 277-293) deleted; call site at line 416 unchanged"
      contains: "from calibration import load_calibration_factor"
    - path: "scripts/test_weekly_metrics.py"
      provides: "TestSigmaInjection updated to redirect the calibration source (path=/CALIBRATION_PATH patch) so it still exercises the imported function after dedup"
      contains: "TestSigmaInjection"
  key_links:
    - from: "scripts/generate_projections.py build_projection (line 416)"
      to: "scripts/calibration.py load_calibration_factor"
      via: "module-level import"
      pattern: "from calibration import load_calibration_factor"
---

<objective>
De-duplicate `load_calibration_factor`. `calibration.py` already owns the canonical, richer copy (`load_calibration_factor(sport, path=CALIBRATION_PATH)` at line 247 — has a `path` param, owns the calibration domain). `generate_projections.py` carries an independent duplicate (lines 277-293). This plan deletes the duplicate and replaces it with `from calibration import load_calibration_factor`, preserving byte-identical behavior (same `CALIBRATION_PATH`, `[0.85, 1.20]` clamp, default 1.0), and updates the existing `TestSigmaInjection` tests in `test_weekly_metrics.py` so they keep exercising the now-imported function (D-08, P4 D-13).

Purpose: Close the v2.0 audit's duplicated-`load_calibration_factor` gap with one canonical owner, with zero projection/gate behavior change.
Output: import substitution in `generate_projections.py` + `TestSigmaInjection` redirection in `test_weekly_metrics.py`.

CRITICAL (planner-found, contradicts the "test passes unchanged" note in RESEARCH): the existing `TestSigmaInjection` tests patch `generate_projections.DATA = tmp_path` and expect `self._gp.load_calibration_factor("MLB")` to read `tmp_path/research/calibration.json`. The current LOCAL copy reads `DATA / "research" / "calibration.json"`, so the patch works. After importing `calibration.load_calibration_factor`, the function resolves its path from `calibration.CALIBRATION_PATH` (built from `calibration.ROOT`), NOT from `generate_projections.DATA` — so patching `_gp.DATA` would NO LONGER redirect the read and those 6 tests would read the real `calibration.json` and FAIL. This plan MUST update those tests in the same change (Task 1) so they patch the correct source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-CONTEXT.md
@.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-RESEARCH.md
@.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-PATTERNS.md

<interfaces>
<!-- Verified contracts — no codebase exploration needed. -->

From scripts/calibration.py (CANONICAL owner — do NOT edit):
- ROOT = Path(__file__).resolve().parents[1] (line 22); CALIBRATION_PATH = DATA / "research" / "calibration.json" (line 24)
- CLAMP_LO = 0.85 (line 29); CLAMP_HI = 1.20 (line 30)
- load_calibration_factor(sport: str, path: Path = CALIBRATION_PATH) -> float (line 247):
  reads JSON, raw = factors[sport.upper()] default 1.0, returns max(CLAMP_LO, min(CLAMP_HI, raw)); 1.0 on any miss/error; never raises.
  Accepts a positional `sport` call AND an explicit `path=` override.
- Module-level: NO file I/O at import time (verified) — importing is side-effect-free; imports only workbook_io + stdlib (no circular import with generate_projections).

From scripts/generate_projections.py:
- imports block lines 23-24: from line_timing import LINE_TIMING_FIELDS / from workbook_io import safe_load_workbook, safe_save_workbook — add the new import here.
- BASE = Path.home() / "sports_picks" (line 26); DATA = BASE / "data" (line 27) — used elsewhere; do NOT remove (only the calibration read stops using DATA).
- LOCAL duplicate to DELETE: def load_calibration_factor(sport: str) -> float: lines 277-293 (reads DATA / "research" / "calibration.json", clamps [0.85,1.20], default 1.0).
- Call site (UNCHANGED): line 416 cal_factor = load_calibration_factor(sport) inside build_projection — called at projection time, not import time.

From scripts/test_weekly_metrics.py:
- TestSigmaInjection (line 532): setUp imports generate_projections as _gp, saves self._orig_data = _gp.DATA; tearDown restores _gp.DATA. Helper _write_cal(tmp, factors) writes tmp/research/calibration.json. 6 tests set self._gp.DATA = tmp_path then assert on self._gp.load_calibration_factor(...) (lines 553-595).
- A test asserts getattr(self._gp, "load_calibration_factor", None) is non-None — still satisfied because from calibration import ... binds the name on the gp module.
- Existing tests at lines 449-470 already call calibration.load_calibration_factor("NBA", path=p) with an explicit path= override — mirror this idiom.
- Integrity tests TestIntegrityNoVerdictChange / TestIntegrityGateOutput (METRICS-03) — must remain green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TestSigmaInjection to redirect the calibration source</name>
  <files>scripts/test_weekly_metrics.py</files>
  <read_first>
    - scripts/test_weekly_metrics.py lines 1-67 (importlib header + Workbook fixture), 449-470 (calibration.load_calibration_factor path= tests — the correct redirection idiom), 532-635 (TestSigmaInjection setUp/tearDown/_write_cal + the 6 DATA-patching tests + the module-attribute assertion)
    - scripts/calibration.py lines 22-30, 247-266 (canonical signature + path= override + clamp/default)
    - scripts/generate_projections.py lines 277-293 (the local copy that is about to be deleted) and line 416 (call site)
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-RESEARCH.md Open Question 2 + Pitfall 4 (call-time vs import-time semantics)
  </read_first>
  <action>
    Update TestSigmaInjection in scripts/test_weekly_metrics.py so it still exercises generate_projections.load_calibration_factor correctly AFTER the dedup, when that name is the imported calibration.load_calibration_factor (which resolves its path from calibration.CALIBRATION_PATH, NOT generate_projections.DATA). Pick the cleanest of these two equivalent redirections and apply it consistently across all 6 tests + setUp/tearDown:
    (a) replace the self._gp.DATA = tmp_path patch with a patch of the calibration module path — in setUp: import calibration; self._orig_cal_path = calibration.CALIBRATION_PATH; in each test: calibration.CALIBRATION_PATH = tmp_path / "research" / "calibration.json"; in tearDown restore it — keeping _write_cal(tmp, factors) writing to tmp/research/calibration.json; OR
    (b) call the function with the explicit path override: self._gp.load_calibration_factor("MLB", path=tmp_path / "research" / "calibration.json") in each test, dropping the DATA patch (mirrors the existing idiom at lines 449-470).
    Keep the existing assertions (1.10 MLB, 1.0 missing-sport/corrupt/missing-file, 1.20 upper clamp, 0.85 lower clamp) — the values stay byte-identical; only the path-redirection mechanism changes. Do NOT remove the module-attribute assertion (getattr(self._gp, "load_calibration_factor", None) non-None) — it must still hold after the import binds the name. Use unittest, no pytest fixtures. This task is test-only; it must NOT touch generate_projections.py. Some methods may be RED until Task 2 lands; record which in the SUMMARY.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_weekly_metrics.py::TestSigmaInjection --collect-only -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - TestSigmaInjection no longer relies on `self._gp.DATA = tmp_path` to redirect the calibration read (`grep -A45 "class TestSigmaInjection" scripts/test_weekly_metrics.py | grep -c "_gp.DATA = tmp_path"` returns 0)
    - The redirection uses either calibration.CALIBRATION_PATH patching or an explicit path= override (grep finds `CALIBRATION_PATH` or `path=` within the TestSigmaInjection block)
    - All original numeric assertions preserved (1.10, 1.0, 1.20, 0.85) — grep confirms each constant still present in the block
    - `git diff --name-only` shows ONLY scripts/test_weekly_metrics.py changed in this task
    - Collection succeeds with no import/syntax errors: `cd scripts && python3 -m pytest test_weekly_metrics.py::TestSigmaInjection --collect-only -q` exits 0
  </acceptance_criteria>
  <done>TestSigmaInjection redirects the calibration source via path-override/CALIBRATION_PATH patch (not _gp.DATA), with all numeric assertions intact, so it will correctly exercise the imported function after Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the duplicate and import calibration.load_calibration_factor</name>
  <files>scripts/generate_projections.py</files>
  <read_first>
    - scripts/generate_projections.py lines 8-27 (import block + BASE/DATA), 277-293 (local load_calibration_factor to delete), 416 (call site cal_factor = load_calibration_factor(sport))
    - scripts/calibration.py lines 22-30, 247-266 (canonical function, signature, clamp, default — confirms behavior parity)
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-PATTERNS.md "scripts/generate_projections.py — delete local ... add import" (exact delete + import placement) + "Lazy module import for runner deps" (use top-level import here, NOT __import__)
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-RESEARCH.md Pattern 3 + Pitfall 4 + Anti-Patterns (no lazy __import__, no import-time read)
  </read_first>
  <action>
    In scripts/generate_projections.py: (1) add `from calibration import load_calibration_factor` to the existing sibling-import block at lines 23-24 (alongside from line_timing import ... / from workbook_io import ...) — top-level import, NOT a lazy __import__ (subprocess from scripts/, calibration.py on sys.path, confirmed no circular import). (2) Delete the entire local definition at lines 277-293 (def load_calibration_factor(sport: str) -> float: through its return 1.0). (3) Leave the call site at line 416 (cal_factor = load_calibration_factor(sport)) unchanged — the canonical signature load_calibration_factor(sport, path=CALIBRATION_PATH) accepts this positional call. (4) Do NOT remove BASE/DATA (used elsewhere in the file). Do NOT alter estimate_sigma, model_over_probability, the sigma-injection math, or any projection/gate behavior (P4 D-13). The change must be byte-identical in behavior: same path (data/research/calibration.json), same [0.85, 1.20] clamp, same default 1.0, read at call time (build_projection), not import time.
  </action>
  <verify>
    <automated>cd scripts && python3 -c "import generate_projections as g; print(round(g.load_calibration_factor('MLB'),4), round(g.load_calibration_factor('NBA'),4))" && python3 -m pytest test_weekly_metrics.py::TestSigmaInjection test_weekly_metrics.py::TestIntegrityNoVerdictChange test_weekly_metrics.py::TestIntegrityGateOutput -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from calibration import load_calibration_factor" scripts/generate_projections.py` returns 1
    - The local def is gone: `grep -c "^def load_calibration_factor" scripts/generate_projections.py` returns 0
    - Import-time safety: `cd scripts && python3 -c "import generate_projections"` exits 0 (no circular import, no import-time crash)
    - Call site unchanged: `grep -c "cal_factor = load_calibration_factor(sport)" scripts/generate_projections.py` returns 1
    - `cd scripts && python3 -m pytest test_weekly_metrics.py::TestSigmaInjection -x -q` exits 0 (GREEN with the Task-1 redirection)
    - METRICS-03 / D-13 integrity intact: `cd scripts && python3 -m pytest test_weekly_metrics.py::TestIntegrityNoVerdictChange test_weekly_metrics.py::TestIntegrityGateOutput -x -q` exits 0
    - BASE/DATA still defined: `grep -c "^DATA = " scripts/generate_projections.py` returns 1
  </acceptance_criteria>
  <done>generate_projections.py imports the canonical load_calibration_factor, the local duplicate is deleted, the call site is unchanged, import-time is side-effect-free, TestSigmaInjection is GREEN, and the METRICS-03/D-13 integrity tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess import-time -> generate_projections.py | adding a top-level import of calibration.py could, in principle, run module-level code at subprocess start |
| filesystem -> calibration read | calibration.json drives the sigma scaler used at projection time (read-only consumer; clamped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-04 | Tampering | calibration.py module-level side effects at subprocess import | mitigate | confirmed (RESEARCH A4, importlib load) calibration.py has NO module-level I/O — CALIBRATION_PATH is a Path construction only; import is side-effect-free; no circular import (calibration imports only workbook_io + stdlib) |
| T-04.1-05 | Tampering | out-of-range / corrupt calibration.json changes projection -> gate -> verdict | mitigate | canonical load_calibration_factor clamps to [0.85,1.20] and returns 1.0 on any miss/error (V5 input validation); read happens at call time inside build_projection (Pitfall 4), not cached at import; METRICS-03 integrity tests assert no verdict/gate-output change |
| T-04.1-SC | Tampering | npm/pip/cargo installs | accept | no packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); calibration.py is existing P4-tested in-repo code |

ASVS V2/V3/V4/V6 are N/A (no auth, session, access-control, or crypto surface). V5 input validation is satisfied by the [0.85,1.20] clamp and 1.0 default already in the canonical function.
</threat_model>

<verification>
- `cd scripts && python3 -c "import generate_projections"` exits 0 (import-time safe)
- `cd scripts && python3 -m pytest test_weekly_metrics.py::TestSigmaInjection test_weekly_metrics.py::TestIntegrityNoVerdictChange test_weekly_metrics.py::TestIntegrityGateOutput -x -q` exits 0
- No second definition of load_calibration_factor anywhere in generate_projections.py
- No change to projection/gate math; calibration read still at call time
- Subprocess stage adds one import — negligible runtime, well under the 660s budget
</verification>

<success_criteria>
- Calibration de-duplicated: load_calibration_factor resolves identically via calibration.py and the now-importing generate_projections.py (D-08)
- Behavior byte-identical: same CALIBRATION_PATH, [0.85,1.20] clamp, default 1.0, read at call time
- METRICS-03 / P4 D-13 preserved: no graded verdict or gate output changes, confirmed by the integrity tests
- TestSigmaInjection updated to keep exercising the imported function (planner-found break averted)
</success_criteria>

<output>
Create `.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-02-SUMMARY.md` when done
</output>
