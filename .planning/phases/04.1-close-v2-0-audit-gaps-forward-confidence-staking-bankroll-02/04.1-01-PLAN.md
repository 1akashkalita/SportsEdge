---
phase: 04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_slips.py
  - scripts/test_build_slips.py
autonomous: true
requirements: [BANKROLL-02]
must_haves:
  truths:
    - "BANKROLL-02 / D-01: a daily slip build applies confidence stakes and no longer emits the flat stake_units=1.0 placeholder when bankroll.json is present"
    - "D-02/D-03: a high-probability +EV slip is staked at 2.5% of bankroll.json current_bankroll (start-of-day snapshot); a sub-0.58-prob or EV<=0 slip is staked 0; rebuild_bankroll remains the authoritative re-stating"
    - "D-04: with bankroll.json missing/unreadable/zero, the build falls back to the literal stake_units=1.0, logs a warning, never stakes 0, never crashes"
    - "D-05: grade_slips.py reads stake_units verbatim with no grading-code change"
  artifacts:
    - path: "scripts/build_slips.py"
      provides: "main() reads bankroll.json current_bankroll and applies apply_confidence_stakes per slip category before write_outputs"
      contains: "apply_confidence_stakes"
    - path: "scripts/test_build_slips.py"
      provides: "TestForwardStaking covering real stake, zero-stake, and missing-bankroll fallback"
      contains: "class TestForwardStaking"
  key_links:
    - from: "scripts/build_slips.py main()"
      to: "scripts/stake_sizing.py apply_confidence_stakes"
      via: "per-category stake application before write_outputs"
      pattern: "apply_confidence_stakes"
    - from: "scripts/build_slips.py main()"
      to: "data/pnl/bankroll.json"
      via: "current_bankroll read at build time (D-02)"
      pattern: "current_bankroll"
---

<objective>
Wire forward confidence staking into the daily slip-build path so BANKROLL-02 is live. Today `build_slips.py make_slip()` hardcodes `stake_units: 1.0` (line 450); the already-built, already-tested `stake_sizing.apply_confidence_stakes(slips, start_of_day_bankroll)` is never called on the daily path, so every slip ships a flat-1.0 placeholder. This plan reads `bankroll.json current_bankroll` inside `build_slips.main()` and applies the existing P3 stake formula per slip category before `write_outputs`, with a money-safe literal-1.0 fallback (D-04).

Purpose: Close the v2.0 audit's BANKROLL-02 forward-path BLOCKER — confidence stakes go live, no more placeholder, on a live real-money write path.
Output: `build_slips.py` staking wiring + `test_build_slips.py::TestForwardStaking`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-CONTEXT.md
@.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-RESEARCH.md
@.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Do NOT explore the codebase — these are verified. -->

From scripts/stake_sizing.py:
- confidence_stake(combined_probability, combined_ev_score, start_of_day_bankroll) -> float
  Formula (already implements P3 D-03/D-04/D-05): EV<=0 -> 0.0; prob<0.58 -> 0.0;
  prob>=0.75 -> round(0.025*bankroll,4); prob>=0.65 -> round(0.015*bankroll,4);
  else (0.58<=prob<0.65) -> round(0.0075*bankroll,4). Every return wrapped in max(0.0, ...).
- apply_confidence_stakes(slips: list[dict], start_of_day_bankroll: float) -> list[dict]
  Returns NEW list of NEW dicts (input never mutated); each has 'stake_units' set from
  confidence_stake(). Missing/None combined_probability and combined_ev_score are coerced
  to 0.0 via float(x or 0) — never raises. All 6 slip categories are eligible (D-01).

From scripts/build_slips.py:
- ROOT = Path(__file__).resolve().parents[1] (line 23) — already defined; reuse it.
- make_slip(...) sets "stake_units": 1.0 (line 450) — this is the placeholder to overwrite.
- build_slips(projections, correlation_payload, date, vetted_source=...) -> dict
  returns payload where payload["slips"] is dict[str, list[dict]] (category -> list of slip dicts).
  Each slip dict carries combined_probability and combined_ev_score.
- main() (line 630): payload = build_slips(...) then json_path, md_path = write_outputs(payload, date) (line 648).
  The stake application MUST be inserted between these two calls.

From scripts/grade_slips.py:
- line 320: stake_units = float(slip.get("stake_units") or 1.0) — reads verbatim. DO NOT EDIT (D-05).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — write TestForwardStaking (RED)</name>
  <files>scripts/test_build_slips.py</files>
  <read_first>
    - scripts/test_build_slips.py (existing BuildSlipsTests pattern at line 37: projection() helpers, build_slips.build_slips(...) call, payload['slips'][category][0] indexing)
    - scripts/build_slips.py lines 23-25 (ROOT/path constants), 419-460 (make_slip), 552-620 (build_slips), 630-660 (main, write_outputs call)
    - scripts/stake_sizing.py lines 25-102 (confidence_stake + apply_confidence_stakes — the exact formula and behavior under test)
    - scripts/test_weekly_metrics.py lines 532-602 (TestSigmaInjection setUp/tearDown + tempfile.TemporaryDirectory + path-patch idiom to copy)
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    Add a TestForwardStaking(unittest.TestCase) class to scripts/test_build_slips.py, following the existing BuildSlipsTests projection/build_slips idiom (line 37) and the TestSigmaInjection tempfile + path-patch idiom from test_weekly_metrics.py:532. The tests must drive the staking behavior that Task 2 implements in build_slips.main(). Because the bankroll read happens inside main(), patch the module-level path the implementation reads — patch build_slips.ROOT (or whatever bankroll-path the implementation derives from ROOT) to a tempfile.TemporaryDirectory() in setUp/tearDown, and write a data/pnl/bankroll.json fixture under it per test. Invoke build_slips.main() via its CLI/argv path (use a temp date and the existing projection/correlation fixtures the file already builds) and then read the resulting slips_<date>.json, OR call the staking step the way Task 2 factors it — coordinate with Task 2 so the test exercises the real main() code path that writes stake_units to the output JSON (Pitfall 1: the JSON file is the build->grade contract). Three required test methods:
    1. test_high_prob_slip_gets_real_stake — fixture bankroll.json with current_bankroll (e.g. 100.0); a slip with combined_probability >= 0.75 and combined_ev_score > 0 ends up with stake_units == round(0.025 * 100.0, 4) (2.5% tier, D-02/D-03), NOT 1.0.
    2. test_low_prob_or_negative_ev_gets_zero — a slip with combined_probability < 0.58 (or combined_ev_score <= 0) ends up with stake_units == 0.0.
    3. test_missing_bankroll_fallback — no bankroll.json present (or unreadable/zero); every slip retains the literal stake_units == 1.0 (D-04 literal fallback — assert NOT 0.025), and main() does not raise.
    Do NOT weaken assertions to make them pass against current code — they MUST fail now (current main() ships flat 1.0). Use unittest.TestCase, no pytest fixtures. Keep the file runnable both via python3 test_build_slips.py and python3 -m pytest.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_build_slips.py::TestForwardStaking -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TestForwardStaking" scripts/test_build_slips.py` returns 1
    - The class defines exactly the three methods named above (`grep -c "def test_high_prob_slip_gets_real_stake\|def test_low_prob_or_negative_ev_gets_zero\|def test_missing_bankroll_fallback" scripts/test_build_slips.py` returns 3)
    - `cd scripts && python3 -m pytest test_build_slips.py::TestForwardStaking -x -q` exits NON-ZERO (RED) before Task 2 — failures are assertion failures (real stake / 0.0 / literal-1.0 expectations), NOT import/collection errors
    - No edits to scripts/build_slips.py in this task
  </acceptance_criteria>
  <done>TestForwardStaking exists with 3 failing tests that pin the D-01/D-02/D-03/D-04 stake contract; failures are assertion failures, not collection errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire forward staking into build_slips.main() (GREEN)</name>
  <files>scripts/build_slips.py</files>
  <read_first>
    - scripts/build_slips.py lines 23-25 (ROOT), 247 (existing json.loads(path.read_text()) idiom), 419-460 (make_slip line 450 placeholder), 600-620 (payload["slips"].items() iteration already used), 630-660 (main, write_outputs at line 648)
    - scripts/stake_sizing.py lines 71-102 (apply_confidence_stakes — returns new list of new dicts, never mutates input, coerces missing signals to 0.0)
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-PATTERNS.md "scripts/build_slips.py — modify main()" (exact wire-in block) + "Defensive JSON read with fallback"
    - .planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-RESEARCH.md Pattern 1 + Pitfalls 1, 2, 6 + Anti-Patterns
  </read_first>
  <behavior>
    - bankroll.json present with current_bankroll>0: each slip's stake_units = confidence_stake(prob, ev, current_bankroll); high-prob +EV -> 2.5% tier; <0.58 or EV<=0 -> 0.0
    - bankroll.json missing / unreadable / current_bankroll<=0: every slip keeps the literal stake_units=1.0 (apply_confidence_stakes is NOT called); a WARNING line is printed; no exception
    - payload["slips"] dict[str,list] structure is preserved (staking applied per category, not by flattening)
  </behavior>
  <action>
    In build_slips.main(), between payload = build_slips(...) and json_path, md_path = write_outputs(payload, date) (line 648), insert the bankroll read + per-category stake application from PATTERNS.md. Add from stake_sizing import apply_confidence_stakes (top-of-file import block alongside the other sibling imports is fine; a local import inside main() also acceptable — match existing file convention). Read ROOT / "data" / "pnl" / "bankroll.json" (reuse the existing ROOT at line 23 — no new constant), using the defensive json.loads(path.read_text(encoding="utf-8")) idiom wrapped in try/except. Set start_of_day_bankroll ONLY when the file exists, parses, and current_bankroll > 0; on any other case (missing file, parse error, value<=0) print a [build_slips] WARNING: ... using fallback stake_units=1.0 line and leave start_of_day_bankroll unset. When set, iterate payload["slips"].items() and reassign payload["slips"][category] = apply_confidence_stakes(slip_list, start_of_day_bankroll) per category (Pitfall 2: it is dict[str,list], never pass the whole dict). When unset, do nothing — make_slip's literal stake_units=1.0 stays (D-04, Pitfall 6: do NOT call apply_confidence_stakes(slips, 1.0), which would yield 0.025). Do NOT touch make_slip line 450, write_outputs, gate logic, or any verdict. No schema change. Confirm grade_slips.py:320 is untouched (D-05).
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_build_slips.py::TestForwardStaking -x -q && python3 -m pytest test_build_slips.py test_stake_sizing.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 -m pytest test_build_slips.py::TestForwardStaking -x -q` exits 0 (GREEN)
    - `cd scripts && python3 -m pytest test_build_slips.py test_stake_sizing.py -x -q` exits 0 (no regression in existing build/stake tests)
    - `grep -c "apply_confidence_stakes" scripts/build_slips.py` >= 1; `grep -c "current_bankroll" scripts/build_slips.py` >= 1
    - `grep -nv '^#' scripts/build_slips.py | grep -c '"stake_units": 1.0'` is still exactly 1 (the make_slip default is the only literal 1.0; the daily path overwrites it via apply_confidence_stakes)
    - `git diff scripts/grade_slips.py` is empty (D-05: no grading change)
    - No fenced gate-logic or verdict edits; data/research/slips/*.json schema unchanged (stake_units is an existing key)
  </acceptance_criteria>
  <done>build_slips.main() applies real confidence stakes per category from bankroll.json before write_outputs; falls back to literal 1.0 with a logged warning on any read failure; all three TestForwardStaking tests pass and no existing build/stake test regresses; grade_slips untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> build_slips.main() | bankroll.json (current_bankroll) is read and drives a real-money stake amount written to slips_<date>.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | corrupt/zero bankroll.json -> bad stake | mitigate | D-04 read block requires current_bankroll > 0; any miss (absent/corrupt/<=0) falls back to literal stake_units=1.0 and logs — never stakes 0 (Pitfall 6), never crashes; confidence_stake also wraps every return in max(0.0, ...) |
| T-04.1-02 | Tampering | slips_<date>.json stake written then mis-read downstream | accept | grade_slips.py:320 reads stake_units verbatim (D-05, unchanged); ensure_slip_defs only invokes build when the file is absent — idempotent; the JSON file is the single build->grade contract |
| T-04.1-03 | Information Disclosure / Repudiation | stake amounts in slips JSON | accept | local-only artifacts, no PII, single-operator machine; no new exposure |
| T-04.1-SC | Tampering | npm/pip/cargo installs | accept | no packages installed this phase (RESEARCH.md Package Legitimacy Audit: N/A); apply_confidence_stakes/confidence_stake are existing in-repo, P3-tested code |

ASVS V2/V3/V4/V6 are N/A (no auth, session, access-control, or crypto surface). V5 input validation is satisfied by the current_bankroll > 0 guard and confidence_stake's max(0.0, ...) clamp.
</threat_model>

<verification>
- `cd scripts && python3 -m pytest test_build_slips.py::TestForwardStaking -x -q` exits 0
- `cd scripts && python3 -m pytest test_build_slips.py test_stake_sizing.py -x -q` exits 0
- `git diff scripts/grade_slips.py` is empty
- The make_slip literal "stake_units": 1.0 remains the only such literal; the daily build path overwrites it via apply_confidence_stakes
- A single daily build invocation completes well under the 660s cron budget (one extra JSON read + in-memory loop — negligible)
</verification>

<success_criteria>
- BANKROLL-02 live: a daily build with bankroll.json present produces confidence stakes (high-prob +EV -> 2.5% × current_bankroll; <0.58/EV<=0 -> 0) and never the flat 1.0 placeholder (D-01/D-02/D-03)
- Money-safe fallback: missing/unreadable/zero bankroll.json -> literal stake_units=1.0, logged, no crash (D-04)
- No grading change (D-05); rebuild_bankroll remains authoritative re-stating (D-03)
- No gate-logic / verdict / schema changes; stays under 660s budget
</success_criteria>

<output>
Create `.planning/phases/04.1-close-v2-0-audit-gaps-forward-confidence-staking-bankroll-02/04.1-01-SUMMARY.md` when done
</output>
