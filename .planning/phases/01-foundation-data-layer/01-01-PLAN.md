---
phase: 01-foundation-data-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/test_dashboard.py
  - scripts/test_dashboard_data.py
autonomous: true
requirements: [DASH-02]
must_haves:
  truths:
    - "Flask imports, binds 127.0.0.1, and a self-GET returns 200 on system python3 3.14.0a2 (verify-first gate, D-06 — confirms the Flask tech choice before any code is built on it; stdlib fallback stays documented-only)"
    - "Both dashboard test files exist and import their (not-yet-built) modules — every behavioral test is RED, naming the exact module it expects"
  artifacts:
    - path: "scripts/test_dashboard.py"
      provides: "DASH-01/02/03 tests: flask_serves (live bind+GET 200), route_index, loopback_only"
      contains: "def test_flask_serves"
      min_lines: 60
    - path: "scripts/test_dashboard_data.py"
      provides: "DASH-04 + D-01/D-02 tests: read_only_untouched, lock_tolerant, missing_is_empty, today_matches_runner, write_in_progress"
      contains: "def test_read_only_untouched"
      min_lines: 80
  key_links:
    - from: "scripts/test_dashboard.py"
      to: "flask"
      via: "import flask + app.test_client / real bind"
      pattern: "import flask"
    - from: "scripts/test_dashboard_data.py"
      to: "dashboard_data"
      via: "bare sibling import (run from scripts/)"
      pattern: "import dashboard_data"
---

<objective>
Create the Wave-0 test scaffolds and the verify-first gating assertion for the dashboard.

Purpose: Nyquist requires every behavioral task to have an automated `<verify>` before the
behavior is built. This plan creates both `unittest` files (RED — they import modules that do
not exist yet) AND proves the single phase-gating invariant up front: Flask imports, binds
`127.0.0.1`, and serves a 200 on the system `python3` (3.14.0a2). If that gate ever regresses,
the whole tech choice (D-06) is invalidated before any code is built on it.

Output: `scripts/test_dashboard.py` and `scripts/test_dashboard_data.py`. The `flask_serves`
test passes immediately (Flask is already installed and live-verified per RESEARCH §Summary);
all other tests are RED until plans 02 and 03 build the modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md
@.planning/phases/01-foundation-data-layer/01-VALIDATION.md

<interfaces>
<!-- Test bootstrap analog — copy this sys.path pattern verbatim. From scripts/test_def02_path_resolution.py:12-20 -->
SCRIPT_DIR = Path(__file__).resolve().parent
if str(SCRIPT_DIR) not in sys.path:
    sys.path.insert(0, str(SCRIPT_DIR))
import dashboard_data            # bare sibling import — runs from scripts/
# End every file with: if __name__ == "__main__": unittest.main()  (test_metrics_report.py:500-501)

<!-- "today" contract the data layer must match. From scripts/sports_system_runner.py:334 -->
def today_str() -> str:
    return datetime.now().strftime("%Y-%m-%d")   # naive LOCAL time, NO ZoneInfo

<!-- Read-only byte-preservation proof (DASH-04). From 01-RESEARCH.md Code Examples — verified live -->
before_mtime = path.stat().st_mtime
before_hash  = hashlib.sha256(path.read_bytes()).hexdigest()
# ... read_only=True load + iter_rows ...
assert path.stat().st_mtime == before_mtime
assert hashlib.sha256(path.read_bytes()).hexdigest() == before_hash

<!-- Cooperative-lock JSON shape the badge reads. From scripts/workbook_io.py:90 -->
{"pid": <int>, "path": <str>, "acquired_at": <iso>}   # files at locks/*.xlsx.lock; stale_seconds=600
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/test_dashboard.py (DASH-01/02/03) — verify-first Flask gate + route + loopback</name>
  <files>scripts/test_dashboard.py</files>
  <read_first>
    - scripts/test_def02_path_resolution.py (lines 1-25 — the canonical sys.path bootstrap + bare sibling import; copy it)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (§Code Examples "Minimal verified serve"; §Validation Architecture → Phase Requirements → Test Map; §Architecture Pattern 1)
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md (§"scripts/test_dashboard.py" — bootstrap exact, route/bind net-new via test_client + socket.getsockname)
  </read_first>
  <action>
    Create scripts/test_dashboard.py as a unittest file with the standard scripts/ bootstrap
    (sys.path.insert(0, SCRIPT_DIR) then bare imports), ending with
    `if __name__ == "__main__": unittest.main()`.

    Define exactly these three test methods so VALIDATION.md `-k` names resolve:
    - `test_flask_serves` (DASH-02, the gating invariant — must PASS now): import flask, build a
      tiny Flask app with one route returning a 200, start it on `127.0.0.1` on an ephemeral port
      in a background thread (or use werkzeug's make_server), issue a self-GET via urllib/requests,
      assert status 200 and the rendered body is present, then shut the server down. Use
      `importlib.metadata.version("flask")` if asserting a version — NEVER `flask.__version__`
      (removed in Flask 3.2, per RESEARCH §State of the Art). This test proves D-06's tech choice
      live and re-asserts it on every run.
    - `test_route_index` (DASH-01 — RED until plan 03): `import dashboard` then use
      `dashboard.app.test_client()` to GET `/` and assert status_code == 200. This import fails now
      (no dashboard.py yet) — that is the intended RED state.
    - `test_loopback_only` (DASH-03 — RED until plan 03): bind the dashboard's server intent to
      127.0.0.1 and assert the bound socket's `getsockname()[0] == "127.0.0.1"` (loopback only),
      proving the server is NOT bound to `0.0.0.0`/`""`. Reference `dashboard` for the host constant
      it must expose (e.g. assert `dashboard.HOST == "127.0.0.1"`). RED until plan 03 defines it.

    Do not implement dashboard.py here. The two RED tests must reference the real module/attribute
    names plan 03 will create (`dashboard.app`, `dashboard.HOST`) so they bind without edits later.
  </action>
  <verify>
    <automated>cd scripts && python3 test_dashboard.py -k flask_serves 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 test_dashboard.py -k flask_serves` exits 0 (the gating invariant passes live).
    - `cd scripts && python3 test_dashboard.py -k route_index` and `-k loopback_only` FAIL with an
      ImportError/AttributeError on `dashboard` (intended RED — module not built yet).
    - File contains `def test_flask_serves`, `def test_route_index`, `def test_loopback_only`, the
      `sys.path.insert(0, SCRIPT_DIR)` bootstrap, and `if __name__ == "__main__": unittest.main()`.
    - No occurrence of `flask.__version__` (uses importlib.metadata if version is checked).
    - No `host="0.0.0.0"` and no `host=""` anywhere in the file.
  </acceptance_criteria>
  <done>flask_serves passes live (DASH-02 gate proven); route_index and loopback_only are RED against the named dashboard module attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/test_dashboard_data.py (DASH-04 + D-01/D-02) — read-only, lock-tolerant, today-match, badge RED tests</name>
  <files>scripts/test_dashboard_data.py</files>
  <read_first>
    - scripts/test_metrics_report.py (lines 26-50 build a sheet from *_HEADERS; lines 65-91 TemporaryDirectory + module-dir override + restore in finally — copy this fixture idiom)
    - scripts/test_def02_path_resolution.py (lines 12-20 — sys.path bootstrap)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (§Code Examples "Read-only proves source untouched"; §Architecture Patterns 2 & 3; §Common Pitfalls 1-4)
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md (§"scripts/test_dashboard_data.py" Analog A/B + net-new asserts)
  </read_first>
  <action>
    Create scripts/test_dashboard_data.py as a unittest file with the same scripts/ bootstrap, a
    bare `import dashboard_data` (RED now — module built in plan 02), and
    `if __name__ == "__main__": unittest.main()`.

    Define exactly these five test methods (the VALIDATION.md `-k` names):
    - `test_read_only_untouched` (DASH-04): build a synthetic .xlsx in a TemporaryDirectory from a
      *_HEADERS constant + sample rows; capture `st_mtime` and `sha256` of the bytes before; call the
      dashboard_data sheet-reader; assert the file's mtime AND sha256 are byte-identical after the
      read (proves the read never writes). Copy the verified assert pattern from RESEARCH Code Examples.
    - `test_lock_tolerant` (DASH-04 / D-01): force the workbook read to hit `WorkbookAccessError`
      (e.g. monkeypatch `dashboard_data.safe_load_workbook` to raise `WorkbookAccessError`, or point
      the reader at a path that triggers it) and assert the accessor returns last-known-good /
      `None`/`[]` WITHOUT raising. Import `WorkbookAccessError` from `workbook_io`.
    - `test_missing_is_empty` (DASH-04): call the JSON reader and the sheet reader against a
      non-existent path; assert each returns the empty/`None` state and raises nothing.
    - `test_today_matches_runner` (D-02): assert `dashboard_data`'s "today" value equals
      `datetime.now().strftime("%Y-%m-%d")` (naive local) — i.e. it must NOT use ZoneInfo. Assert the
      dashboard_data source has no `zoneinfo`/`ZoneInfo` import (read its source text and check), so a
      midnight mismatch with the pipeline can't be introduced.
    - `test_write_in_progress` (D-01): point the badge function at a temp lock dir; write a lock file
      with the live `os.getpid()` and a fresh mtime → assert `write_in_progress()` is True; write a
      lock with a dead/bogus pid OR an mtime older than 600s → assert False. Mirror the
      `{"pid","path","acquired_at"}` shape and the 600s staleness threshold from workbook_io.

    Use TemporaryDirectory + module-constant override (restore in `finally`) for any path the reader
    anchors on (lock dir, data dirs) — exactly the test_metrics_report.py idiom. Do not implement
    dashboard_data.py here.
  </action>
  <verify>
    <automated>cd scripts && python3 test_dashboard_data.py 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 test_dashboard_data.py` FAILS at collection/import time with an
      ImportError on `dashboard_data` (intended RED — module not built yet).
    - File contains `def test_read_only_untouched`, `def test_lock_tolerant`,
      `def test_missing_is_empty`, `def test_today_matches_runner`, `def test_write_in_progress`.
    - File imports `WorkbookAccessError` from `workbook_io` and uses `TemporaryDirectory` for fixtures.
    - `read_only_untouched` asserts BOTH `st_mtime` unchanged AND `sha256` unchanged.
    - File contains the bootstrap and `if __name__ == "__main__": unittest.main()`.
  </acceptance_criteria>
  <done>All five DASH-04/D-01/D-02 tests exist, are RED against the named dashboard_data module, and reference the verified mtime+sha256, WorkbookAccessError, and lock-liveness contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → Flask dev server | Only loopback (127.0.0.1) traffic should ever reach the app; the bind is the boundary |
| workbook/JSON files → read layer | Untrusted-shaped file contents (locked, partial, missing) cross into the reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | Flask bind host (`test_flask_serves`/`test_loopback_only`) | mitigate | Tests assert bind to `127.0.0.1`; forbid `0.0.0.0`/`""` in test_dashboard.py (grep gate in acceptance) — locks DASH-03 in at the test layer up front |
| T-1-02 | Tampering | dashboard read path | mitigate | `test_read_only_untouched` asserts source mtime+sha256 unchanged after a read — proves no accidental write |
| T-1-SC | Tampering | python/pip installs | accept | No new package installs in this plan — Flask + deps already installed and audited (RESEARCH §Package Legitimacy Audit, all Pallets/openpyxl, Approved). No install task → no legitimacy checkpoint needed |
</threat_model>

<verification>
- `cd scripts && python3 test_dashboard.py -k flask_serves` → exit 0 (gating invariant proven).
- `cd scripts && python3 test_dashboard.py` and `python3 test_dashboard_data.py` → the non-gate tests
  are RED (ImportError on the not-yet-built modules) — confirming Nyquist RED-first state.
- No regression to the rest of the suite (these are new files; baseline stays "2 failed, 202 passed").
</verification>

<success_criteria>
- Both test files exist in scripts/ with the exact `-k` method names from VALIDATION.md.
- `flask_serves` passes live on python3 3.14.0a2 (DASH-02 verify-first gate green).
- All behavioral tests are RED and bound to the real module/attribute names plans 02/03 will create.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-01-SUMMARY.md` when done.
</output>
