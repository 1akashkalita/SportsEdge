---
phase: 01-foundation-data-layer
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - scripts/dashboard_data.py
autonomous: true
requirements: [DASH-04]
must_haves:
  truths:
    - "The data layer reads existing workbooks (read_only=True, data_only=True) and JSON artifacts and returns data without writing — source mtime + sha256 are unchanged after a read"
    - "When a workbook is locked / atomically swapped mid-write, a read returns last-known-good (None/[]) and never raises (D-01)"
    - "Missing workbook/JSON returns an empty state, never an exception"
    - "'today' matches the runner's today_str() exactly — naive local time, no ZoneInfo (D-02)"
    - "The 'updating…' badge is True only when a live pipeline pid holds a fresh (<600s) cooperative lock; dead/stale pids read as not-in-progress (D-01)"
  artifacts:
    - path: "scripts/dashboard_data.py"
      provides: "Read-only data layer: read_json, read_sheet_rows (lock-tolerant), today_str-equivalent, write_in_progress, last_updated_hhmm"
      exports: ["read_json", "read_sheet_rows", "write_in_progress", "last_updated_hhmm"]
      min_lines: 80
  key_links:
    - from: "scripts/dashboard_data.py"
      to: "workbook_io.safe_load_workbook"
      via: "lock-aware read_only load with WorkbookAccessError tolerance"
      pattern: "safe_load_workbook\\(.*read_only=True"
    - from: "scripts/dashboard_data.py"
      to: "locks/*.xlsx.lock"
      via: "pid liveness (os.kill(pid,0)) + age<600 for the badge"
      pattern: "os\\.kill"
---

<objective>
Build the read-only data layer `scripts/dashboard_data.py` — the foundation both Phase 2 views
and the Phase 1 shell consume. It reads persisted data without ever modifying it and tolerates a
workbook locked mid-write, serving last-known-good complete data with an "updating…" signal
instead of blocking or raising (D-01). Delivers DASH-04.

Purpose: Isolates ALL file I/O + lock tolerance from the web app, so the read contract is proven
once and reused. The risky parts (lock tolerance, atomic-IO awareness, "today" semantics) reuse
vetted repo primitives — the work is wiring, not building new machinery.

Output: `scripts/dashboard_data.py` exporting JSON-first + lock-tolerant sheet readers, a
today-date matching the pipeline, and the freshness/badge functions. Makes the Wave-0
`test_dashboard_data.py` tests green (read_only_untouched, lock_tolerant, missing_is_empty,
today_matches_runner, write_in_progress).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md

@scripts/workbook_io.py
@scripts/metrics_report.py

<interfaces>
<!-- The lock-aware load to CALL (do NOT bare load_workbook). From scripts/workbook_io.py:120 -->
def safe_load_workbook(path: Path, retries: int = 5, delay: float = 1.0, **kwargs): ...
    # raises WorkbookAccessError after retries on a mid-swap race
class WorkbookAccessError(RuntimeError): ...        # workbook_io.py:30
# cooperative-lock JSON written at workbook_io.py:90:
#   {"pid": os.getpid(), "path": str(path), "acquired_at": now_iso()}
# stale_seconds default = 600 (workbook_io.py:80)

<!-- The production read idiom to mirror (try/except → continue = last-known-good). From metrics_report.py:130-141 -->
try:
    wb = safe_load_workbook(wb_path, read_only=True, data_only=True)
except Exception:
    continue                       # locked/unreadable → last-known-good, never raises (D-01)
if "Slip History" not in wb.sheetnames:
    continue
ws = wb["Slip History"]
for row_vals in ws.iter_rows(min_row=2, values_only=True): ...

<!-- Portable path constants — anchor on Path.home(), never hardcode a username (test_def02 guard). From runner:52-59 / workbook_io:22-25 -->
HOME = Path.home(); ROOT = HOME / "sports_picks"; DATA = ROOT / "data"
NBA_DIR = DATA / "nba"; MLB_DIR = DATA / "mlb"; LOCK_DIR = ROOT / "locks"
# run_log lives at: DATA / "pnl" / "logs" / "run_log.jsonl"

<!-- "today" — mirror exactly, NO ZoneInfo. From sports_system_runner.py:334 -->
def today_str() -> str:
    return datetime.now().strftime("%Y-%m-%d")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the JSON-first + lock-tolerant read accessors (read_json, read_sheet_rows) and today-date</name>
  <files>scripts/dashboard_data.py</files>
  <read_first>
    - scripts/workbook_io.py (safe_load_workbook :120-144; WorkbookAccessError :30-31; lock JSON shape :90; stale_seconds :80)
    - scripts/metrics_report.py (lines 130-165 — try/except-continue around safe_load_workbook + sheetnames check + iter_rows)
    - scripts/sports_system_runner.py (lines 52-59 path constants; line 334 today_str)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (§Architecture Pattern 2; §Common Pitfalls 2/3/4)
    - scripts/test_dashboard_data.py (the Wave-0 tests this must satisfy — read_only_untouched, lock_tolerant, missing_is_empty, today_matches_runner)
  </read_first>
  <behavior>
    - read_json(path) returns the parsed dict/list for valid JSON; returns None for a missing file or a partial/invalid JSON, never raising (FileNotFoundError, json.JSONDecodeError caught).
    - read_sheet_rows(xlsx, sheet) returns a list of dicts (header-mapped via dict(zip(headers, row))) for a present sheet; returns [] if the sheet is absent; returns None if the workbook is locked/unreadable (WorkbookAccessError/FileNotFoundError) — never raises.
    - A read_only=True, data_only=True read leaves the source file's mtime and sha256 byte-identical (DASH-04).
    - The workbook is always .close()d (read_only keeps the zip open — Pitfall 4).
    - dashboard_data's "today" equals datetime.now().strftime("%Y-%m-%d") with no ZoneInfo (D-02).
  </behavior>
  <action>
    Create scripts/dashboard_data.py with the standard `from __future__ import annotations`,
    portable path constants anchored on `Path.home() / "sports_picks"` (HOME/ROOT/DATA/NBA_DIR/
    MLB_DIR/LOCK_DIR + the run_log path under DATA/"pnl"/"logs"/"run_log.jsonl"), and a bare
    `from workbook_io import safe_load_workbook, WorkbookAccessError` (runs from scripts/).

    Implement:
    - `read_json(path) -> dict | list | None`: `json.loads(Path(path).read_text())`, catching
      `(FileNotFoundError, json.JSONDecodeError)` → return None. This is the lock-free fast path for
      `bankroll.json`, `calibration.json`, and `*_latest.json` (D-03 prefers JSON first).
    - `read_sheet_rows(xlsx, sheet) -> list[dict] | None`: call
      `safe_load_workbook(xlsx, read_only=True, data_only=True)` inside try/except catching
      `(WorkbookAccessError, FileNotFoundError, Exception)` → return None (last-known-good, never
      raise — D-01). On success: if `sheet not in wb.sheetnames` return []; else iter_rows
      `values_only=True`, take the first row as headers, map each remaining row with
      `dict(zip(headers, row))`. ALWAYS `wb.close()` in a `finally` (Pitfall 4). Use
      `data_only=True` so computed cells are values, not `=` formula strings (Pitfall 3).
    - `dashboard_today() -> str` (or expose a `today` value): return
      `datetime.now().strftime("%Y-%m-%d")` — naive local, NO `zoneinfo` import anywhere in the
      module (Pitfall 2; a ZoneInfo re-derivation mismatches the pipeline at midnight).

    Use the *_HEADERS constants from the runner as the authoritative column contract when the
    caller needs typed columns — but do NOT import the 5,650-line runner just for constants; the
    header is read from row 1 of each sheet, so callers map by the literal header strings
    (`EV`, `Model Over Probability`, `Edge`, `Confidence`, `Reasoning`, `Correlation Group`, etc.).
    Never open any save/write path — this module is read-only.
  </action>
  <verify>
    <automated>cd scripts && python3 test_dashboard_data.py -k "read_only_untouched or lock_tolerant or missing_is_empty or today_matches_runner" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 test_dashboard_data.py -k read_only_untouched` passes (source mtime+sha256 unchanged).
    - `-k lock_tolerant` passes (WorkbookAccessError → None/[] , no raise).
    - `-k missing_is_empty` passes (missing path → empty state, no exception).
    - `-k today_matches_runner` passes (today == naive-local strftime; no ZoneInfo).
    - `grep -nE 'zoneinfo|ZoneInfo' scripts/dashboard_data.py` returns nothing.
    - `grep -nE 'safe_save_workbook|\.save\(|wb\.save' scripts/dashboard_data.py` returns nothing (no write path).
    - `read_sheet_rows` calls `safe_load_workbook(... read_only=True, data_only=True ...)` and has a `wb.close()` in a `finally`.
  </acceptance_criteria>
  <done>JSON-first + lock-tolerant readers and the pipeline-matching today date exist; the four read/lock/missing/today tests are green; the module has no write path and no ZoneInfo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the freshness/badge signals (write_in_progress, last_updated_hhmm) from cooperative locks + run_log.jsonl</name>
  <files>scripts/dashboard_data.py</files>
  <read_first>
    - scripts/workbook_io.py (lock JSON shape :90 → {"pid","path","acquired_at"}; stale_seconds=600 :80; LOCK_DIR :25)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (§Architecture Pattern 3 — write_in_progress + last_updated_hhmm; §Common Pitfalls 1 — stale-lock trap; §Badge signal ranking)
    - scripts/test_dashboard_data.py (the write_in_progress Wave-0 test this must satisfy)
  </read_first>
  <behavior>
    - write_in_progress() is True iff some locks/*.xlsx.lock file is fresh (mtime age < 600s) AND its embedded pid is live (os.kill(pid, 0) does not raise ProcessLookupError; PermissionError counts as alive).
    - write_in_progress() is False when the only lock has a dead/bogus pid, OR when the lock mtime is older than 600s (stale — workbook_io will reap it). Presence alone is never sufficient (Pitfall 1).
    - last_updated_hhmm() returns "HH:MM" from the timestamp of the last run_log.jsonl line; returns None if the log is missing/empty/malformed (caught), never raising.
  </behavior>
  <action>
    Add to scripts/dashboard_data.py:
    - `write_in_progress() -> bool`: iterate `LOCK_DIR.glob("*.xlsx.lock")`. For each, read+parse the
      JSON (catch JSONDecodeError/FileNotFoundError/ValueError → skip that file). Compute
      `age = time.time() - lock.stat().st_mtime`; if `age > 600` (STALE_SECONDS, mirroring
      workbook_io's stale_seconds) skip it. Probe liveness with `os.kill(pid, 0)`: catch
      `ProcessLookupError` → dead, skip; treat `PermissionError` as alive (RESEARCH A4). If a live,
      fresh lock is found, return True. Return False otherwise. This is the precise "a write is
      happening right now" signal — NOT mere lock presence (Pitfall 1).
    - `last_updated_hhmm() -> str | None`: read the last non-empty line of
      `DATA/"pnl"/"logs"/"run_log.jsonl"`, `json.loads` it, take `["timestamp"]`, parse with
      `datetime.fromisoformat(...)` and return `.strftime("%H:%M")`. Catch
      `(FileNotFoundError, IndexError, KeyError, ValueError)` → return None. NOTE: run_log timestamps
      carry a `+00:00` (UTC) offset; convert to the machine-local clock with `.astimezone()` before
      formatting so the "HH:MM" label matches the (Pacific) machine clock the page shows (D-02).

    Define `STALE_SECONDS = 600` as a module constant so the test can assert/override it. Keep
    `LOCK_DIR` overridable (module-level constant) so the Wave-0 test can point it at a temp dir.
  </action>
  <verify>
    <automated>cd scripts && python3 test_dashboard_data.py -k write_in_progress 2>&1 | tail -5; echo "--- full file ---"; cd scripts && python3 test_dashboard_data.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 test_dashboard_data.py -k write_in_progress` passes (live+fresh pid → True; dead OR >600s → False).
    - `cd scripts && python3 test_dashboard_data.py` runs all five methods with 0 failures, 0 errors.
    - Module exports `write_in_progress` and `last_updated_hhmm`; `STALE_SECONDS == 600`.
    - `grep -n 'os.kill' scripts/dashboard_data.py` shows the liveness probe; `grep -nE 'safe_save_workbook|wb\.save' scripts/dashboard_data.py` still returns nothing.
  </acceptance_criteria>
  <done>The badge + last-updated signals derive correctly from lock liveness and run_log; all of test_dashboard_data.py is green; the module remains read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workbook/JSON/lock files → read layer | Locked, partial, missing, or malformed file contents cross into the reader; must degrade, never raise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Tampering | dashboard_data read path | mitigate | All reads are `read_only=True`; module has NO save/write path (grep-gated in acceptance); `test_read_only_untouched` proves source bytes unchanged |
| T-1-03 | Denial of Service | read_sheet_rows during a mid-write swap | mitigate | `safe_load_workbook` retry + `WorkbookAccessError` caught → returns last-known-good; a locked workbook never hangs or crashes a page render (D-01) |
| T-1-04 | Spoofing | stale lock read as "live run" | accept | Mitigated by pid-liveness + age<600 (Pitfall 1); residual risk is a benign false "updating…" badge from a PermissionError pid (RESEARCH A4), no data impact |
| T-1-SC | Tampering | python/pip installs | accept | No package installs in this plan (openpyxl/workbook_io already present and audited) — no legitimacy checkpoint needed |
</threat_model>

<verification>
- `cd scripts && python3 test_dashboard_data.py` → all five tests green (DASH-04 + D-01 + D-02).
- `grep -nE 'safe_save_workbook|wb\.save|\.save\(' scripts/dashboard_data.py` → empty (no write path).
- `grep -nE 'zoneinfo|ZoneInfo' scripts/dashboard_data.py` → empty (no midnight mismatch risk).
- Run the targeted suite only; the full `python3 -m pytest` (~34 min) is reserved for the phase gate.
</verification>

<success_criteria>
- `scripts/dashboard_data.py` exists, is read-only, and exports read_json, read_sheet_rows,
  write_in_progress, last_updated_hhmm, and a pipeline-matching today date.
- All five Wave-0 data-layer tests pass; baseline suite stays "2 failed, 202 passed".
- DASH-04 is satisfied: reads leave sources byte-identical and tolerate mid-write locks.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-02-SUMMARY.md` when done.
</output>
