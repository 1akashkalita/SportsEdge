---
phase: 01-foundation-data-layer
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - scripts/dashboard.py
  - scripts/templates/base.html
  - scripts/templates/index.html
autonomous: true
requirements: [DASH-01, DASH-03]
user_setup: []
must_haves:
  truths:
    - "Running `python3 dashboard.py` from scripts/ starts the server and the operator can open it at a 127.0.0.1 localhost URL (DASH-01)"
    - "The server binds to 127.0.0.1 only (fixed default port 8787, overridable via --port/DASHBOARD_PORT) and is unreachable from another machine (DASH-03)"
    - "On launch the browser auto-opens to the dashboard URL (D-04)"
    - "The shell renders a dark, dense-table Pico.css theme with an extensible nav whose Calibration/Line-changes/Live tabs are reserved stubs (D-05, deferred tabs)"
    - "Every page shows the updating badge and last-updated HH:MM from the Phase-1 read layer (D-01/D-02)"
    - "The index route reads data fresh on every page load with no long-lived cache, calling the read layer per request (D-03 fresh-on-load)"
  artifacts:
    - path: "scripts/dashboard.py"
      provides: "Flask app, index route, loopback-only launch with auto-open and --port/DASHBOARD_PORT override"
      exports: ["app", "HOST"]
      min_lines: 50
    - path: "scripts/templates/base.html"
      provides: "Dark Pico.css shell, extensible nav with reserved tab stubs, badge + last-updated slots, Chart.js block for Phase 2"
      contains: "data-theme"
      min_lines: 30
    - path: "scripts/templates/index.html"
      provides: "Phase-1 landing extending base.html (views fill it in Phase 2)"
      contains: "extends"
      min_lines: 5
  key_links:
    - from: "scripts/dashboard.py"
      to: "dashboard_data.write_in_progress / last_updated_hhmm"
      via: "computed per request, passed to render_template"
      pattern: "write_in_progress"
    - from: "scripts/dashboard.py"
      to: "127.0.0.1"
      via: "app.run(host=HOST) loopback-only bind"
      pattern: "127.0.0.1"
    - from: "scripts/templates/index.html"
      to: "scripts/templates/base.html"
      via: "Jinja extends"
      pattern: "extends.*base.html"
---

<objective>
Build the one-command localhost dashboard app shell scripts/dashboard.py plus the dark Pico.css
templates, bound to 127.0.0.1 only with auto-open and a --port/DASHBOARD_PORT override.
Delivers DASH-01 and DASH-03. The index route consumes the Phase-1 read layer's freshness signals
(badge + last-updated) and renders an extensible shell that Phase 2's three views will fill in.

Purpose: Completes the foundation — a launchable, loopback-only process on the verified Flask
stack with the visual baseline (D-05) and the freshness hints (D-01/D-02) wired through the read
layer from plan 02. No views, no actions, no write path.

Output: scripts/dashboard.py, scripts/templates/base.html, scripts/templates/index.html.
Makes the Wave-0 test_dashboard.py route_index and loopback_only tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md
@docs/superpowers/specs/2026-06-23-localhost-dashboard-design.md

@scripts/dashboard_data.py

<interfaces>
Read-layer signals to consume (built in plan 02, scripts/dashboard_data.py):
  write_in_progress() -> bool        drives the updating badge (D-01)
  last_updated_hhmm() -> str | None  the last-updated HH:MM label (D-02)
  read_json / read_sheet_rows / a today value (used by Phase 2; available now)

Loopback-only launch + auto-open (NET-NEW; copy from 01-RESEARCH.md Pattern 1, verified live on 3.14.0a2):
  app = Flask(__name__)   templates/ resolved relative to scripts/ when run from scripts/
  __main__: argparse --port (int) default int(os.environ.get("DASHBOARD_PORT", "8787"))
  url = http://127.0.0.1:PORT/ ; threading.Timer(1.0, lambda: webbrowser.open(url)).start()
  app.run(host="127.0.0.1", port=args.port, debug=False, use_reloader=False)
  NEVER host="0.0.0.0" / host="" (violates DASH-03). use_reloader=False (Pitfall 5: double-open).

argparse + __main__ entry analog: sports_system_runner.py main() (:7907-7911) and :8059.

Tests this plan must turn green (built RED in plan 01, scripts/test_dashboard.py):
  test_route_index    -> dashboard.app.test_client().get("/").status_code == 200
  test_loopback_only  -> dashboard.HOST == "127.0.0.1" (and a bound socket getsockname is loopback)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build templates/base.html + index.html — dark Pico.css shell, nav tab stubs, badge/last-updated + Chart.js block</name>
  <files>scripts/templates/base.html, scripts/templates/index.html</files>
  <read_first>
    - docs/superpowers/specs/2026-06-23-localhost-dashboard-design.md (sec 3 tech, sec 5 pages, the reserved-tab-stubs line for Calibration/Line-changes/Live)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Supporting stack: Pico.css dark data-theme via CDN, Chart.js CDN slot reserved for Phase 2; Assumptions A1/A2; Security Domain: autoescape ON, never the safe filter)
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md (D-05 dark dense-table aesthetic; Deferred Ideas: leave room for later tabs, build nothing)
  </read_first>
  <action>
    Create scripts/templates/base.html as the dark, dense-table shell (D-05):
    - An html element carrying data-theme set to "dark" and a Pico.css link from its CDN (verify the
      exact current Pico CDN URL at build time per RESEARCH A1; classless dark theme).
    - A persistent nav whose three Phase-2 view links are present as labels (Today / Slips /
      History) and which ALSO renders reserved, inert stub tabs for Calibration / Line-changes /
      Live (e.g. aria-disabled true with a hash href and a coming-soon title). These are the
      deferred TAB-01/02/03 stubs: leave room, build nothing.
    - A header that renders the freshness hints the route passes in: an updating badge shown only
      when write_in_progress is truthy (a Jinja if-write_in_progress guard), and a
      last-updated last_updated label.
    - A reserved Jinja block named scripts for Chart.js (Phase 2 History chart). Do NOT add Chart.js
      itself now (A2).
    - A Jinja block named content for pages to fill.
    - Keep Jinja autoescaping ON (the default). Do NOT apply the safe filter to any field — workbook
      strings like Reasoning are rendered in Phase 2 and the safe filter would open stored-XSS (T-1-05).

    Create scripts/templates/index.html: extend base.html with a minimal content block — a heading
    plus a note that the three views land in Phase 2. Tables-over-cards is the directive (D-05), but
    Phase 1 ships only the shell; the data tables are Phase 2.
  </action>
  <verify>
    <automated>cd scripts && python3 -m unittest test_dashboard -k route_index -v 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - scripts/templates/base.html exists and contains: data-theme, a Pico.css CDN link, a Jinja block named content, a Jinja block named scripts, a write_in_progress conditional, and a last_updated reference.
    - base.html contains NO occurrence of the Jinja safe filter (grep for "| safe" returns nothing).
    - base.html nav contains the three deferred stub labels: Calibration, Line-changes (or Line), Live, rendered inert (aria-disabled or a hash href).
    - scripts/templates/index.html extends base.html (contains extends and base.html) and fills the content block.
  </acceptance_criteria>
  <done>The dark Pico shell renders with the freshness hints wired to template variables, reserved tab stubs present, no safe-filter usage; index.html extends it.</done>
</task>

<task type="auto">
  <name>Task 2: Build scripts/dashboard.py — Flask app, index route wired to the read layer, loopback-only launch with auto-open + --port override</name>
  <files>scripts/dashboard.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Architecture Pattern 1 the verified loopback launch + auto-open; Code Examples minimal verified serve; Common Pitfalls 5/6; Open Questions 1 port-conflict policy)
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md (the dashboard.py config + __main__ section; env_value/env_bool analog; the net-new Flask-body note)
    - scripts/sports_system_runner.py (lines 220-224 env_bool, 388-402 env_value, 7907-7911 main argparse, 8059 __main__ guard)
    - scripts/dashboard_data.py (write_in_progress, last_updated_hhmm, read_json, read_sheet_rows — the signals to pass to render_template)
    - scripts/test_dashboard.py (test_route_index and test_loopback_only — the exact module attributes app and HOST this must expose)
  </read_first>
  <action>
    Create scripts/dashboard.py with `from __future__ import annotations`, a bare
    `import dashboard_data` (runs from scripts/), and the Flask app.

    - Define a module-level `HOST = "127.0.0.1"` constant (test_loopback_only asserts
      dashboard.HOST == "127.0.0.1"; this also documents the DASH-03 security boundary in one place).
    - `app = Flask(__name__)` so templates/ resolves relative to scripts/ when launched from scripts/.
    - Index route `@app.route("/")` rendering index.html, passing the freshness context computed per
      request (D-03 fresh-on-load, no cache): `write_in_progress=dashboard_data.write_in_progress()`,
      `last_updated=dashboard_data.last_updated_hhmm()`. Do NOT render any view data here (views are
      Phase 2) — just the shell + freshness hints.
    - A `_port()` helper that reads the port: int(os.environ.get("DASHBOARD_PORT", "8787")) for the
      default (D-04 fixed default 8787, env-overridable). Validate the --port value as int (argparse
      type=int — RESEARCH V5 input validation).
    - `main()` with argparse (--port, default from _port()); build url = http://127.0.0.1:PORT/ ;
      schedule the browser auto-open with threading.Timer(1.0, lambda: webbrowser.open(url)).start()
      (D-04); then app.run(host=HOST, port=args.port, debug=False, use_reloader=False) — bind 127.0.0.1
      ONLY, never 0.0.0.0/"" (DASH-03), and use_reloader=False so the reloader does not double-open the
      browser or double-bind (Pitfall 5).
    - Port-conflict policy (Open Question 1, Claude's discretion): on OSError Errno 48 (address in use),
      fail fast with a clear message naming the busy port and the --port / DASHBOARD_PORT override —
      do NOT silently fall back to 0.0.0.0 (would violate DASH-03). Predictable URL for a solo tool.
    - End with `if __name__ == "__main__": raise SystemExit(main())` (or call main()).
    - No write path, no subprocess, no runner import — Phase 1 is read-only and the dashboard is a
      standalone process (actions are Phase 3).
  </action>
  <verify>
    <automated>cd scripts && python3 -m unittest test_dashboard -v 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 -m unittest test_dashboard` runs and test_route_index, test_loopback_only, and test_flask_serves all pass.
    - dashboard.py defines `HOST = "127.0.0.1"` and calls `app.run(host=HOST, ...)`; grep for "0.0.0.0" and for 'host=""' in dashboard.py returns nothing.
    - dashboard.py uses use_reloader=False and webbrowser.open (auto-open) and an argparse --port with type=int defaulting from DASHBOARD_PORT/8787.
    - The index route passes write_in_progress and last_updated into render_template (grep for write_in_progress in dashboard.py).
    - grep of dashboard.py for safe_save_workbook, subprocess, and 'import sports_system_runner' returns nothing (no write path, no inline runner).
  </acceptance_criteria>
  <done>python3 dashboard.py launches a 127.0.0.1-only server on port 8787 (overridable), auto-opens the browser, and the index route renders the shell with live freshness hints; the loopback and route tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network -> Flask dev server | Only loopback (127.0.0.1) traffic should reach the app; the bind host is the security boundary (DASH-03) |
| workbook string fields -> rendered HTML | Stored workbook strings (e.g. Reasoning) become HTML in Phase 2; autoescape is the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | dashboard.py bind host | mitigate | Hardcode HOST = "127.0.0.1"; app.run(host=HOST); forbid 0.0.0.0/"" (grep-gated in acceptance); test_loopback_only asserts the bound socket is loopback (DASH-03) |
| T-1-05 | Tampering (XSS) | base.html / index.html rendering | mitigate | Jinja2 autoescape ON (verified working incl. C speedup); base.html uses no safe filter (grep-gated) — stored workbook strings cannot inject script |
| T-1-06 | Elevation of Privilege | Werkzeug reloader child | mitigate | use_reloader=False, debug=False — no reloader child, no debugger console exposed (Pitfall 5) |
| T-1-07 | Tampering | dashboard.py write surface | mitigate | No save path, no subprocess, no runner import (grep-gated) — Phase 1 dashboard cannot mutate any workbook or trigger a run |
| T-1-SC | Tampering | python/pip installs | accept | No package installs in this plan — Flask + deps already installed and audited (RESEARCH Package Legitimacy Audit, all Approved) |
</threat_model>

<verification>
- `cd scripts && python3 -m unittest test_dashboard` → flask_serves, route_index, loopback_only all green (DASH-01/02/03).
- grep dashboard.py for "0.0.0.0" / 'host=""' / safe_save_workbook / subprocess / import sports_system_runner → all empty.
- Manual (per VALIDATION.md Manual-Only): `cd scripts && python3 dashboard.py` opens a browser tab at http://127.0.0.1:8787 rendering the dark shell; from another machine `curl http://<mac-ip>:8787` is refused.
- Phase gate: full `cd scripts && python3 -m pytest` → baseline "2 failed, 202 passed" (no new regressions).
</verification>

<success_criteria>
- `python3 dashboard.py` from scripts/ launches a 127.0.0.1-only server on port 8787 (overridable), auto-opens the browser, and serves a dark Pico.css shell with reserved tab stubs.
- The index route renders the badge + last-updated from the Phase-1 read layer (D-01/D-02).
- test_dashboard.py route_index and loopback_only pass; DASH-01 and DASH-03 satisfied; no write path exists.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md` when done.
</output>
