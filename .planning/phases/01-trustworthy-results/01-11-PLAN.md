---
phase: 01-trustworthy-results
plan: 11
type: execute
wave: 4
depends_on: [01-7, 01-8, 01-9, 01-10]
files_modified:
  - .planning/phases/01-trustworthy-results/01-GAP-VERIFICATION.md
autonomous: false
gap_closure: true
requirements: [RESULTS-02, RESULTS-05, RESULTS-06, RESULTS-07, BANKROLL-01]
must_haves:
  truths:
    - "The full pytest suite shows no NEW failures beyond the known baseline (2 pre-existing projection failures)"
    - "test_june8_dryrun_gate.py now PASSES (GAP 3 closed) and is no longer one of the 'pre-existing' failures"
    - "All four gap-closure test files pass (test_prop_pnl_slip_terms, test_dnp_void, test_fantasy_score, test_june8_dryrun_gate)"
    - "No gate-gauntlet or pick-selection test regressed (grading-only changes confirmed)"
  artifacts:
    - path: ".planning/phases/01-trustworthy-results/01-GAP-VERIFICATION.md"
      provides: "Full-suite run evidence: before/after failure counts, gap-test results, baseline confirmation"
  key_links:
    - from: "full pytest run"
      to: "known baseline (2 failed, 202 passed)"
      via: "no new failures; june8 gate flips to PASS"
      pattern: "passed"
---

<objective>
Final gap-closure gate. Run the full test suite and confirm the baseline does not
regress: the two known pre-existing projection failures remain the only failures,
test_june8_dryrun_gate.py now PASSES (GAP 3 closed), and all four gap-closure test
files are green. Confirm grading-only scope (no gate-gauntlet / pick-selection regression).

Purpose: Prove the four fixes landed without collateral damage before the phase
re-verification.
Output: 01-GAP-VERIFICATION.md with the full-suite evidence and a human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trustworthy-results/01-UAT.md

<read_first>
- MEMORY: "Test suite is slow" — full pytest ~34 min; run the full suite ONCE here at phase end.
- MEMORY: "Pre-existing projection test failures" — clean baseline is exactly
  "2 failed, 202 passed" (2 known failures in test_generate_projections.py). After this
  phase the count rises by the new gap-test files but NEW failures must be 0, AND
  test_june8_dryrun_gate.py (previously failing/red) must now be GREEN — so the baseline
  failure set shrinks/holds, never grows.
- 01-UAT.md — the four gaps and their `test:` mapping (test 1 DNP, test 4 fantasy + prop PnL,
  test 2 june8 gate).
- CLAUDE.md — run from scripts/ with python3; tests are stdlib unittest via importlib.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the four gap-closure test files + the full suite; record evidence</name>
  <files>.planning/phases/01-trustworthy-results/01-GAP-VERIFICATION.md</files>
  <action>
    1. Run the four gap-closure test files individually first (fast feedback):
       `cd scripts && python3 -m pytest test_prop_pnl_slip_terms.py test_dnp_void.py test_fantasy_score.py test_june8_dryrun_gate.py -x`
       All four MUST pass.
    2. Run the full suite once (it is slow, ~34 min): `cd scripts && python3 -m pytest 2>&1 | tail -40`.
       Capture the final summary line (passed/failed/skipped counts) and the names of any failures.
    3. Compare against the known baseline "2 failed, 202 passed":
       - The ONLY acceptable failures are the 2 known projection failures in
         test_generate_projections.py (named explicitly in MEMORY).
       - test_june8_dryrun_gate.py MUST be GREEN (it was red before GAP 3).
       - Any OTHER failure is a regression → record it as a blocking finding (do NOT mask it).
    4. Confirm grading-only scope: verify the gate-gauntlet and pick-selection tests
       (e.g. test_dynamic_gate8.py and any evaluate_no_bet_gates tests) still pass — these
       must be untouched by grading-result changes (scope fence).
    5. Write `.planning/phases/01-trustworthy-results/01-GAP-VERIFICATION.md` recording:
       before/after failure counts, the four gap-test results, the full-suite summary line,
       the list of remaining failures (must == the 2 known), and a PASS/FAIL verdict.
       If anything beyond the 2 known failures fails, the verdict is FAIL with the regression named.
    Run from scripts/ with python3.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_prop_pnl_slip_terms.py test_dnp_void.py test_fantasy_score.py test_june8_dryrun_gate.py -x 2>&1 | tail -10</automated>
    <automated>cd scripts && python3 -m pytest test_dynamic_gate8.py -x 2>&1 | tail -6  # gate gauntlet not regressed</automated>
    <automated>test -f .planning/phases/01-trustworthy-results/01-GAP-VERIFICATION.md && grep -c "PASS\|FAIL" .planning/phases/01-trustworthy-results/01-GAP-VERIFICATION.md</automated>
  </verify>
  <done>Four gap tests green; full suite shows only the 2 known failures; june8 gate green; gate-gauntlet untouched; 01-GAP-VERIFICATION.md written with the verdict</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — confirm gap closure and money-safety</name>
  <action>Present the verification evidence to the operator and pause for approval. This checkpoint verifies AFTER the automated full-suite run in Task 1 — it does not perform new work, it confirms the four fixes landed money-safely with no regression.</action>
  <what-built>Four grading-only fixes (GAP 1 DNP→VOID, GAP 2 fantasy-score formula + platform disambiguation, GAP 3 idempotent June 8 gate, GAP 4 prop PnL = slip-terms-only) plus a full-suite regression check.</what-built>
  <how-to-verify>
    1. Review `.planning/phases/01-trustworthy-results/01-GAP-VERIFICATION.md` — confirm the
       full-suite summary shows only the 2 known projection failures and test_june8_dryrun_gate.py
       is now PASS.
    2. Confirm money-safety: a DNP grades VOID (not LOSS); a platform-ambiguous fantasy row with a
       SB/W/QS stays MANUAL REVIEW (abstain); prop rows now carry PnL = 0 while keeping WIN/LOSS.
    3. Optionally re-run: `cd scripts && python3 -m pytest test_prop_pnl_slip_terms.py test_dnp_void.py test_fantasy_score.py test_june8_dryrun_gate.py -x`.
    4. Confirm no bankroll movement is implied by prop PnL (bankroll remains slips-only, D-09).
  </how-to-verify>
  <resume-signal>Type "approved" to close the gaps, or describe any failure/regression to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test baseline → regression detection | a masked new failure would let a grading bug ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-G5-01 | Repudiation | full-suite verdict | mitigate | Record exact before/after failure counts and names; any failure beyond the 2 known is a blocking FAIL |
| T-01-G5-02 | Tampering | gate-gauntlet scope fence | mitigate | Explicitly re-run test_dynamic_gate8.py to confirm grading-only changes didn't touch pick selection |
| T-01-G5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- Four gap-closure test files pass.
- Full suite: only the 2 known projection failures remain; test_june8_dryrun_gate.py is green.
- test_dynamic_gate8.py (gate gauntlet) still passes.
- 01-GAP-VERIFICATION.md exists with a clear PASS verdict.
</verification>

<success_criteria>
- No new test failures beyond the known baseline.
- The June 8 gate (GAP 3) now passes.
- Grading-only scope confirmed (no gate-gauntlet / pick-selection regression).
- Human checkpoint approves gap closure.
</success_criteria>

<output>
Create `.planning/phases/01-trustworthy-results/01-11-SUMMARY.md` when done
</output>
