---
phase: 02-reliability-fixes-defect-removal
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/repro_broken_pipe.py
  - scripts/test_fix01_broken_pipe.py
  - scripts/test_fix02_telegram_circuit_breaker.py
autonomous: true
requirements: [FIX-01, FIX-02]
must_haves:
  truths:
    - "A regression test proves no spurious TASK FAILED fires after a stdout pipe-close on a completed task (FIX-01)"
    - "A regression test proves send_telegram, under forced failure, completes bounded, trips the breaker, and logs the suppressed-count line (FIX-02)"
    - "The repro harness no longer writes synthetic failures into the production run_log.txt and no longer relies on a racy byte-offset scan (WR-03)"
    - "Each regression test fails on the pre-fix code path and passes on the post-fix code path (D-10)"
  artifacts:
    - path: "scripts/test_fix01_broken_pipe.py"
      provides: "FIX-01 pipe-close regression assertion"
      contains: "unittest"
    - path: "scripts/test_fix02_telegram_circuit_breaker.py"
      provides: "FIX-02 forced-Telegram-failure / circuit-breaker assertions"
      contains: "_telegram_breaker"
    - path: "scripts/repro_broken_pipe.py"
      provides: "hardened fault-injection harness (isolated log sink, no byte-offset scan)"
      contains: "uuid"
  key_links:
    - from: "test_fix02_telegram_circuit_breaker.py"
      to: "runner._telegram_breaker"
      via: "forced ConnectionError trips breaker, asserts tripped + bounded + suppressed-line"
      pattern: "_telegram_breaker"
    - from: "repro_broken_pipe.py"
      to: "isolated log path"
      via: "env override / temp file instead of production run_log.txt"
      pattern: "uuid"
---

<objective>
Ship the FIX-01 and FIX-02 regression tests (D-10) and harden the existing repro harness
(D-09 / WR-03) so it becomes a safe standing regression test. Because both bugs only manifest
under failure conditions, a clean run cannot prove them — these tests force the failure
conditions: a stdout pipe-close on a completed task (FIX-01) and a forced unreachable Telegram
(FIX-02). Each must fail on the pre-fix code path and pass on the post-fix path.

Purpose: Lock in the Plan-01 fixes with executable proof and seed the RES-04 sweep (Phase 3).
Output: Two new test files plus a hardened repro_broken_pipe.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-fixes-defect-removal/02-CONTEXT.md
@.planning/phases/02-reliability-fixes-defect-removal/02-PATTERNS.md
@.planning/phases/01-diagnosis/01-REVIEW.md

<interfaces>
Post-Plan-01 runner state these tests assert against:
  send_telegram(message, retries=2, backoff=5) -> bool  — now 10s timeout + circuit-breaker.
  _telegram_breaker: dict with keys consecutive_failures, tripped, suppressed (module-level).
  The breaker trips after 3 consecutive failures; a tripped breaker returns False immediately.
  On trip, one run-log line contains the substring "alerts suppressed — Telegram unreachable".
  All three JSON_RESULT prints in main() and the two fetch stdout prints are now safe_print.

Existing repro harness (scripts/repro_broken_pipe.py) facts:
  SCRIPTS_DIR = Path(__file__).resolve().parent ; RUNNER = SCRIPTS_DIR / "sports_system_runner.py".
  RUN_LOG (line 118) currently points at the PRODUCTION <repo>/data/pnl/logs/run_log.txt.
  count_new_log_signals (lines 143-165) uses a byte-offset seek (RACY — WR-03).
  Subprocess spawn pattern (lines ~205-213): subprocess.Popen([sys.executable, "-u", RUNNER,
    "--task", REPRO_TASK], stdout=PIPE, stderr=PIPE, cwd=SCRIPTS_DIR).
  REPRO_TASK = "verify" ; _SENTINEL = "verification complete".
  Reader thread closes the read end at the sentinel to trigger the broken pipe.

Test analog patterns (from PATTERNS.md):
  importlib runner load: spec_from_file_location("sports_system_runner", SCRIPT); exec_module.
  unittest.mock.patch.object(requests, "post", side_effect=ConnectionError(...)).
  test header + __main__ guard: see test_stage5_telegram_platform.py / test_slip_payouts.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden repro_broken_pipe.py (WR-03) and invert its pass/fail for the post-fix regression (D-09)</name>
  <files>scripts/repro_broken_pipe.py</files>
  <read_first>
    - scripts/repro_broken_pipe.py in full (current 289-line harness; especially the RUN_LOG constant ~line 118, count_new_log_signals ~143-165, the byte-offset snapshot ~195, the Popen spawn ~205-213, and the pass/fail classifier ~252-284)
    - .planning/phases/01-diagnosis/01-REVIEW.md WR-03 (production-log pollution + racy byte-offset scan) and WR-02 (exit-code coupling) and IN-04 (count_new_log_signals swallows read errors)
    - .planning/phases/02-reliability-fixes-defect-removal/02-PATTERNS.md "Extend for FIX-01/FIX-02 regression (D-09)" section
  </read_first>
  <action>
    Harden the harness per WR-03 so it is safe as a standing regression test. (1) Stop writing into the
    production run-log: point the runner subprocess at an ISOLATED log sink for the repro — set an env
    var on the spawned subprocess (e.g. via env=... on Popen) that the runner already honors for the log
    path if one exists, OR write to a temp file under tempfile.mkdtemp() and scan that. If the runner has
    no log-path override env var, the simplest safe approach is: generate a unique nonce token
    (uuid.uuid4().hex) per run, pass it into the runner via an env var, and scan the production log only
    for lines containing that nonce — replacing the racy byte-offset seek with a nonce substring count
    (this removes the byte-offset race even if the production sink must be used). Choose the temp-file
    approach if the runner honors a log-path env var; otherwise use the nonce approach. Record which was
    used in the SUMMARY. (2) Replace count_new_log_signals's byte-offset scan with the nonce/temp scan;
    make an unreadable log return a distinct sentinel (not 0) so an infra failure is not misread as
    "not reproduced" (IN-04). (3) Invert the post-fix classification (D-09): after the Plan-01 safe_print
    sweep, returncode == 0 with zero new broken-pipe signals is the PASS (fix confirmed) condition; the
    OLD reproduction (broken-pipe signals present) is now the FAIL (regression leaked) condition. Update
    the printed messages accordingly. Keep the Popen spawn pattern, REPRO_TASK="verify", and the
    sentinel-driven reader thread unchanged.
  </action>
  <verify>
    <automated>cd scripts && python3 repro_broken_pipe.py; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - After Plan 01, python3 repro_broken_pipe.py (from scripts/) exits 0 and prints a PASS/fix-confirmed message (no broken-pipe signals on a completed verify task)
    - grep -c "uuid" scripts/repro_broken_pipe.py is >= 1 OR grep -c "tempfile" scripts/repro_broken_pipe.py is >= 1 (the harness uses an isolated sink or a nonce, not a raw byte-offset)
    - grep -c "seek(" scripts/repro_broken_pipe.py returns 0 (the racy byte-offset seek is removed)
    - The harness no longer appends synthetic TRACEBACK/ERROR lines into the production run_log.txt as its evidence channel (verified by reading the changed scan logic in the SUMMARY)
    - cd scripts && python3 -c "import repro_broken_pipe" exits 0
  </acceptance_criteria>
  <done>The repro harness uses an isolated/nonce-scoped log sink, has no byte-offset seek, distinguishes infra failure from "not reproduced", and treats clean-exit-no-signals as the post-fix PASS condition.</done>
</task>

<task type="auto">
  <name>Task 2: FIX-01 regression test — no spurious TASK FAILED after a pipe-close (D-10)</name>
  <files>scripts/test_fix01_broken_pipe.py</files>
  <read_first>
    - scripts/repro_broken_pipe.py (the hardened harness from Task 1 — reuse its Popen + sentinel-close mechanism, or import its helpers)
    - scripts/test_game_completion_monitor_smoke.py lines 1-25 (structured subprocess-test header pattern)
    - scripts/sports_system_runner.py lines 5612-5658 (main() — confirm that after the safe_print sweep, a pipe-close on a completed task does NOT reach the except branch's send_telegram TASK FAILED call)
    - .planning/phases/02-reliability-fixes-defect-removal/02-PATTERNS.md "NEW scripts/test_fix01_broken_pipe.py" section
  </read_first>
  <action>
    Create scripts/test_fix01_broken_pipe.py as a plain unittest.TestCase. Spawn the runner with the
    "verify" task using the same -u + sentinel-close mechanism as the hardened repro (import the helper
    from repro_broken_pipe if practical, else replicate the Popen + reader-thread pattern). The single
    test asserts the POST-FIX behavior: when the stdout pipe is closed at the task-completion sentinel,
    (a) the runner process exits 0 (the completed task is not misclassified as failed), and (b) no
    broken-pipe signal is recorded for that run in the isolated/nonce-scoped sink (no ERROR task= /
    TRACEBACK task= / Broken pipe line attributable to this run). Use the isolated-sink/nonce approach
    from Task 1 so the test does not pollute or race the production log. Add the __main__ guard with
    unittest.main(). Document in the docstring that this is the FIX-01 regression test (D-10) and that it
    would FAIL on the pre-fix runner (bare JSON_RESULT print raised BrokenPipeError into main()'s except,
    firing the TASK FAILED alert and exiting 1).
  </action>
  <verify>
    <automated>cd scripts && python3 test_fix01_broken_pipe.py</automated>
  </verify>
  <acceptance_criteria>
    - python3 test_fix01_broken_pipe.py (from scripts/) exits 0 with an OK summary line
    - The test asserts the runner exits 0 after a pipe-close on the completed verify task (would FAIL on the pre-fix code where the bare print raised BrokenPipeError and main() returned 1 — a real failing-before/passing-after test per D-10)
    - The test asserts no broken-pipe signal is recorded for the run in an isolated/nonce-scoped sink (it does not scan the shared production log by raw byte offset)
    - python3 -m pytest test_fix01_broken_pipe.py (from scripts/) reports the test passing
  </acceptance_criteria>
  <done>A FIX-01 regression test proves a completed task survives a stdout pipe-close with exit 0 and no spurious TASK FAILED; it would fail on the pre-fix runner; it uses an isolated sink.</done>
</task>

<task type="auto">
  <name>Task 3: FIX-02 regression test — forced Telegram failure trips bounded circuit-breaker (D-10)</name>
  <files>scripts/test_fix02_telegram_circuit_breaker.py</files>
  <read_first>
    - scripts/sports_system_runner.py lines 233-259 (post-Plan-01 send_telegram with the 10s timeout + breaker) and the module-level _telegram_breaker dict
    - scripts/test_stage5_telegram_platform.py lines 11-20 (importlib runner load + attribute monkeypatch pattern)
    - scripts/test_odds_api_io_client.py lines 37-48 (FakeSession / mock-network pattern)
    - .planning/phases/02-reliability-fixes-defect-removal/02-PATTERNS.md "NEW scripts/test_fix02_telegram_circuit_breaker.py" section (the full reference test, including setUp resetting the breaker)
  </read_first>
  <action>
    Create scripts/test_fix02_telegram_circuit_breaker.py as a plain unittest.TestCase, loading the runner
    via importlib (spec_from_file_location + exec_module). In setUp, set valid dummy TELEGRAM_BOT_TOKEN /
    TELEGRAM_HOME_CHANNEL env vars (so the creds guard passes) and reset runner._telegram_breaker to
    consecutive_failures=0, tripped=False, suppressed=0. Add three tests mirroring PATTERNS.md:
    (1) test_breaker_trips_after_n_failures — patch.object(requests, "post",
    side_effect=requests.exceptions.ConnectionError("simulated")), call send_telegram repeatedly (e.g. 5x),
    assert runner._telegram_breaker["tripped"] is True and the whole loop completes in bounded time
    (assert elapsed < 30s — proves the timeout/breaker bounded what previously stalled for hours).
    (2) test_breaker_tripped_suppresses_immediately — set tripped=True manually, time a single
    send_telegram call, assert it returns False and elapsed < 0.2s (no network call when tripped).
    (3) test_suppressed_count_logged — monkeypatch runner.log to capture lines, force the breaker to trip,
    assert at least one captured line contains the substring "alerts suppressed" or "unreachable".
    Add the __main__ guard with unittest.main(). Docstring notes this is the FIX-02 regression test (D-10),
    failing on the pre-fix runner (no _telegram_breaker attribute -> AttributeError, and the 30s x retries
    loop would not be bounded).
  </action>
  <verify>
    <automated>cd scripts && python3 test_fix02_telegram_circuit_breaker.py</automated>
  </verify>
  <acceptance_criteria>
    - python3 test_fix02_telegram_circuit_breaker.py (from scripts/) exits 0 with an OK summary line
    - test_breaker_trips_after_n_failures asserts runner._telegram_breaker["tripped"] is True after forced consecutive failures and that the loop completes in < 30s
    - test_breaker_tripped_suppresses_immediately asserts a tripped breaker returns False in < 0.2s
    - test_suppressed_count_logged asserts a captured log line contains "alerts suppressed" or "unreachable"
    - The test references runner._telegram_breaker (would raise AttributeError on the pre-fix runner — confirms failing-before/passing-after per D-10)
    - python3 -m pytest test_fix02_telegram_circuit_breaker.py (from scripts/) reports the tests passing
  </acceptance_criteria>
  <done>A FIX-02 regression test proves the breaker trips, bounds the stall, short-circuits immediately when tripped, and logs the suppressed-count line; it would fail on the pre-fix runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> runner subprocess / module | Tests spawn or import the runner with forced-failure inputs (mocked requests, closed pipe). All inputs are test-controlled; no external network is actually contacted (requests.post is mocked). |
| repro harness -> log sink | Hardened to an isolated/nonce-scoped sink so it no longer writes synthetic failures into the production operational log. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | dummy Telegram creds in tests | mitigate | Tests set placeholder TOKEN/CHANNEL env values only (e.g. "x"/"y"); they never read or log the real ~/.hermes/.env secrets, and the network call is mocked so no real token is transmitted. |
| T-02-10 | Information Disclosure | harness log scan reading run_log.txt | mitigate | The hardened scan keys on a per-run nonce or an isolated temp file; it does not surface arbitrary run-log contents (which may contain str(e) text) into test output beyond the matched signal. |
| T-02-11 | Tampering | production run_log pollution by the harness (the WR-03 risk) | mitigate | This plan IS the mitigation: the harness no longer writes its synthetic failure evidence into the shared production log. |
| T-02-12 | Tampering | npm/pip/cargo installs | accept | No package installs; stdlib (unittest, uuid, tempfile, importlib, subprocess) + existing requests only. No legitimacy gate needed. |

No new untrusted-input paths, secrets handling, or network endpoints are introduced (the only network
call is mocked). No HIGH-severity threat applies at ASVS L1.
</threat_model>

<verification>
- python3 repro_broken_pipe.py exits 0 post-fix with a fix-confirmed message and no byte-offset seek.
- python3 test_fix01_broken_pipe.py passes (completed task survives pipe-close, exit 0, no spurious alert).
- python3 test_fix02_telegram_circuit_breaker.py passes (breaker trips, bounded, suppressed line logged).
- All three are run from scripts/ with python3 and pass under python3 -m pytest.
</verification>

<success_criteria>
- FIX-01 and FIX-02 each have an executable regression test that fails before the fix and passes after (D-10).
- The repro harness is safe to keep as a standing regression test (no production-log pollution, no racy scan — WR-03).
</success_criteria>

<output>
Create `.planning/phases/02-reliability-fixes-defect-removal/02-04-SUMMARY.md` when done.
</output>
