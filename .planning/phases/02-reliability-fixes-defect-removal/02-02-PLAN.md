---
phase: 02-reliability-fixes-defect-removal
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/sports_system_runner.py
autonomous: true
requirements: [DEF-01]
must_haves:
  truths:
    - "Exactly one definition of injury_monitor and one of clv_tracker exist in sports_system_runner.py"
    - "The surviving definitions are the active (later, superset) implementations that the runner already used"
    - "Removing the dead earlier definitions changes no observable runner behavior (confirmed by diff + existing tests)"
  artifacts:
    - path: "scripts/sports_system_runner.py"
      provides: "single canonical injury_monitor and clv_tracker"
      contains: "def injury_monitor"
  key_links:
    - from: "run_task() dispatch table"
      to: "injury_monitor / clv_tracker"
      via: "name resolution now unambiguous (only one def each)"
      pattern: "injury_monitor"
---

<objective>
Remove the two stability-threatening dead duplicate definitions in `sports_system_runner.py`:
the earlier `injury_monitor` (~line 3610, a 38-line cached-baseline stub) and the earlier
`clv_tracker` (~line 3651, a 17-line odds_api stub). Python already binds the LATER definitions
(~5049 / ~5443) as the active implementations; the earlier ones are dead and confusing. PATTERNS.md
diffed both and found the earlier defs are strict subsets with no unique logic — but this plan
re-confirms via a fresh diff before deletion (DEF-01: surface unique logic rather than silently drop).

Purpose: Eliminate the duplicate-definition footgun (a maintainer editing the earlier def has no
effect) and confirm the active behavior is unchanged.
Output: A runner with exactly one definition of each function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-fixes-defect-removal/02-CONTEXT.md
@.planning/phases/02-reliability-fixes-defect-removal/02-PATTERNS.md

<interfaces>
Current state extracted from scripts/sports_system_runner.py:

Dead earlier defs (to remove):
  injury_monitor at line 3610 — reads only injuries.cell(r,5/6).value from the cached "Injury Baseline"
    sheet; no ESPN call, no espn_injury_rows, no calculate_injury_impact, no find_injury_baseline_row.
    Block runs lines 3610-3648 (def line through its return).
  clv_tracker at line 3651 — calls odds_api(sport) directly and append_unique; block 3651-3667.

Active later defs (to KEEP — do NOT touch):
  injury_monitor at line 5049 — calls espn_injury_rows, espn_injury_news_flags,
    enrich_picks_with_espn_odds, calculate_injury_impact, find_injury_baseline_row,
    affected_items_for_player, set_affected_statuses, extract_current_pp_line (full superset).
  clv_tracker at line 5443 — uses the OddsApiIoClient path with resolve_odds_api_io_league.

run_task() dispatch (line 5593) maps nba_injury_monitor/mlb_injury_monitor -> injury_monitor and
  nba_clv_tracker/mlb_clv_tracker -> clv_tracker; with one def each, resolution is unambiguous.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm earlier defs are strict subsets, then remove them (DEF-01)</name>
  <files>scripts/sports_system_runner.py</files>
  <read_first>
    - scripts/sports_system_runner.py lines 3610-3668 (the two earlier/dead defs in full)
    - scripts/sports_system_runner.py lines 5049-5200 (active injury_monitor)
    - scripts/sports_system_runner.py lines 5443-5560 (active clv_tracker)
    - scripts/sports_system_runner.py lines 5593-5610 (run_task dispatch table that resolves these names)
    - .planning/phases/02-reliability-fixes-defect-removal/02-PATTERNS.md "DEF-01: Remove duplicate" section (the recorded diff evidence: both earlier defs are strict subsets, no unique logic)
  </read_first>
  <action>
    FIRST (DEF-01 safety step): diff the earlier injury_monitor (3610-3648) against the active one (5049+)
    and the earlier clv_tracker (3651-3667) against the active one (5443+). Confirm the earlier ones
    reference no helper, branch, side-effect, or workbook column/sheet write that the active versions do
    NOT also perform. The expected finding (per PATTERNS.md) is: earlier injury_monitor only reads the
    cached baseline and the active one is a strict superset; earlier clv_tracker uses the legacy odds_api()
    path while the active one uses the OddsApiIoClient path and writes a richer result dict. If the diff
    surfaces ANY unique behavior in an earlier def that is not covered by the active def, STOP and surface
    it to the operator in the SUMMARY (do NOT delete) — do not silently drop logic. Record the diff
    outcome in the SUMMARY. THEN, only if confirmed subset: delete the entire earlier injury_monitor block
    (def line through its return, lines ~3610-3648) and the entire earlier clv_tracker block (~3651-3667),
    leaving a single blank-line separation consistent with surrounding style. Do not modify the active
    definitions, run_task, or any caller. This is a pure dead-code removal — no gate logic, pick output,
    or workbook schema change.
  </action>
  <verify>
    <automated>cd scripts && python3 -c "import ast,sys; t=ast.parse(open('sports_system_runner.py').read()); names=[n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)]; inj=names.count('injury_monitor'); clv=names.count('clv_tracker'); print('injury_monitor=',inj,'clv_tracker=',clv); sys.exit(0 if inj==1 and clv==1 else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - The AST check prints injury_monitor= 1 clv_tracker= 1 and exits 0
    - grep -c 'def injury_monitor' scripts/sports_system_runner.py returns 1
    - grep -c 'def clv_tracker' scripts/sports_system_runner.py returns 1
    - The SURVIVING injury_monitor still references espn_injury_rows: grep -c 'espn_injury_rows' scripts/sports_system_runner.py is >= 1 (the active superset def was kept, not the stub)
    - The SURVIVING clv_tracker still references resolve_odds_api_io_league: grep -c 'resolve_odds_api_io_league' scripts/sports_system_runner.py is >= 1
    - cd scripts && python3 -c "import sports_system_runner" exits 0
    - The SUMMARY records the diff outcome confirming the earlier defs held no unique logic
  </acceptance_criteria>
  <done>Exactly one injury_monitor and one clv_tracker remain; the survivors are the active superset implementations; the diff confirmation is recorded; the module still imports.</done>
</task>

<task type="auto">
  <name>Task 2: Add a DEF-01 behavior-confirmation test and confirm the existing suite passes</name>
  <files>scripts/test_def01_no_duplicate_defs.py</files>
  <read_first>
    - scripts/test_generate_projections.py lines 1-16 (direct-import + sys.path.insert + unittest.TestCase header pattern)
    - scripts/test_slip_payouts.py lines 1-20 and the __main__ guard (the canonical importlib/unittest layout)
    - scripts/sports_system_runner.py lines 5593-5610 (run_task dispatch — the names whose resolution this test guards)
    - .planning/phases/02-reliability-fixes-defect-removal/02-CONTEXT.md D-10 (DEF-01 ships with a regression test confirming active behavior unchanged)
  </read_first>
  <action>
    Create scripts/test_def01_no_duplicate_defs.py as a plain unittest.TestCase, loading the runner via
    importlib.util.spec_from_file_location (the established runner-load idiom) so import does not execute a
    task. Because the active injury_monitor/clv_tracker call live ESPN/Odds-API networks, this test does
    NOT execute them; instead it asserts the structural guarantee that DEF-01 establishes:
    (1) Parse the runner source with ast and assert exactly one FunctionDef named injury_monitor and one
    named clv_tracker (this is the regression that fails on the pre-fix code path with two each).
    (2) Assert the surviving module attribute runner.injury_monitor is the SUPERSET implementation by
    checking its source (inspect.getsource) contains the marker call espn_injury_rows, and
    runner.clv_tracker source contains resolve_odds_api_io_league — proving the active def survived and the
    stub was the one removed. (3) Assert run_task's dispatch maps the four sport tasks to callables without
    error (the names resolve unambiguously). Add the __main__ guard calling unittest.main(). Note in the
    file docstring this is the DEF-01 regression test (D-10) and that it intentionally avoids invoking the
    networked task bodies. After writing, run the FULL existing suite from scripts/ to confirm no
    regression from the deletion (the minimal-invasive / no-behavior-change contract).
  </action>
  <verify>
    <automated>cd scripts && python3 test_def01_no_duplicate_defs.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - python3 test_def01_no_duplicate_defs.py (from scripts/) exits 0 with an OK summary line
    - The test asserts exactly one FunctionDef each for injury_monitor and clv_tracker (would FAIL against the pre-deletion code that has two each — confirms it is a real regression test per D-10)
    - The test asserts the surviving injury_monitor source contains espn_injury_rows and the surviving clv_tracker source contains resolve_odds_api_io_league
    - python3 -m pytest (from scripts/) shows no NEW failures relative to the pre-edit baseline (record the baseline pass/fail counts in the SUMMARY)
  </acceptance_criteria>
  <done>A DEF-01 regression test exists that fails on duplicate defs and passes on the deduplicated runner; the full existing suite shows no new failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only removes dead code and adds a static-structure test. No new input, network, or secret path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | accidental deletion of the WRONG (active) def | mitigate | The DEF-01 safety step diffs earlier vs active before deletion; acceptance criteria assert the SURVIVING def still contains the superset markers (espn_injury_rows / resolve_odds_api_io_league), catching an inverted deletion. |
| T-02-06 | Tampering | npm/pip/cargo installs | accept | No package installs; stdlib (ast, inspect, importlib, unittest) only. No legitimacy gate needed. |

No new untrusted-input paths, secrets handling, or network endpoints. No HIGH-severity threat applies
at ASVS L1 — this is a local dead-code removal on a single-operator cron backend.
</threat_model>

<verification>
- AST shows exactly one def each for injury_monitor and clv_tracker.
- The new DEF-01 test passes; it would fail on the pre-deletion (duplicate) code.
- python3 -m pytest (from scripts/) shows no new failures versus baseline — active behavior unchanged.
- python3 -c "import sports_system_runner" succeeds.
</verification>

<success_criteria>
- Only one definition of injury_monitor and one of clv_tracker exist (DEF-01 success criterion 4).
- The surviving definitions are the active superset implementations.
- The existing tests confirm no behavior change; the new DEF-01 regression test passes.
</success_criteria>

<output>
Create `.planning/phases/02-reliability-fixes-defect-removal/02-02-SUMMARY.md` when done.
</output>
