---
phase: 03-safe-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/test_dashboard_actions.py
  - scripts/dashboard_writes.py
  - scripts/dashboard.py
autonomous: true
requirements: [ACTION-01, ACTION-02, ACTION-03, ACTION-04]

must_haves:
  truths:
    - "Running the new test file produces RED (failing/erroring) results for all 9 ACTION-01..04 cases — the central guarantee's tests exist before implementation"
    - "dashboard_writes.py exists as an importable module with mark_placed and add_note signatures (slips-only, per D-08 research verdict) but no implementation"
    - "flash() works in dashboard.py because app.secret_key is set (D-06 inline banner prerequisite)"
    - "dashboard_data.py read-only contract is preserved — no write code added there (write path lives in dashboard_writes.py)"
  artifacts:
    - path: "scripts/test_dashboard_actions.py"
      provides: "RED stubs for TestRefreshAction, TestStatusEndpoint, TestMarkPlaced, TestAddNote, TestActionFourHardLine (9 cases)"
      contains: "class TestActionFourHardLine"
      min_lines: 120
    - path: "scripts/dashboard_writes.py"
      provides: "Write-helper module stub: mark_placed(date, slip_id, placed), add_note(date, slip_id, note)"
      contains: "def mark_placed"
    - path: "scripts/dashboard.py"
      provides: "app.secret_key wired for flash()"
      contains: "app.secret_key"
  key_links:
    - from: "scripts/test_dashboard_actions.py"
      to: "scripts/sports_system_runner.py"
      via: "importlib.util.spec_from_file_location in setUpClass"
      pattern: "spec_from_file_location.*sports_system_runner"
    - from: "scripts/test_dashboard_actions.py"
      to: "scripts/dashboard_writes.py"
      via: "import dashboard_writes"
      pattern: "import dashboard_writes"
---

<objective>
Lay the test-first foundation for Phase 3 safe actions. Create the RED test scaffold
(`test_dashboard_actions.py`) covering all 9 ACTION-01..04 behaviors, the
`dashboard_writes.py` module stub (signatures only, no implementation), and wire
`app.secret_key` into `dashboard.py` so Flask `flash()` works in later waves.

Purpose: The phase's central guarantee (ACTION-04: "no action changes gate logic,
grades, EV, or exposure caps") is only credible if its tests exist and fail-then-pass.
This plan establishes the RED state. Subsequent waves turn each test GREEN.

Output: `scripts/test_dashboard_actions.py` (RED), `scripts/dashboard_writes.py` (stub),
`scripts/dashboard.py` (+ `app.secret_key`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-safe-actions/03-CONTEXT.md
@.planning/phases/03-safe-actions/03-RESEARCH.md
@.planning/phases/03-safe-actions/03-PATTERNS.md
@.planning/phases/03-safe-actions/03-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live source; do not re-explore. -->

Runner load idiom (test_dynamic_gate8.py:1-13 — the ONLY supported way to load the runner in tests):
  MOD_PATH = Path(__file__).with_name("sports_system_runner.py")
  spec = importlib.util.spec_from_file_location("sports_system_runner", MOD_PATH)
  runner = importlib.util.module_from_spec(spec); spec.loader.exec_module(runner)
  runner.load_suppressed_edge_types = lambda: {}   # stub external I/O

evaluate_no_bet_gates signature (sports_system_runner.py:2457):
  (ok: bool, skip_record: dict|None, gates_passed: list[str]) = evaluate_no_bet_gates(pick, {})

Cap constants (sports_system_runner.py:2584-2585) — assert these, NOT the removed DAILY_EXPOSURE_CAP:
  PER_PLAYER_CAP = 6.0
  PER_GAME_CAP   = 6.0

Flask test_client POST idiom (test_dashboard.py:100-103 GET pattern, extended):
  client = dashboard.app.test_client()
  resp = client.post("/action/mark-placed", data={"date": "...", "slip_id": "...", "placed": "1"})
  # expect 302 (POST->redirect)

sys.path setup (test_dashboard_views.py:22-24):
  SCRIPT_DIR = Path(__file__).resolve().parent
  if str(SCRIPT_DIR) not in sys.path: sys.path.insert(0, str(SCRIPT_DIR))

dashboard.py current Flask init (dashboard.py:32) — secret_key is absent:
  app: Flask = Flask(__name__)

Slip History headers (slip_payouts.py:18-24) — for the synthetic workbook fixture; col 1 = Date, col 2 = Slip ID:
  Date, Slip ID, Platform, Slip Type, Number of Legs, Legs, Stake Units, Winning Legs,
  Losing Legs, Push/Void/DNP Legs, Contains Demon, Contains Goblin, Special Line Count,
  Slip Result, Standard Payout Multiplier, Estimated Payout Multiplier,
  Actual Payout Multiplier, Payout Confidence, Gross Return, Net PnL,
  Needs Payout Reconciliation, Graded At, Notes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard_writes.py stub + wire app.secret_key</name>
  <read_first>
    - scripts/dashboard.py (the file being modified — note line 22 import, line 32 `app = Flask(__name__)`, the read-only `_freshness_context` at 51)
    - scripts/dashboard_data.py:1-58 (module skeleton + path-constant analog — copy the `Path.home()`-anchored PNL_DIR pattern; never hardcode username)
    - 03-PATTERNS.md "dashboard_writes.py (utility, CRUD)" — imports/path-constant/signature analog (lines 32-58, 115-138)
    - 03-RESEARCH.md Pitfall 2 (flash silently fails if secret_key is None) and Pitfall 6 (do NOT `from sports_system_runner import ensure_ws_columns` — inline it)
  </read_first>
  <action>
    Create `scripts/dashboard_writes.py` as a STUB module (no working write logic yet — that is Plan 02). Include: the module docstring stating it is the additive write path for ACTION-02/03 and never touches gate logic/grades/EV/exposure (ACTION-04 hard line); `from __future__ import annotations`; imports of `datetime`/`timezone`, `Path`, `Any`, and `from workbook_io import WorkbookAccessError, safe_load_workbook, safe_save_workbook, workbook_file_lock`; a module-level `PNL_DIR = Path.home() / "sports_picks" / "data" / "pnl"` constant (anchored on `Path.home()` per the dashboard_data.py analog, never a literal username). Define two functions with full type annotations and `-> None` returns but bodies of `raise NotImplementedError` (or `pass` with a TODO): `mark_placed(date: str, slip_id: str, placed: bool)` and `add_note(date: str, slip_id: str, note: str)`. Scope is SLIPS-ONLY for v1 per the D-08 research verdict (Picks `Slip ID` is null in live data; `Date+Selection` is not guaranteed unique) — document this in the module docstring; do NOT add pick-note functions.

    Then modify `scripts/dashboard.py`: immediately after `app: Flask = Flask(__name__)` (line 32), add `app.secret_key = os.environ.get("DASHBOARD_SECRET_KEY") or os.urandom(16)`. `os` is already imported (line 17). Do NOT add routes, do NOT touch `_freshness_context`, do NOT change `HOST`.
  </action>
  <verify>
    <automated>cd scripts && python3 -c "import dashboard_writes; assert hasattr(dashboard_writes,'mark_placed') and hasattr(dashboard_writes,'add_note'); import dashboard; assert dashboard.app.secret_key, 'secret_key not set'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import dashboard_writes"` succeeds from `scripts/` (module imports cleanly, no runner import side-effects).
    - `dashboard_writes` exposes `mark_placed` and `add_note` callables with the documented signatures.
    - `dashboard_writes.py` does NOT contain `from sports_system_runner import` (Pitfall 6 — runner is not imported).
    - `dashboard_writes.py` module docstring states slips-only scope (D-08 verdict) and the ACTION-04 hard line.
    - `dashboard.app.secret_key` is truthy after import (flash prerequisite, Pitfall 2).
    - `dashboard_data.py` is unchanged (read-only contract preserved).
  </acceptance_criteria>
  <done>dashboard_writes.py importable with stub signatures; dashboard.app.secret_key set; dashboard_data.py untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED test scaffold test_dashboard_actions.py (all 9 ACTION cases)</name>
  <read_first>
    - scripts/test_dynamic_gate8.py:1-54 (importlib runner-load idiom + `cand()` synthetic-pick helper — the canonical-pick fixture source)
    - scripts/test_dashboard_views.py:22-80 (sys.path setup + `_make_today_wb` synthetic-workbook fixture pattern + Flask test_client usage)
    - scripts/test_dashboard.py:95-103 (test_client GET pattern to extend to POST)
    - 03-PATTERNS.md "test_dashboard_actions.py" section (lines 142-286 — file header, runner load, canonical pick fixture, gate assertion, test_client POST, synthetic Slip History workbook, ACTION-04 integrity assertion, cap-constant assertion, class names)
    - 03-VALIDATION.md "Per-Task Verification Map" (the 9 required test node IDs and their behaviors)
    - scripts/dashboard_writes.py (the stub just created — tests import it)
  </read_first>
  <action>
    Create `scripts/test_dashboard_actions.py` using `unittest.TestCase`, run from `scripts/`. Add the sys.path-insert header (test_dashboard_views.py:22-24). Load the runner once in `setUpClass` via the importlib idiom (test_dynamic_gate8.py:1-13) and stub `runner.load_suppressed_edge_types = lambda: {}`. Provide a `_canonical_pick()` helper returning the full gate-ready pick dict (copy the field set from 03-PATTERNS.md lines 186-214) and a `_make_slip_history_wb(rows)` helper that builds an in-memory `openpyxl` Workbook with the exact 23 SLIP_HISTORY_HEADERS (03-PATTERNS.md lines 246-262), title "Slip History".

    Define exactly these classes and test methods (the node IDs must match 03-VALIDATION.md verbatim so the sampling map resolves):
    - `TestRefreshAction`: `test_refresh_triggers_subprocess` (ACTION-01a — POST `/action/refresh` with a valid task returns 302 and spawns async, never inline; patch/mock the subprocess spawn), `test_refresh_refused_when_locked` (ACTION-01b — when the lock probe reports held, POST flashes a "run already in progress" warning and does NOT spawn), `test_refresh_invalid_task_rejected` (ACTION-01c — a task NOT in `ALLOWED_TASKS` is rejected with no spawn).
    - `TestStatusEndpoint`: `test_status_fields` (ACTION-01d — GET `/api/status?task=<t>` returns JSON containing `locked` and `last_run`).
    - `TestMarkPlaced`: `test_mark_placed_additive` (ACTION-02 — calling the write helper on a temp `master_pnl.xlsx` adds `Placed`/`Placed At` columns to the right and toggles the matching `(Date, Slip ID)` row; existing columns unchanged).
    - `TestAddNote`: `test_add_note_additive` (ACTION-03 — write helper sets `Operator Note` on the matching row; the grading-owned `Notes` column value is byte-for-byte unchanged).
    - `TestActionFourHardLine`: `test_mark_placed_does_not_alter_gate_output` (ACTION-04a — `evaluate_no_bet_gates(pick, {})` returns the identical `(ok, skip, passed)` tuple before and after a write), `test_exposure_caps_unchanged` (ACTION-04b — assert `runner.PER_PLAYER_CAP == 6.0` and `runner.PER_GAME_CAP == 6.0`; do NOT reference `DAILY_EXPOSURE_CAP`), `test_write_only_touches_slip_history` (ACTION-04c — after a write, the Picks/Skipped Picks/CLV sheets are not added/modified).

    These are RED scaffolds: the tests should FAIL or ERROR now (write helpers raise NotImplementedError; routes `/action/*` and `/api/status` don't exist yet → 404/405). Write real assertions (not `self.skipTest`) so they go GREEN automatically as later waves land. Use `tempfile.NamedTemporaryFile(suffix=".xlsx", delete=False)` for write-test workbook paths and clean up via `addCleanup`. For the route tests, use `dashboard.app.test_client()`.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_dashboard_actions.py -q 2>&1 | tail -5; python3 -c "import ast,sys; t=ast.parse(open('test_dashboard_actions.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.ClassDef))}; req={'TestRefreshAction','TestStatusEndpoint','TestMarkPlaced','TestAddNote','TestActionFourHardLine','test_refresh_triggers_subprocess','test_refresh_refused_when_locked','test_refresh_invalid_task_rejected','test_status_fields','test_mark_placed_additive','test_add_note_additive','test_mark_placed_does_not_alter_gate_output','test_exposure_caps_unchanged','test_write_only_touches_slip_history'}; missing=req-names; sys.exit('MISSING: '+str(missing) if missing else print('ALL 9 NODE IDS PRESENT'))"</automated>
  </verify>
  <acceptance_criteria>
    - The file collects under pytest with no collection/import errors (`pytest test_dashboard_actions.py --collect-only` lists 9 tests).
    - All 14 required class/method names (5 classes + 9 test methods) are present (the AST check above passes).
    - Running the suite NOW yields RED: tests fail or error (NotImplementedError / 404), proving the tests are real and not vacuously passing.
    - The ACTION-04 cap test references `PER_PLAYER_CAP`/`PER_GAME_CAP` and does NOT reference `DAILY_EXPOSURE_CAP` (grep finds no `DAILY_EXPOSURE_CAP`).
    - Runner is loaded via `importlib.util.spec_from_file_location` (no `import sports_system_runner` top-level).
  </acceptance_criteria>
  <done>test_dashboard_actions.py exists, collects 9 tests, all currently RED, all required node IDs present, no DAILY_EXPOSURE_CAP reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | This plan creates test scaffolds + a stub + a config key. No untrusted input is processed at runtime yet. The secret_key is the only security-relevant addition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `app.secret_key` source | mitigate | Key from `DASHBOARD_SECRET_KEY` env or ephemeral `os.urandom(16)`; never hardcoded, never committed (matches the no-hardcoded-secrets rule in CLAUDE.md). Used only to sign localhost flash-session cookies. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase — Flask/openpyxl/stdlib already present and verified (03-RESEARCH "Package Legitimacy Audit"). No install task exists. |

Auth / CSRF: accepted / out-of-scope — single-operator `127.0.0.1`-only tool (REQUIREMENTS.md Out of Scope; design §8).
</threat_model>

<verification>
- `cd scripts && python3 -m pytest test_dashboard_actions.py -q` runs and reports RED (expected at this wave).
- `cd scripts && python3 -m pytest test_dashboard.py test_dashboard_data.py test_dashboard_views.py -q` still passes (no regression in existing dashboard tests from the secret_key addition).
- `dashboard_writes.py` imports without pulling in the runner module.
</verification>

<success_criteria>
- `scripts/test_dashboard_actions.py` exists with all 9 ACTION-01..04 test node IDs, currently RED.
- `scripts/dashboard_writes.py` exists as an importable stub (slips-only, ACTION-04 docstring).
- `dashboard.app.secret_key` is set; existing dashboard tests still pass.
- `dashboard_data.py` unchanged (read-only contract intact).
</success_criteria>

<output>
Create `.planning/phases/03-safe-actions/03-01-SUMMARY.md` when done.
</output>
