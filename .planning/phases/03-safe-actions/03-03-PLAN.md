---
phase: 03-safe-actions
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - scripts/dashboard.py
autonomous: true
requirements: [ACTION-01, ACTION-02, ACTION-03]

must_haves:
  truths:
    - "POST /action/refresh spawns the runner as a cron-style subprocess (python3 sports_system_runner.py --task <task>, cwd=scripts/) async via threading.Thread+Popen, never inline in the Flask worker (ACTION-01, D-02, success criterion 1)"
    - "POST /action/refresh is lock-aware: when the runner holds its fcntl.LOCK_EX on LOCK_FILE, it refuses and flashes 'run already in progress' instead of starting a concurrent run (ACTION-01, D-03, success criterion 2)"
    - "POST /action/refresh enforces an ALLOWED_TASKS frozenset whitelist (the D-01 curated 5) BEFORE any spawn; unknown tasks are rejected with no subprocess (security: --task injection mitigation)"
    - "POST /action/mark-placed and POST /action/add-note call the Plan-02 dashboard_writes helpers and surface success/failure via flash, then redirect (POST->redirect->render, D-06)"
    - "GET /api/status returns JSON with locked + write_in_progress + last_updated + last_run for the queried task, reusing existing signals (D-04)"
  artifacts:
    - path: "scripts/dashboard.py"
      provides: "_runner_is_locked probe + ALLOWED_TASKS + /action/refresh, /action/mark-placed, /action/add-note, /api/status routes"
      contains: "ALLOWED_TASKS"
      min_lines: 200
  key_links:
    - from: "scripts/dashboard.py (/action/refresh)"
      to: "sports_system_runner.py subprocess"
      via: "threading.Thread -> subprocess.Popen([PYTHON3, 'sports_system_runner.py', '--task', task], cwd=SCRIPTS_DIR, stdout/stderr=DEVNULL)"
      pattern: "subprocess.Popen"
    - from: "scripts/dashboard.py (_runner_is_locked)"
      to: "data/pnl/logs/sports_system_runner.lock"
      via: "fcntl.flock(f, LOCK_EX | LOCK_NB) non-blocking probe"
      pattern: "LOCK_EX \\| fcntl.LOCK_NB|LOCK_EX\\|fcntl.LOCK_NB|flock"
    - from: "scripts/dashboard.py (/action/mark-placed, /action/add-note)"
      to: "dashboard_writes.mark_placed / add_note"
      via: "import dashboard_writes; helper call wrapped in try/except -> flash"
      pattern: "dashboard_writes\\.(mark_placed|add_note)"
    - from: "scripts/dashboard.py (/api/status)"
      to: "dashboard_data.last_run_record"
      via: "last_run_record(task) for the polled task"
      pattern: "last_run_record"
---

<objective>
Add the four action endpoints to the existing Flask app (`dashboard.py`):
`POST /action/refresh` (lock-aware async subprocess), `POST /action/mark-placed`,
`POST /action/add-note` (both calling the Plan-02 write helpers), and
`GET /api/status` (reuses existing freshness + run-log signals). This turns the
ACTION-01 route/status tests GREEN and wires the mark-placed/add-note POST handlers.

Purpose: Deliver ROADMAP success criteria 1 and 2 (refresh runs as a cron-style
subprocess, never inline; refuses concurrent runs) and the POST→redirect→render
plumbing (D-06) that the Wave-4 UI templates attach to.

Output: `scripts/dashboard.py` (+ lock probe, ALLOWED_TASKS, 3 POST routes, status endpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-safe-actions/03-CONTEXT.md
@.planning/phases/03-safe-actions/03-RESEARCH.md
@.planning/phases/03-safe-actions/03-PATTERNS.md
@.planning/phases/03-safe-actions/03-01-SUMMARY.md
@.planning/phases/03-safe-actions/03-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live source; do not re-explore. -->

dashboard.py current state: import line 22 (`from flask import Flask, render_template`); `import os` (17), `import threading` (18); app + app.secret_key (32-33, secret_key added in Plan 01); `_freshness_context()` DRY helper (51-61) — DO NOT MODIFY it (used by all GET routes); existing GET routes index/slips/history (68-100); main() (107+).

New imports to add: extend flask import to `from flask import Flask, flash, jsonify, redirect, render_template, request, url_for`; add `import fcntl`, `import subprocess`, `import dashboard_writes`.

New module constants (add near HOST at line 27):
  PYTHON3: str = "/usr/local/bin/python3"
  SCRIPTS_DIR: Path = Path(__file__).resolve().parent   # requires `from pathlib import Path`
  RUNNER_LOCK_FILE: Path = SCRIPTS_DIR.parent / "data" / "pnl" / "logs" / "sports_system_runner.lock"
  ALLOWED_TASKS: frozenset[str] = frozenset({"nba_daily_picks","mlb_daily_picks","check_results","nba_prop_monitor","mlb_prop_monitor"})   # D-01 curated set

Lock probe (mirror of runner acquisition sports_system_runner.py:7937-7940, in non-blocking form):
  open RUNNER_LOCK_FILE with "r" (NOT "w" — must not truncate); try fcntl.flock(f, fcntl.LOCK_EX|fcntl.LOCK_NB);
  success -> flock(f, LOCK_UN); return False;  BlockingIOError -> return True;  FileNotFoundError/OSError -> return False.

Async spawn (03-RESEARCH Q3, Pitfall 3): threading.Thread(target=..., daemon=True).start() wrapping
  subprocess.Popen([PYTHON3, "sports_system_runner.py", "--task", task], cwd=str(SCRIPTS_DIR),
  stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)  — NO communicate(); DEVNULL is mandatory (pipe-buffer deadlock).

Route return-type annotation convention: `-> object` (Phase 1 convention for Flask responses).
Redirect targets: url_for("slips") / url_for("index") — never hardcoded path strings. POST -> 302.
dashboard_writes.mark_placed(date, slip_id, placed) / add_note(date, slip_id, note) — from Plan 02.
dashboard_data.last_run_record(task) -> dict|None — from Plan 02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lock probe + ALLOWED_TASKS + POST /action/refresh + GET /api/status</name>
  <behavior>
    - /action/refresh with a task in ALLOWED_TASKS and no run in progress: returns 302, spawns the runner subprocess async (Thread+Popen, DEVNULL), flashes success; the Flask worker returns immediately (never blocks on the run).
    - /action/refresh with a task NOT in ALLOWED_TASKS: returns 302, flashes an error, spawns NOTHING.
    - /action/refresh when _runner_is_locked() is True: returns 302, flashes a "run already in progress" warning, spawns NOTHING.
    - /api/status?task=<t>: returns 200 JSON with keys locked, write_in_progress, last_updated, last_run (last_run is last_run_record(task) or null).
  </behavior>
  <read_first>
    - scripts/dashboard.py (the file being modified — imports at 14-22, constants near 27, app+secret_key 32-33, _freshness_context 51-61 DO-NOT-MODIFY, GET route template at 68-76, main at 107)
    - scripts/sports_system_runner.py:7937-7940 (the runner's LOCK_EX acquisition — the probe mirrors this in non-blocking form)
    - 03-PATTERNS.md "dashboard.py" section (lines 289-398 — new imports, constants, lock probe helper, POST route pattern, /api/status endpoint) and "Runner lock probe" section (lines 450-475)
    - 03-RESEARCH.md Q2 (lock model — fcntl probe, NOT write_in_progress), Q3 (async spawn — Thread+Popen+DEVNULL), Q4 (/api/status shape), Pitfalls 1 (wrong lock check), 3 (DEVNULL), 5 (ALLOWED_TASKS enforcement)
    - scripts/test_dashboard_actions.py (the RED tests this task turns GREEN — TestRefreshAction x3, TestStatusEndpoint)
  </read_first>
  <action>
    Modify `scripts/dashboard.py`. Add `from pathlib import Path`, `import fcntl`, `import subprocess` to the import block; extend the flask import to include `flash, jsonify, redirect, request, url_for`. Add the module constants `PYTHON3`, `SCRIPTS_DIR`, `RUNNER_LOCK_FILE`, and `ALLOWED_TASKS` (the D-01 curated five) near the `HOST` constant. Add a private `_runner_is_locked() -> bool` helper after `_freshness_context()` that opens `RUNNER_LOCK_FILE` with mode `"r"` (never `"w"` — must not truncate the runner's lock file) and probes with `fcntl.flock(f, fcntl.LOCK_EX | fcntl.LOCK_NB)`: on success release with `LOCK_UN` and return `False`; on `BlockingIOError` return `True`; on `FileNotFoundError`/`OSError` return `False`. Do NOT use `dashboard_data.write_in_progress()` for this check (Pitfall 1 — it probes per-workbook locks, not the runner lock).

    Add `@app.route("/action/refresh", methods=["POST"])` `action_refresh() -> object`: read `task = request.form.get("task", "")`; if `task not in ALLOWED_TASKS` flash an error and `redirect(request.referrer or url_for("index"))` with NO spawn (Pitfall 5); else if `_runner_is_locked()` flash a "run already in progress" warning and redirect with NO spawn (D-03); else spawn via `threading.Thread(target=lambda: subprocess.Popen([PYTHON3, "sports_system_runner.py", "--task", task], cwd=str(SCRIPTS_DIR), stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL), daemon=True).start()`, flash a success message, and redirect. The Popen MUST pass `DEVNULL` for both streams (Pitfall 3) and MUST NOT call `communicate()` (stays non-blocking, D-02).

    Add `@app.route("/api/status")` `api_status() -> object`: read optional `task = request.args.get("task")`; return `jsonify({"locked": _runner_is_locked(), "write_in_progress": dashboard_data.write_in_progress(), "last_updated": dashboard_data.last_updated_hhmm(), "last_run": dashboard_data.last_run_record(task) if task else None})`. Do NOT modify `_freshness_context()`.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest "test_dashboard_actions.py::TestRefreshAction" "test_dashboard_actions.py::TestStatusEndpoint" -q && grep -q "ALLOWED_TASKS" dashboard.py && grep -q "subprocess.DEVNULL" dashboard.py && ! grep -q "communicate(" dashboard.py && ! grep -q "write_in_progress()" dashboard.py && echo "REFRESH+STATUS GREEN, lock/spawn-safe" || (grep -c "write_in_progress" dashboard.py && echo "note: write_in_progress allowed in /api/status only — verify it is NOT used as the refresh lock check")</automated>
  </verify>
  <acceptance_criteria>
    - `TestRefreshAction` (3 tests) and `TestStatusEndpoint` (1 test) pass GREEN.
    - `ALLOWED_TASKS` is a frozenset of exactly the D-01 five tasks; the whitelist is checked before any `subprocess.Popen` (Pitfall 5).
    - The refresh spawn uses `threading.Thread(..., daemon=True)` + `subprocess.Popen(... stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)` and no `communicate()` (non-blocking, D-02, Pitfall 3).
    - `_runner_is_locked()` probes `RUNNER_LOCK_FILE` via `fcntl.flock(... LOCK_EX | LOCK_NB)`; the refresh lock check is `_runner_is_locked()`, NOT `dashboard_data.write_in_progress()` (Pitfall 1). (`write_in_progress` may still appear inside `/api/status` as an informational field only.)
    - `/api/status` returns JSON with `locked`, `write_in_progress`, `last_updated`, `last_run`.
    - `_freshness_context()` is unchanged; existing GET routes still render (`python3 -m pytest test_dashboard.py -q`).
  </acceptance_criteria>
  <done>Lock probe + ALLOWED_TASKS + /action/refresh + /api/status added; TestRefreshAction + TestStatusEndpoint GREEN; non-blocking + lock-aware + whitelisted; _freshness_context untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add POST /action/mark-placed + POST /action/add-note handlers</name>
  <read_first>
    - scripts/dashboard.py (now with the imports/constants from Task 1)
    - scripts/dashboard_writes.py (the Plan-02 helpers: mark_placed(date, slip_id, placed), add_note(date, slip_id, note))
    - 03-PATTERNS.md "dashboard.py" POST→redirect→render pattern (lines 360-376) and the mark-placed route example (357-376)
    - 03-RESEARCH.md "Pattern 2: Mark-Placed POST Route" (lines 598-615) and Security Domain (path-traversal note: validate form fields, never use in filesystem paths)
    - scripts/test_dashboard_actions.py (TestMarkPlaced / TestAddNote — confirm the form field names the routes must read: date, slip_id, placed; date, slip_id, note)
  </read_first>
  <action>
    Add two POST routes to `scripts/dashboard.py`. `@app.route("/action/mark-placed", methods=["POST"])` `action_mark_placed() -> object`: read `date`, `slip_id` from `request.form` and `placed = request.form.get("placed", "0") == "1"`; if `date` or `slip_id` is empty, flash an error and `redirect(url_for("slips"))` with no write; else call `dashboard_writes.mark_placed(date, slip_id, placed)` inside `try/except Exception as exc` — on success flash `"Slip placed."`/`"Slip unplaced."`, on exception flash `f"Save failed: {exc}"` (the helper lets `WorkbookAccessError` propagate, per Plan 02) — then `redirect(url_for("slips"))`. `@app.route("/action/add-note", methods=["POST"])` `action_add_note() -> object`: read `date`, `slip_id`, `note` from `request.form`; if `date`/`slip_id` empty flash an error and redirect with no write; else call `dashboard_writes.add_note(date, slip_id, note)` in the same try/except→flash pattern; redirect to `url_for("slips")`. Both follow POST→redirect→render (D-06, no inline confirm per D-05 — mark-placed and add-note apply directly). The `date`/`slip_id` form values are passed only to the write helper for cell matching — never interpolated into a filesystem path (path-traversal mitigation).
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest "test_dashboard_actions.py::TestMarkPlaced" "test_dashboard_actions.py::TestAddNote" -q && grep -q "dashboard_writes.mark_placed" dashboard.py && grep -q "dashboard_writes.add_note" dashboard.py && echo "MARK-PLACED+ADD-NOTE ROUTES GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `TestMarkPlaced` and `TestAddNote` route-level tests pass GREEN (POST returns 302; helper invoked).
    - Both routes call the `dashboard_writes` helpers inside `try/except` and flash a result (success or `Save failed: ...`).
    - Empty `date`/`slip_id` short-circuits with an error flash and NO write call.
    - Both routes `redirect(url_for("slips"))` (no hardcoded path), per POST→redirect→render (D-06).
    - No form field is interpolated into a filesystem path (the only path used is the fixed `master_pnl.xlsx` inside the helper).
  </acceptance_criteria>
  <done>/action/mark-placed and /action/add-note added; TestMarkPlaced + TestAddNote GREEN; POST->redirect->render with flash on success/failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (127.0.0.1) → POST handlers | Form fields `task`, `date`, `slip_id`, `placed`, `note` are untrusted input crossing into the Flask routes. |
| POST /action/refresh → runner subprocess | The `task` field selects which runner task is spawned — the highest-risk crossing (arbitrary command/task injection). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering / Elevation | `task` form field → subprocess `--task` | mitigate | `ALLOWED_TASKS` frozenset whitelist enforced on the POST handler BEFORE any `subprocess.Popen`; unknown task → flash error, no spawn (Pitfall 5; research Security Domain). The runner binary + args are fixed (`PYTHON3`, `"sports_system_runner.py"`, `"--task"`, task) — only the whitelisted task string varies; no shell, no `shell=True`. |
| T-03-06 | Tampering | `date` / `slip_id` form fields | mitigate | Passed only to the write helper for `(Date, Slip ID)` cell matching; never interpolated into a filesystem path. The single write path is the fixed `master_pnl.xlsx`. |
| T-03-07 | Denial of Service | concurrent runner spawn | mitigate | `_runner_is_locked()` fcntl probe refuses a concurrent run; the runner's own `fcntl.LOCK_EX` is the kernel-level backstop (D-03). |
| T-03-08 | DoS (resource) | Popen pipe-buffer deadlock | mitigate | `stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL` on the fire-and-forget spawn (Pitfall 3). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (03-RESEARCH "Package Legitimacy Audit"). |

Auth / CSRF: accepted / out-of-scope — single-operator `127.0.0.1`-only tool with no external reach (REQUIREMENTS.md Out of Scope; design §8). Documented assumption, not an oversight.
</threat_model>

<verification>
- `cd scripts && python3 -m pytest test_dashboard_actions.py -q` — all 9 ACTION-01..04 tests now GREEN (write tests from Plan 02 + route/status tests from this plan).
- `cd scripts && python3 -m pytest test_dashboard.py test_dashboard_views.py test_dashboard_data.py -q` — no regression in existing dashboard tests.
- Anti-pattern grep on `dashboard.py`: `subprocess.DEVNULL` present, no `communicate(`, refresh lock check is `_runner_is_locked()` (not `write_in_progress()`), `ALLOWED_TASKS` enforced before `Popen`.
</verification>

<success_criteria>
- `/action/refresh` spawns the cron-style runner subprocess async (never inline), lock-aware (refuses concurrent runs), whitelist-guarded (ACTION-01, success criteria 1 & 2).
- `/action/mark-placed` and `/action/add-note` invoke the Plan-02 helpers and flash success/failure (ACTION-02/03 routes wired).
- `/api/status` returns the reused-signals JSON for the D-04 poll.
- All 9 `test_dashboard_actions.py` tests GREEN; no regression in existing dashboard tests.
</success_criteria>

<output>
Create `.planning/phases/03-safe-actions/03-03-SUMMARY.md` when done.
</output>
