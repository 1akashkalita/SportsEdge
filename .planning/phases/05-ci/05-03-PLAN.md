---
phase: 05-ci
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scripts/repro_ci_regression.py
autonomous: true
requirements: [CI-01]

must_haves:
  truths:
    - "ROADMAP success criterion 3: a deliberate regression injected into a TESTED code path causes the gate to go RED and surfaces the failure; reverting it restores green"
    - "The proof follows Phase-3 RES-04 fault-injection rigor (mirroring repro_broken_pipe.py + test_res02_pipe_reclassify.py): it is constructed so it CANNOT pass without the gate surfacing the injected regression"
    - "CI-01: the proof exercises the same run_ci_gate.py gate that reports pass/fail, demonstrating the gate actually catches a real regression rather than only running green"
  artifacts:
    - path: "scripts/repro_ci_regression.py"
      provides: "Criterion-3 harness: inject a one-line fault into a tested code path → run run_ci_gate.py → assert it exits non-zero (RED) → revert → assert exit 0 (GREEN); tri-state exit codes (PASS / INFRA-FAIL / REGRESSION-NOT-CAUGHT)"
      contains: "run_ci_gate"
      min_lines: 70
  key_links:
    - from: "scripts/repro_ci_regression.py"
      to: "scripts/run_ci_gate.py"
      via: "subprocess run of the gate after fault injection, asserting non-zero"
      pattern: "run_ci_gate"
    - from: "scripts/repro_ci_regression.py"
      to: "a tested code path"
      via: "temporary one-line fault injection + guaranteed revert (finally/restore)"
      pattern: "(finally|restore|revert)"
---

<objective>
Build the criterion-3 deliberate-regression proof: a fault-injection harness
(`scripts/repro_ci_regression.py`) that injects a fault into a TESTED code path, runs the
Wave-1 `run_ci_gate.py`, confirms the gate goes RED (non-zero exit, failure surfaced),
reverts the fault, and confirms the gate returns GREEN — constructed in the Phase-3 RES-04
fault-injection style so it cannot pass without the gate actually catching the regression.

Purpose: ROADMAP success criterion 3 requires PROVING the gate surfaces a deliberate
regression, not just that it runs green. This is the RES-04 rigor CONTEXT mandates (the test
must not be able to pass without surfacing the regression) applied to the CI gate itself.

Output: `scripts/repro_ci_regression.py` — a fault-injection-by-construction harness with a
documented FAILS-without / PASSES-with block and tri-state exit codes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ci/05-CONTEXT.md
@.planning/phases/05-ci/05-PATTERNS.md
@.planning/phases/05-ci/05-01-SUMMARY.md
@.planning/phases/03-resilience/03-CONTEXT.md

<interfaces>
<!-- The gate this proof drives + the fault-injection style to mirror. -->
scripts/run_ci_gate.py — main() returns 0 on green / non-zero on failure;
invoked as `python3 run_ci_gate.py` from scripts/. This is the gate the proof drives RED then GREEN.

Fault-injection-by-construction rigor (from scripts/repro_broken_pipe.py lines 98-105 and
scripts/test_res02_pipe_reclassify.py lines 7-59): the harness docstring carries an explicit
"FAILS without the fix / PASSES with the fix" (here: "gate goes RED when fault injected /
GREEN when reverted") block, and maps DISTINCT exit codes to PASS / INFRA-FAIL / REGRESSION-
NOT-CAUGHT, each with a printed explanation — so the proof self-documents and cannot pass by
accident.

Tri-state exit-code precedent (repro_broken_pipe.py lines 98-105):
  0 — PASS (the criterion-3 expectation: gate RED on fault, GREEN on revert)
  1 — INFRA FAIL (could not run the gate / could not inject or revert cleanly)
  2 — REGRESSION NOT CAUGHT (gate stayed GREEN despite the injected fault — the failure mode)

VERIFIED FACT (planner, 2026-06-21): the denylist subset is clean green (272 passed, exit 0),
so a freshly-injected fault into a tested path is the ONLY new failure the gate would surface —
a clean RED/GREEN signal with no ambiguity from the 2 known (excluded) projection failures.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criterion-3 deliberate-regression fault-injection harness</name>
  <files>scripts/repro_ci_regression.py</files>
  <read_first>
    - scripts/repro_broken_pipe.py (the full fault-injection harness shape: module docstring with mechanism + exit-code semantics, portable paths, subprocess spawn with sys.executable + cwd, tri-state exit codes lines 98-105)
    - scripts/test_res02_pipe_reclassify.py (lines 7-59: the fault-injection-by-construction docstring rigor — explicit FAILS-without / PASSES-with reasoning that makes the proof unable to pass by accident)
    - scripts/run_ci_gate.py (the Wave-1 gate the harness drives RED then GREEN)
    - .planning/phases/03-resilience/03-CONTEXT.md (the RES-04 fault-injection rigor this proof must mirror)
  </read_first>
  <action>
    Create scripts/repro_ci_regression.py as a fault-injection harness proving ROADMAP criterion 3.
    Derive SCRIPTS_DIR = Path(__file__).resolve().parent (no hardcoded paths). The module docstring
    states the mechanism and carries the RES-04-style "gate goes RED when fault injected / GREEN when
    reverted — cannot pass without the gate surfacing the regression" block plus the tri-state exit-code
    legend. Mechanism: pick a TESTED code path (a function or constant covered by a test that runs in the
    denylist subset — e.g. a value in slip_payouts.py asserted by test_slip_payouts.py, or a helper in
    special_line_value.py covered by test_special_line_value.py); temporarily inject a one-line fault
    (mutate the source on disk, or monkeypatch via a temp shim — prefer in-place source edit captured as a
    text backup so the revert is exact), run `run_ci_gate.py` via subprocess([sys.executable,
    "run_ci_gate.py"], cwd=str(SCRIPTS_DIR)) and assert it returns NON-ZERO (gate RED, regression surfaced);
    then GUARANTEE revert in a finally/try block (restore the exact original source bytes) and run the gate
    again, asserting it returns 0 (gate GREEN). Map outcomes to tri-state exit codes: 0 = PASS (RED-then-
    GREEN as expected), 1 = INFRA FAIL (gate unrunnable / inject or revert failed), 2 = REGRESSION NOT
    CAUGHT (gate stayed GREEN despite the fault). Print a clear explanation for each. The revert MUST be
    unconditional (finally) so the harness never leaves a real-money source file mutated. NEVER hardcode
    /Users/... or /usr/local/bin/python3 (test_def02 guards this). Do NOT change gate logic, pick outputs,
    or workbook schema — the injected fault is temporary and reverted within the same run.
    Whether this lives as the one-shot proof run here (documented in the SUMMARY) is fine — it is a
    standing re-runnable harness either way (Claude's Discretion per CONTEXT).
  </action>
  <verify>
    <automated>cd scripts && python3 repro_ci_regression.py; echo "criterion-3 proof exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd scripts && python3 repro_ci_regression.py` exits 0 (PASS): it injected a fault into a tested path, observed the gate go RED (non-zero), reverted, and observed the gate return GREEN (0).
    - The harness drives the actual gate: `grep -c 'run_ci_gate' scripts/repro_ci_regression.py` >= 1 and it spawns it with `sys.executable` + `cwd=str(SCRIPTS_DIR)`.
    - The revert is unconditional: the harness restores the original source in a `finally`/guaranteed path (grep: `grep -cE 'finally|restore|revert' scripts/repro_ci_regression.py` >= 1), and after the run the targeted source file is byte-identical to before (`git diff --stat` shows no residual change to the injected file).
    - Tri-state exit codes are documented and distinct (PASS=0, INFRA-FAIL, REGRESSION-NOT-CAUGHT) per the RES-04 rigor; the docstring carries the "RED on fault / GREEN on revert" by-construction reasoning.
    - No hardcoded paths: `grep -RnE "/usr/local/bin/python3|/Users/" scripts/repro_ci_regression.py` returns NOTHING.
    - After the run, `cd scripts && python3 run_ci_gate.py` still exits 0 (the fault was fully reverted; no residual regression).
  </acceptance_criteria>
  <done>repro_ci_regression.py proves the gate goes RED on a deliberate fault in a tested path and GREEN on revert, with guaranteed restore and tri-state exit codes, leaving no residual source change — ROADMAP criterion 3 satisfied with RES-04 rigor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repro_ci_regression.py → project source | the harness temporarily mutates a tested source file on the operator's own machine, then reverts it. |
| repro_ci_regression.py → run_ci_gate.py child | the harness spawns the gate to observe RED/GREEN. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | a temporary fault is injected into a real-money source file | mitigate | The revert is UNCONDITIONAL (finally), restoring the exact original bytes; an acceptance criterion verifies the file is byte-identical (git diff clean) after the run. The fault lives only for the duration of one gate run. |
| T-05-08 | Denial of service | an interrupted harness leaves a mutated source file | mitigate | Restore from an in-memory/text backup in a finally block so even on exception the source is restored; the harness reports INFRA-FAIL (exit 1) if revert cannot complete. |
| T-05-09 | Tampering | harness executes arbitrary test code via the gate | accept | Same as Plan 01 T-05-01 — running your own suite is inherent to a CI proof on a single-operator machine; accepted. |

No HIGH-severity threats. The one real risk (a left-behind source mutation) is mitigated by the guaranteed revert + a byte-identical post-run verification.
</threat_model>

<verification>
- `cd scripts && python3 repro_ci_regression.py` exits 0 (gate went RED on the injected fault, GREEN after revert).
- The targeted source file is byte-identical after the run (`git diff` clean).
- `cd scripts && python3 run_ci_gate.py` still exits 0 after the proof (no residual regression).
- No hardcoded interpreter or user path in the new file (test_def02 stays green).
</verification>

<success_criteria>
- repro_ci_regression.py proves, by construction, that a deliberate regression in a tested code path turns the gate RED and surfaces the failure, and that reverting restores GREEN — ROADMAP success criterion 3, with Phase-3 RES-04 fault-injection rigor.
- The harness guarantees revert (finally) so it never leaves the real-money source mutated; tri-state exit codes self-document PASS / INFRA-FAIL / REGRESSION-NOT-CAUGHT.
</success_criteria>

<output>
Create `.planning/phases/05-ci/05-03-SUMMARY.md` when done (record the criterion-3 RED-then-GREEN proof: which tested path was faulted, the gate exit codes observed, and confirmation of clean revert).
</output>
