---
phase: 03-slips-only-bankroll
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sports_system_runner.py
  - scripts/test_dynamic_gate8.py
autonomous: true
requirements: [BANKROLL-02]
must_haves:
  truths:
    - "D-07: the Gate-8 GLOBAL exposure caps are removed — DAILY_EXPOSURE_CAP, BASE/STRONG/EXCEPTIONAL/ABSOLUTE_DAILY_CAP, the dynamic-cap skip ('GATE 8 — DYNAMIC EXPOSURE CAP'), and the daily_cap arg at the generate_picks call site no longer block picks"
    - "D-07: ALL other gates stay intact — evaluate_no_bet_gates (G1-G7, G9, G12, MLB sub-gates) is untouched, and the per-player/per-sport/per-game/per-correlation CONCENTRATION caps ('GATE 8 — CONCENTRATION CAP') are PRESERVED (not authorized for removal; flagged for operator review)"
    - "D-08: no daily slip-exposure budget is introduced; per-slip tiers + zero-floor (other plans) remain the only risk controls"
  artifacts:
    - path: "scripts/sports_system_runner.py"
      provides: "allocate_eligible_candidates and generate_picks with the global exposure cap removed and concentration caps preserved"
      contains: "GATE 8 — CONCENTRATION CAP"
    - path: "scripts/test_dynamic_gate8.py"
      provides: "Updated regression: no dynamic-cap skip rows after removal; concentration caps still block; prop-data-source boundary tests unchanged"
      contains: "DYNAMIC EXPOSURE CAP"
  key_links:
    - from: "scripts/sports_system_runner.py generate_picks call site (~:3326)"
      to: "allocate_eligible_candidates"
      via: "daily_cap argument removed"
      pattern: "allocate_eligible_candidates"
---

<objective>
Remove the single sanctioned pick-selection change of Phase 3: the Gate-8 GLOBAL exposure caps (D-07). This deletes the `DAILY_EXPOSURE_CAP` constant and the `BASE/STRONG/EXCEPTIONAL/ABSOLUTE_DAILY_CAP` tier constants, the dynamic-cap skip block inside `allocate_eligible_candidates`, and the `daily_cap` plumbing through `generate_picks`. It does NOT touch the no-bet gate gauntlet (`evaluate_no_bet_gates`) and explicitly PRESERVES the per-player/per-sport/per-game/per-correlation concentration caps, which D-07 did not authorize removing.

Purpose: With slips bankroll-managed by per-slip confidence tiers + the zero-floor (D-03/D-04/D-05) instead of a daily exposure cap (D-08), the global Gate-8 cap that artificially limited approved picks to ~4/day is no longer the risk control. Removing it lets more vetted picks flow through to the slip universe.

Output: Edited `scripts/sports_system_runner.py` (cap removal, confined to the pick-generation path) and updated `scripts/test_dynamic_gate8.py` (assertions that previously checked specific cap values now assert that no dynamic-cap skip rows appear, while concentration-cap and prop-data-source-boundary tests stay green).

NOTE TO REVIEWER (open question from research, A1): D-07/CONTEXT.md is silent on the concentration caps. This plan PRESERVES them by default. If the operator wants them removed too, that is a scope expansion to confirm before/at execution review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-slips-only-bankroll/03-CONTEXT.md
@.planning/phases/03-slips-only-bankroll/03-RESEARCH.md
@.planning/phases/03-slips-only-bankroll/03-PATTERNS.md

<interfaces>
<!-- Exact removal targets, all VERIFIED against live source in 03-RESEARCH.md.
     The cap lives in the GENERATION path, NOT in evaluate_no_bet_gates (:2416 — do not touch). -->

Constants to remove (top-of-file + near allocate_eligible_candidates):
  - DAILY_EXPOSURE_CAP = 10.0            (:91)
  - BASE_DAILY_CAP = 10.0                (:2543)
  - STRONG_DAILY_CAP = 12.0             (:2544)
  - EXCEPTIONAL_DAILY_CAP = 15.0        (:2545)
  - ABSOLUTE_DAILY_CAP = 15.0           (:2546)

allocate_eligible_candidates (def at :2661, signature has daily_cap: float | None = None):
  - Remove the dynamic_cap computation and the dynamic-cap skip block:
      `if exposure + units > dynamic_cap or exposure + units > ABSOLUTE_DAILY_CAP:`
      -> skip_record(pick, "GATE 8 — DYNAMIC EXPOSURE CAP", ...) ; would_have_played=True ; blocked_dynamic += 1 ; continue
  - PRESERVE the concentration-cap block immediately after it: per_sport/per_player/per_game/per_corr/MAX_SAME_PLAYER_PROPS checks emitting skip_record(pick, "GATE 8 — CONCENTRATION CAP", cap_reason). Those use PER_SPORT_CAP/PER_PLAYER_CAP/PER_GAME_CAP/CORRELATION_GROUP_CAP — keep them and their constants.
  - board_quality_from_eligible() feeds the now-removed dynamic_cap; simplify or leave its classification for logging, but it must no longer gate picks.

generate_picks (def at :2774):
  - Remove the `daily_cap: float = DAILY_EXPOSURE_CAP` parameter (or neutralize so nothing references DAILY_EXPOSURE_CAP).
  - Call site (~:3318-3326): remove the `daily_cap=DAILY_EXPOSURE_CAP` argument.
  - Return-dict keys referencing the dynamic cap (`daily_cap`, `dynamic_daily_cap`) -> remove or set to None; update any log line referencing dynamic_cap / blocked_dynamic.

NOT affected (do NOT edit): evaluate_no_bet_gates (:2416), clear_generated_rows / clear_today_rows
(rerun-clears-own-rows keys off date + GENERATED_MARKER, not cap), global_daily_exposure() (informational).

build_slips.py downstream (document only, NO code change): _collect_gate8() matches
GATE8_VETTED_MARKERS = ("GATE 8 — DYNAMIC EXPOSURE CAP", "GATE 8 — CONCENTRATION CAP"). After removal the
dynamic-cap half is a no-op; the concentration-cap half still populates the vetted universe. Degrades gracefully.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Gate-8 global exposure caps from the pick-generation path</name>
  <files>scripts/sports_system_runner.py</files>
  <read_first>
    - scripts/sports_system_runner.py line 91 (DAILY_EXPOSURE_CAP) and lines 2543-2546 (BASE/STRONG/EXCEPTIONAL/ABSOLUTE_DAILY_CAP constants)
    - scripts/sports_system_runner.py lines 2661-2772 (allocate_eligible_candidates — locate the dynamic_cap computation, the dynamic-cap skip block, and the concentration-cap block that must be preserved)
    - scripts/sports_system_runner.py lines 2774-2790 (generate_picks signature with daily_cap param) and lines 3310-3330 (the generate_picks call site passing daily_cap=DAILY_EXPOSURE_CAP)
    - scripts/sports_system_runner.py line 2416 (evaluate_no_bet_gates — confirm it does NOT reference the cap; do NOT edit it)
    - .planning/phases/03-slips-only-bankroll/03-RESEARCH.md lines 443-475 (Q6 full change enumeration — the authoritative removal/preserve list)
    - .planning/phases/03-slips-only-bankroll/03-PATTERNS.md lines 332-407 (exact constants/blocks to remove vs preserve)
  </read_first>
  <action>
    In `scripts/sports_system_runner.py`, remove the global Gate-8 exposure cap per D-07, confined to the pick-generation path. (1) Delete the `DAILY_EXPOSURE_CAP = 10.0` constant at :91 and the `BASE_DAILY_CAP`/`STRONG_DAILY_CAP`/`EXCEPTIONAL_DAILY_CAP`/`ABSOLUTE_DAILY_CAP` constants at :2543-2546. (2) In `allocate_eligible_candidates` (:2661), remove the `dynamic_cap` computation and the dynamic-cap skip block that emits `skip_record(pick, "GATE 8 — DYNAMIC EXPOSURE CAP", ...)` / sets `would_have_played=True` / increments `blocked_dynamic` / `continue`; also drop the now-dead `daily_cap` parameter usage and any `daily_cap`/`dynamic_daily_cap` keys from its return dict (set to None if a key must stay for shape stability), and fix any log line referencing `dynamic_cap`/`blocked_dynamic`. (3) PRESERVE verbatim the concentration-cap block right after it — the `per_sport`/`per_player`/`per_game`/`per_corr`/`MAX_SAME_PLAYER_PROPS` checks emitting `skip_record(pick, "GATE 8 — CONCENTRATION CAP", cap_reason)` and the `PER_SPORT_CAP`/`PER_PLAYER_CAP`/`PER_GAME_CAP`/`CORRELATION_GROUP_CAP` constants — D-07 did not authorize removing them. (4) In `generate_picks` (:2774) remove the `daily_cap: float = DAILY_EXPOSURE_CAP` parameter and at the call site (~:3326) remove the `daily_cap=DAILY_EXPOSURE_CAP` argument; if `board_quality_from_eligible()` only fed the dynamic cap, leave its classification for logging but ensure it no longer gates picks. Do NOT touch `evaluate_no_bet_gates` (:2416), `clear_generated_rows`/`clear_today_rows`, or `global_daily_exposure()`. Do NOT change `build_slips.py` — its `_collect_gate8` degrades gracefully (document the no-op in the SUMMARY). After editing, ensure no remaining reference to `DAILY_EXPOSURE_CAP` exists anywhere in the file.
  </action>
  <verify>
    <automated>cd scripts && python3 -c "import ast,sys; src=open('sports_system_runner.py').read(); assert 'DAILY_EXPOSURE_CAP' not in src, 'DAILY_EXPOSURE_CAP still referenced'; assert 'GATE 8 — CONCENTRATION CAP' in src, 'concentration cap was removed (must be preserved)'; assert 'DYNAMIC EXPOSURE CAP' not in src, 'dynamic exposure cap skip still present'; ast.parse(src); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DAILY_EXPOSURE_CAP' scripts/sports_system_runner.py` returns 0 (constant and all references gone)
    - `grep -c 'GATE 8 — DYNAMIC EXPOSURE CAP' scripts/sports_system_runner.py` returns 0 (dynamic-cap skip removed)
    - `grep -c 'GATE 8 — CONCENTRATION CAP' scripts/sports_system_runner.py` returns >= 1 (concentration caps preserved)
    - `grep -v '^#' scripts/sports_system_runner.py | grep -c 'evaluate_no_bet_gates'` is unchanged from baseline (gate gauntlet untouched); the function body at :2416 is byte-identical
    - `python3 -c "import ast; ast.parse(open('scripts/sports_system_runner.py').read())"` succeeds (file still parses)
    - No edit was made to `build_slips.py`
  </acceptance_criteria>
  <done>Global Gate-8 exposure caps removed from the generation path; concentration caps and the full no-bet gate gauntlet preserved; file parses and no DAILY_EXPOSURE_CAP reference remains.</done>
</task>

<task type="auto">
  <name>Task 2: Update test_dynamic_gate8.py for post-removal regression</name>
  <files>scripts/test_dynamic_gate8.py</files>
  <read_first>
    - scripts/test_dynamic_gate8.py (full file — identify which tests assert specific dynamic-cap values vs which assert concentration-cap behavior vs the prop-data-source-boundary tests)
    - .planning/phases/03-slips-only-bankroll/03-PATTERNS.md lines 409-457 (exact assertion updates: which tests change, the replacement assertions, which tests stay unchanged)
    - scripts/sports_system_runner.py allocate_eligible_candidates (post-edit — the return-dict shape after cap removal, so updated assertions match reality)
  </read_first>
  <action>
    Update `scripts/test_dynamic_gate8.py` so the suite is green after D-07. Replace the assertions in the dynamic-cap-value tests (those asserting `res["dynamic_daily_cap"] == 10.0/12.0/15.0` or `global_exposure <= 10.0/15.0`) — instead assert that NO skipped row carries `"GATE 8 — DYNAMIC EXPOSURE CAP"` after removal (e.g. a `test_no_dynamic_cap_skip_rows_after_removal` that builds 12 spread-out candidates, runs the allocator, and asserts the dynamic-skip list is empty). Add a `test_concentration_caps_still_block_overexposure` that stacks candidates on the same player and asserts a concentration-cap skip still fires (proving D-07 preserved them). KEEP unchanged every concentration-cap test (per-player/per-game/per-sport) and the entire `PropDataSourceBoundaryTests` class — those are unaffected by D-07. Do not introduce watch-mode flags. The whole file must pass under `python3 -m pytest test_dynamic_gate8.py -x`.
  </action>
  <verify>
    <automated>cd scripts && python3 -m pytest test_dynamic_gate8.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest test_dynamic_gate8.py -x` exits 0 (whole file green after cap removal)
    - A test exists asserting NO `GATE 8 — DYNAMIC EXPOSURE CAP` skip row appears post-removal
    - A test asserts concentration caps still block over-concentration (D-07 preserved them)
    - The `PropDataSourceBoundaryTests` class and the concentration-cap tests are unchanged in intent and still green
  </acceptance_criteria>
  <done>test_dynamic_gate8.py green: dynamic-cap value assertions replaced with absence-of-dynamic-skip assertions; concentration-cap and prop-data-source-boundary tests preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate picks -> approved slate | removing a risk control (the global exposure cap) changes which picks flow into the real-money slip universe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | accidental removal of a NON-exposure gate (concentration caps or a no-bet gate) silently changing real-money pick outputs | mitigate | Scope is fenced to the global exposure cap only; `evaluate_no_bet_gates` (:2416) is untouched; grep gates assert `GATE 8 — CONCENTRATION CAP` survives and `evaluate_no_bet_gates` count is unchanged; `test_concentration_caps_still_block_overexposure` proves concentration caps still fire |
| T-03-04 | Denial of Service | unbounded approved-pick count blowing the 660s cron budget | accept | D-08 explicitly chooses no daily cap; per-slip tiers + zero-floor are the controls; pick generation is in-memory and fast, well under budget; eligible board is already bounded (~75) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no legitimacy gate needed |

No `high`-severity unmitigated threats remain. The concentration-cap preservation is the explicit guard against (c) accidental removal of a non-exposure gate.
</threat_model>

<verification>
- `cd scripts && python3 -m pytest test_dynamic_gate8.py -x` exits 0
- `grep -c 'DAILY_EXPOSURE_CAP' scripts/sports_system_runner.py` returns 0
- `grep -c 'GATE 8 — CONCENTRATION CAP' scripts/sports_system_runner.py` returns >= 1
- `grep -c 'GATE 8 — DYNAMIC EXPOSURE CAP' scripts/sports_system_runner.py` returns 0
- `python3 -c "import ast; ast.parse(open('scripts/sports_system_runner.py').read())"` succeeds
</verification>

<success_criteria>
- Gate-8 GLOBAL exposure caps (D-07) are removed from the generation path; no `DAILY_EXPOSURE_CAP` reference remains.
- Concentration caps and the full no-bet gate gauntlet are preserved (D-07 fence; A1 open question handled conservatively).
- No daily slip-exposure budget added (D-08).
- `test_dynamic_gate8.py` is green with updated dynamic-cap regression and preserved concentration / prop-data-source-boundary tests.
</success_criteria>

<output>
Create `.planning/phases/03-slips-only-bankroll/03-02-SUMMARY.md` when done. In the SUMMARY, explicitly note (a) the build_slips._collect_gate8 dynamic-cap half is now a no-op (no code change needed) and (b) the concentration-cap-preservation open question (A1) for operator awareness.
</output>
